# P2 快照与插件档案：实施计划

> 状态：进行中｜日期：2026-09-24｜所属：M0-P2｜依据：`02-P2-快照与插件档案.md` §3、§6

## 目标

按 Phase 文档 §6 完成 S1–S6，产出 V03、V04、V05 的证据、插件档案 v1 草案与报告。

## 涉及文件

- `spikes/m0/`：server、profiles、harness、experiments、e2e、fixtures、public
- `docs/v0.1/M0-技术验证/reports/P2-验证报告.md`、`reports/插件档案v1.md`（新建）

## 任务

M0 不为验证工程补测试；每个任务以"脚本能跑通、结果文件生成"为完成标志。

**S1 验证工程增量**

- [ ] 1. `server/serve.ts`：内存文档存储（GET/PUT、复制）
- [ ] 2. `src/profiles/`：按插件组重构候选档案，档案 id 改为 `*@1-draft`；支持 `without`
- [ ] 3. `src/harness/`：`doc` 参数、`resourceHooks()`、`persist()`、`builders` 挂载点；`resource-guard.ts`
- [ ] 4. P1 的 33 个用例仍然全部通过；提交

**S2 样本集**

- [ ] 5. 表格构建器：core、条件格式、数据验证、筛选、超链接、备注、图片（浮动与单元格）、保护、综合
- [ ] 6. 文字文档构建器：正文与样式、列表、超链接、表格、图片（内联与环绕）、综合
- [ ] 7. `e2e/v03-build-samples.spec.ts` 生成 fixtures；提交

**S3 V03**

- [ ] 8. `e2e/v03-roundtrip.spec.ts`：三浏览器的 S0 → S1 → S2、差异分类、资源核对、跨浏览器比较、资源 hook 清单、保护类资源；提交

**S4 V04**

- [ ] 9. `e2e/v04-resource-loss.spec.ts`：插件缺失、资源损坏、晚注册，以及两种防护规则的判定结果；提交

**S5 V05**

- [ ] 10. `e2e/v05-copy.spec.ts`：复制、两个标签页并行编辑、重开核对、同一实例重复 `unitId` 的负向核对；提交

**S6 报告**

- [ ] 11. `reports/P2-验证报告.md`、`reports/插件档案v1.md`；提交

## 验证命令

```bash
cd spikes/m0
npx -y pnpm@12.6.0 install --frozen-lockfile
npx vite build
M0_BUILD_SAMPLES=1 npx playwright test e2e/v03-build-samples.spec.ts --project=chromium   # 只在需要重新生成样本时
npx playwright test e2e/v03-roundtrip.spec.ts e2e/v04-resource-loss.spec.ts e2e/v05-copy.spec.ts
npx playwright test                                                                         # 全部（含 P1）
```

## 完成标准

- 以上命令可以一键重跑，结果文件与报告中的数据一致；
- V03、V04、V05 在报告中都有结论、证据、环境和对设计的影响。
