# P4 资源与外部请求：实施计划

> 状态：进行中｜日期：2026-09-25｜所属：M0-P4｜依据：`04-P4-资源与外部请求.md` §3、§6

## 目标

按 Phase 文档 §6 完成 S1–S4，产出 V11、V12 的证据与报告，并给插件档案 v1 补充图片服务与 `IMAGE()` 的配置。

## 涉及文件

- `spikes/m0/`：server、harness、profiles、workers、e2e
- `docs/v0.1/M0-技术验证/reports/P4-验证报告.md`（新建）、`reports/插件档案v1.md`（补充）

## 任务

M0 不为验证工程补测试；每个任务以"脚本能跑通、结果文件生成"为完成标志。

**S1 验证工程增量**

- [ ] 1. `server/assets.ts`：上传（文件头识别、大小与像素限制、按内容去重、记录上传者）、读取（会话 Cookie、按引用或上传者授权）、观测接口；`server/serve.ts` 下发会话 Cookie 并接入
- [ ] 2. `server/snapshot-images.ts`：结构化提取与通用扫描、分类、引用关系；保存文档时更新引用关系，可选的保存校验
- [ ] 3. `src/harness/platform-image-io.ts`：平台图片服务（上传、`URL` 类型、错误约定、缓存）；`img=platform` 时经 `override` 注册
- [ ] 4. `src/harness/image-guards.ts`：文字文档剪贴板钩子、命令守卫、`IMAGE()` 的 `off` 与 `restricted`（主线程与 Worker）
- [ ] 5. `e2e/p4-helpers.ts`：合成粘贴、文件选择框、CSP 违规与资源读取记录、外部请求拦截；三个浏览器上的合成粘贴探针
- [ ] 6. P1–P3 的用例仍然通过；提交

**S2 V11**

- [ ] 7. `e2e/v11-image-paths.spec.ts`：路径矩阵 × 两种配置 × 三浏览器；提交
- [ ] 8. `e2e/v11-assets.spec.ts`：重开、另一个会话、无 Cookie、复制文档、上传失败、大小与类型；提交

**S3 V12**

- [ ] 9. `e2e/v12-external.spec.ts`：外链图片、Facade 外链、`IMAGE()` 三种处理 × 两种模式；外部请求计数；提交
- [ ] 10. `e2e/v12-degradation.spec.ts`：功能降级清单、拖放、探测策略下的违规汇总；提交

**S4 报告**

- [ ] 11. `scripts/p4-summary.ts`：把结果汇总成报告中的表格
- [ ] 12. `reports/P4-验证报告.md`；插件档案 v1 补充；提交
