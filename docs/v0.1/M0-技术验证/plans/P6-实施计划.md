# P6 本地优先可行性：实施计划

> 状态：已完成｜日期：2026-09-25｜所属：M0-P6｜依据：`06-P6-本地优先可行性.md` §3、§6

## 目标

按 Phase 文档 §6 完成 S1–S6，产出 V14、V15（及 V16）的证据与报告。

## 涉及文件

- `spikes/m0/`：server、harness（`outbox/`、`mutation-log.ts`）、workers、e2e、scripts
- `docs/v0.1/M0-技术验证/reports/P6-验证报告.md`（新建）

## 任务

M0 不为验证工程补测试；每个任务以"脚本能跑通、结果文件生成"为完成标志。

**S1 验证工程增量**

- [x] 1. `server/keys.ts` 与文档存储的修订号（`?base=`、`X-Revision`）
- [x] 2. `src/harness/outbox/`：`crypto.ts`、`store.ts`、`pipeline.ts`（含 `src/workers/outbox.worker.ts`）
- [x] 3. `autosave.ts`、`tab-lock.ts`、`recovery.ts`
- [x] 4. 页面参数 `outbox`、`user`、`durability`、`mutlog`；`window.__m0.outbox`
- [x] 5. P1–P5 的用例仍然通过；提交

**S2 V14 成本与配额**

- [x] 6. `e2e/v14-pipeline.spec.ts`：管道成本（样本 × 放置方式 × 持久性）、端到端、解密与解压
- [x] 7. `e2e/v14-quota.spec.ts`：配额与写满；提交

**S3 V14 协调与恢复**

- [x] 8. `e2e/v14-locks.spec.ts`
- [x] 9. `e2e/v14-crash.spec.ts`：进程被杀、写入中被杀、修订号已变、密钥吊销；提交

**S4 V15**

- [x] 10. `src/harness/mutation-log.ts`：记录与重放
- [x] 11. `e2e/v15-mutation-log.spec.ts`：重放等价矩阵、组合恢复、成本；提交

**S5 V16（可选）**

- [x] 12. `scripts/v16-node-load.ts` 与结果；提交

**S6 报告**

- [x] 13. `scripts/p6-summary.ts`：汇总成报告中的表格
- [x] 14. `reports/P6-验证报告.md`；提交

## 验证命令

- 功能用例：`npx playwright test e2e/v14-quota.spec.ts e2e/v14-locks.spec.ts e2e/v14-crash.spec.ts e2e/v15-mutation-log.spec.ts`
- 性能（单独运行）：`npx playwright test e2e/v14-pipeline.spec.ts`
- Node：`node scripts/v16-node-load.ts`
- 汇总：`node scripts/p6-summary.ts`

## 完成标准

Phase 文档 §8 的完成定义。

## 审查修复

- [x] 按 `reviews/P6-审查报告.md` 修订原型与用例（R1–R3、G1–G6、S1–S8），三个浏览器复验
- [x] 收尾自查：修正表格修改的测试辅助 `edit`（P3 大样本上从第二次修改起内容不变、捕获被去重跳过），端到端断言每次捕获都真的写入；三个浏览器重跑写入管道用例，配额用例在 Chromium 内核上重跑
- [x] 收尾：修复后对 P1–P5 的回归子集（`v03-roundtrip`、`v05-copy`、`v06-open-quiet`、`v09-read-mode`）三个浏览器各跑一遍；V16 在 Chromium 上重跑
