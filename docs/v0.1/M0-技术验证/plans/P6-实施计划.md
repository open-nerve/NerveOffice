# P6 本地优先可行性：实施计划

> 状态：进行中｜日期：2026-09-25｜所属：M0-P6｜依据：`06-P6-本地优先可行性.md` §3、§6

## 目标

按 Phase 文档 §6 完成 S1–S6，产出 V14、V15（及 V16）的证据与报告。

## 涉及文件

- `spikes/m0/`：server、harness（`outbox/`、`mutation-log.ts`）、workers、e2e、scripts
- `docs/v0.1/M0-技术验证/reports/P6-验证报告.md`（新建）

## 任务

M0 不为验证工程补测试；每个任务以"脚本能跑通、结果文件生成"为完成标志。

**S1 验证工程增量**

- [ ] 1. `server/keys.ts` 与文档存储的修订号（`?base=`、`X-Revision`）
- [ ] 2. `src/harness/outbox/`：`crypto.ts`、`store.ts`、`pipeline.ts`（含 `src/workers/outbox.worker.ts`）
- [ ] 3. `autosave.ts`、`tab-lock.ts`、`recovery.ts`
- [ ] 4. 页面参数 `outbox`、`user`、`durability`、`mutlog`；`window.__m0.outbox`
- [ ] 5. P1–P5 的用例仍然通过；提交

**S2 V14 成本与配额**

- [ ] 6. `e2e/v14-pipeline.spec.ts`：管道成本（样本 × 放置方式 × 持久性）、端到端、解密与解压
- [ ] 7. `e2e/v14-quota.spec.ts`：配额与写满；提交

**S3 V14 协调与恢复**

- [ ] 8. `e2e/v14-locks.spec.ts`
- [ ] 9. `e2e/v14-crash.spec.ts`：进程被杀、写入中被杀、修订号已变、密钥吊销；提交

**S4 V15**

- [ ] 10. `src/harness/mutation-log.ts`：记录与重放
- [ ] 11. `e2e/v15-mutation-log.spec.ts`：重放等价矩阵、组合恢复、成本；提交

**S5 V16（可选）**

- [ ] 12. `scripts/v16-node-load.ts` 与结果；提交

**S6 报告**

- [ ] 13. `scripts/p6-summary.ts`：汇总成报告中的表格
- [ ] 14. `reports/P6-验证报告.md`；提交

## 验证命令

- 功能用例：`npx playwright test e2e/v14-quota.spec.ts e2e/v14-locks.spec.ts e2e/v14-crash.spec.ts e2e/v15-mutation-log.spec.ts`
- 性能（单独运行）：`npx playwright test e2e/v14-pipeline.spec.ts`
- Node：`node scripts/v16-node-load.ts`
- 汇总：`node scripts/p6-summary.ts`

## 完成标准

Phase 文档 §8 的完成定义。
