# P1 SDK 基线与验证工程：实施计划

> 状态：进行中｜日期：2026-09-23｜所属：M0-P1｜依据：`01-P1-SDK基线与验证工程.md` §3、§6

## 目标

按 Phase 文档 §6 完成 S1–S4，产出 V01、V02 的证据与报告。

## 涉及文件

- `spikes/m0/` 下的全部文件（新建）
- `docs/v0.1/M0-技术验证/reports/P1-验证报告.md`（新建）

## 任务

M0 不为验证工程补测试；每个任务以"脚本能跑通、结果文件生成"为完成标志。

**S1 验证工程骨架**

- [ ] 1. `package.json`：精确版本；`.npmrc` 设置 `save-exact`；用 `npx pnpm@12.6.0` 安装，生成锁文件
- [ ] 2. `vite.config.ts`：index、sheet、doc 三个页面入口；生成 manifest；Worker 使用 ES 模块格式
- [ ] 3. `src/profiles/sheet.ts`、`doc.ts`：候选插件、配置、中文语言包、样式
- [ ] 4. `src/harness/`：按档案创建 Univer、创建文档单元、等待首帧、`save()`、事件收集；挂到 `window.__m0`
- [ ] 5. `src/host/`：React 宿主页面（状态栏 + 编辑器容器）；`index.html` 入口页
- [ ] 6. `src/workers/sheet-formula.worker.ts`：公式 Worker
- [ ] 7. `server/serve.ts`：静态服务、CSP 头（强制 + 探测）、违规报告接收
- [ ] 8. Playwright 配置：三个浏览器项目；`webServer` 启动 `server/serve.ts`
- [ ] 9. 冒烟：开发模式与生产构建下，两类页面都能加载、能输入；提交

**S2 V01 依赖与许可**

- [ ] 10. `scripts/deps-report.ts`：版本一致性、重复实例、Pro 检查、许可分类 → `e2e/results/v01-*.json`
- [ ] 11. `scripts/url-scan.ts`：构建产物的绝对 URL 按主机归类 → `e2e/results/v01-url-scan.json`
- [ ] 12. 逐项给出结论；提交

**S3 V02 CSP 验证**

- [ ] 13. `e2e/v02-csp.spec.ts`：表格（主线程）、表格（Worker）、文字文档三个场景；收集违规、错误、请求来源 → `e2e/results/v02-*.json`
- [ ] 14. 三个浏览器跑通；如有违规，定位来源并逐条给出结论；提交

**S4 包体积与报告**

- [ ] 15. `scripts/bundle-size.ts` → `e2e/results/p1-bundle-size.json`
- [ ] 16. `README.md`：运行方式、环境要求、不进入生产的声明
- [ ] 17. `reports/P1-验证报告.md`；提交

## 验证命令

```bash
cd spikes/m0
npx pnpm@12.6.0 install --frozen-lockfile
npx pnpm@12.6.0 build
npx pnpm@12.6.0 report:deps && npx pnpm@12.6.0 report:urls && npx pnpm@12.6.0 report:size
npx pnpm@12.6.0 e2e -- e2e/v02-csp.spec.ts
```

## 完成标准

- 以上命令在干净环境中可以一键重跑，结果文件与报告中的数据一致；
- V01、V02 在报告中都有结论、证据、环境和对设计的影响。
