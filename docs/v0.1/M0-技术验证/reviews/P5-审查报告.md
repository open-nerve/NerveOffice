# P5 文字文档与中文：审查报告

> 审查者：独立审查代理（与主会话同为 Claude Opus）｜日期：2026-09-25｜审查范围：`f063a15..d78470c` 与未提交的报告草稿；修复见其后的提交｜结论：修复后通过

M0 的审查对象是验证结论：结论有没有可复现的证据支撑、实验设计有没有漏洞、对设计的影响是否写对写全，以及将来要落进生产的方案有没有安全或正确性问题。

## 检查项

- [x] 正确性与边界条件：
  - "排版 Worker 默认启用"的性能依据测错了口径（R1），Worker 模式下单元格里的 Tab 有时序缺陷（R2）；
  - "单元格里不放图片"可以用拖动浮动图片绕过（R3）；
  - `/` 键的 input 路径（G1）、链接白名单的判定与保存不一致（G3）、捕获时机没有上限（G4）、事件归一的延后派发会丢输入（G5）。

  均已修正或调整结论，并复验。
- [x] 实验设计：补上命令路径的链接用例（C7c）、拖动图片进单元格的步骤、段落把手与粘贴选项（L5b）、带分节符与引号地址的内部片段、提交后紧接着输入的用例、`doc@1` 的最终组合变体；性能用例增加"画出来"的口径。
- [x] 证据是否充分：
  - 审查者用汇总脚本核对了提交 `53a8065`、`c14b795` 的三浏览器结果，与报告和提交信息一致；
  - 用临时探针复现了 R1–R3、G1–G5、G10；
  - 修正后按汇总脚本复核。
- [x] 安全：链接地址判定之后一律保存规范写法，命令路径与内部片段都覆盖；服务端要求地址等于规范写法；单元格图片的全部路径都有守卫。
- [x] 约束符合性：没有引入 Pro；`@univerjs/*` 都是 1.0.0（`@univerjs/icons` 1.43.0 已登记）；`refer/univer` 未被修改，停在 v1.0.0 标签；提交信息没有 AI 署名；内部 API 登记已补全。
- [x] 文档：报告、Phase 文档、插件档案、能力矩阵、交接单、延期登记已按审查意见更新。

## 问题清单

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| R1 | 重要 | `e2e/v13-perf.spec.ts`；报告摘要、§3.1、§5.3；插件档案 §2 | 延迟的终点是"事件后两次 rAF"。Worker 模式下排版结果异步回到主线程，两帧时通常还没画出来；起点取监听器执行的时刻，不含排队时间。"排版 Worker 默认启用"的依据不成立 | 已修复：<br>• 性能用例增加"画出来"的口径：起点为事件的 `timeStamp`，终点为光标右侧画布像素变化，键入间隔 350 ms，不排队；<br>• 两帧口径保留，注明只在主线程模式下等于画出来；<br>• 报告按新口径改写；Worker 的理由改为主线程阻塞，并与 R2 一起权衡（结论见 R2） | `e2e/results/v13/perf/`（三个浏览器，约 2 万字的样本）：<br>• 键入画出来 p50：主线程 94–110 ms，Worker 64–71 ms；<br>• 组合更新画出来 p50：主线程 90–107 ms，Worker 63–73 ms；<br>• 快速键入的最长阻塞：主线程 81–123 ms，Worker 25–51 ms。<br>WebKit 每组有一两次 0.4–0.9 秒的离群值，与文档规模无关，只看 p50（报告 §5.3 第 4 条） |
| R2 | 重要 | 插件档案 §2；报告 §5.3 | Worker 模式下，单元格里键入后立即按 Tab，Tab 被吞掉，后面的字写进同一格。主会话自己的变体结果（C5 7/8）已经显示，报告没有提到。设计要求的 `worker=1` 下 V06 文字文档动作回归也没有做 | 已处理：<br>• 排版 Worker 暂不启用，v0.1 用主线程排版（插件档案 §2、报告 §1.2 第 7 条、§5.3 第 4 条）；<br>• 缺陷写入报告，列入上游报告清单；<br>• DEF-007（M6）登记重新评估的前提：缺陷修复（上游，或平台把排版发布前的 Tab 排到发布之后）、三个浏览器的表格导航用例、`worker=1` 下回归 V06 的文字文档动作、按新口径复测 | `e2e/results/v13/capabilities-worker/`：C5 的"键入后立即按 Tab"在 Chromium、Chrome 上复现（"甲一甲二"在同一格），WebKit 这一轮没有复现；其余能力项与主线程模式一致 |
| R3 | 重要 | `doc-policy.ts`；报告 §1.2 第 5 条、§2.3 第 3 条；插件档案 §7 | 把浮动图片（四周型）拖进单元格，锚点落进单元格，再改成内联，就得到单元格里的内联图片，守卫没有覆盖。设计里"记录单元格图片的实际表现"也没有执行 | 已修复：<br>• 守卫补上 `doc.command.transform-non-inline-drawing`（目标位置在表格里时取消）与 `doc.command.update-doc-drawing-wrapping-style`（锚点在表格里时不能改为内联）；<br>• C5b 增加真实鼠标拖动的步骤；<br>• SDK 默认下 C5b 做保存重开，记录单元格图片的实际表现 | `capabilities/*-C5b-*.json`：SDK 默认下拖进单元格后锚点在单元格里，平台配置下不在；三条路径平台配置下都是 0 张。SDK 默认下单元格里的三张图片保存重开一致（报告 §2.3 第 3 条） |
| G1 | 一般 | `doc-policy.ts`；报告 §1.2 第 1 条 | 只拦了 `/` 的 keydown，不经过 `/` 键的输入（系统文字替换、CDP 的 `insertText`）仍被 SDK 接管：`/` 进了正文，菜单弹出，其后的字丢失 | 已修复：<br>• 平台在捕获阶段同样处理数据为 `/` 的 `insertText` beforeinput（不在组合中）；<br>• C8 增加"不经过按键输入的 `/`"步骤；<br>• 附录 A 增加中文标点与英文标点两种状态下按 `/` 的核对，`ime-real.ts` 打印 `/` 的 keydown 与 beforeinput | `capabilities/*-C8-platform.json`：正文为"用于/x对齐"，其后的字不丢 |
| G2 | 一般 | 报告 §2.3 | 空段落里的 `/` 弹出菜单后，其后键入的字仍然丢失（平台与 SDK 默认相同），报告没有记录 | 写入报告 §2.1、§2.3 第 1 条与已知限制建议（§6、§8 第 2 条）；M6 处理：菜单打开时收到可打印字符，就关闭菜单并插入 `/` 与这个字符；上游清单补充 | 报告 §2.3 第 1 条 |
| G3 | 一般 | `doc-policy.ts`；报告 §1.2 第 4 条；插件档案 §7 | 白名单判定用的是解析结果，保存的却是原串：<br>• 相对地址里的制表符、换行可以绕过（`/\t/evil.example`）；<br>• https 地址里的引号经内部片段保存后，复制时注入 HTML | 已修复：<br>• `normalizeLinkUrl` 统一判定并返回规范写法：含控制字符一律拒绝；相对地址按站点源解析，结果必须仍在本站；`http`、`https`、`mailto` 保存 `URL.href`；<br>• 命令守卫与粘贴清洗都保存规范写法；<br>• 服务端要求地址等于规范写法（M3、M6）；<br>• 上游清单补充"复制时 `href` 不转义"；<br>• 新增 C7c（直接执行添加、修改链接的命令）；内部片段用例加入带引号的地址 | `capabilities/*-C7c-platform.json`：5 种绕过写法都不保存，带引号的地址保存为编码后的写法，修改链接的命令被取消；SDK 默认下 5 种写法都原样保存（对照）。`paste/*-fragment-platform.json`：引号地址已编码 |
| G4 | 一般 | `capture-timing.ts`、`change-detector.ts`；报告 §1.2 第 3 条 | 组合感知没有时间上限，也没有失效复位：组合一直不提交时，连组合开始之前的修改也写不进本机；`compositionend` 没有到达时，之后所有捕获都停住。与 00 号计划书 §7.2"最长每 3 秒捕获一次"、§7.8 的丢失边界冲突 | 已修复：<br>• 组合持续超过 3 秒（`compositionMaxWaitMs`）就照常捕获；<br>• 隐藏输入元素失焦时复位组合状态；<br>• 报告 §1.2 第 3 条与 §6 的 §7.3 修订按此写 | `ime/*-capture-platform.json`：组合进行中等到组合持续满 3 秒才捕获，提交后捕获到最终文字 |
| G5 | 一般 | `doc-policy.ts`；`e2e/p5-helpers.ts` | 事件归一用 `setTimeout(0)` 延后派发 `compositionend`，SDK 在这段时间里忽略 keydown 与 input，紧接着的输入会丢失；合成驱动每次派发后让出一个宏任务，正好掩盖了这个时间窗 | 已修复：<br>• 延后期间收到 keydown、beforeinput、input、compositionstart、focusout、mousedown 或 paste，先同步派发 `compositionend`；<br>• 合成驱动改为只清空微任务；<br>• 新增"提交后同一任务里紧接着输入"的用例 | `ime/*-followup-platform.json`：Chrome 与 WebKit 两种顺序都得到"你好，" |
| G6 | 一般 | `ui-config.ts`；`v13-layout-entries.spec.ts`；报告 §5.1 | 功能搜索能找到"插入矩形""插入椭圆"（父菜单隐藏，子项没有隐藏），平台配置下点了没反应；搜索词没有覆盖所有隐藏项；报告写的"14"是出现次数，实际是 12 个 id | 已修复：<br>• 隐藏清单补上形状的 4 个子项；<br>• 搜索词覆盖每个隐藏项的标题（14 个）；<br>• 报告的数字更正：清单现为 30 个 id（含 G9 新增的 14 个） | `layout/*-menus.json`：14 个搜索词都找不到隐藏项 |
| G7 | 一般 | 报告 §5.1、§6 | 表格的每个单元格里都有一个分节符，按字面"拒绝分节"，所有含表格的文档都会被拒；没有定清楚页眉数据在保存时拒绝还是加载时去掉 | 已修正：<br>• 规则写成"表格之外只允许文末一个分节符"；<br>• 这类数据加载时去掉、保存时拒绝：加载时的规范保证历史数据仍能保存；正常流程里这类数据只可能来自缺陷或绕过客户端的请求；<br>• 粘贴清洗去掉表格之外的分节符（见 S6） | 报告 §5.1；插件档案 §2 |
| G8 | 一般 | 报告 §5；`scripts/p5-summary.ts` | 推荐的 `doc@1` 组合从没整体跑过；`without-formula` 变体只有能力矩阵，变体回归只有 Chromium；汇总脚本把 SDK 默认的对照项算成"安全问题"，能力矩阵表混入对照行 | 已修复：<br>• 新增组合变体 `without=formula&outline=1`（去掉公式引擎、主线程排版、大纲侧栏，即 `doc@1` 的最终组合），三个浏览器重跑能力矩阵、输入法与粘贴；<br>• 汇总脚本：变体一节只统计平台配置，能力矩阵表把对照行单列并注明 | `*-without-formula-outline/`（三个浏览器）：<br>• 能力矩阵 54 个结果文件，步骤全部通过；<br>• 输入法 16 组矩阵，没有异常；<br>• 粘贴 75 个平台配置结果，没有安全问题或页面错误 |
| G9 | 一般 | 报告 §5.1 | 菜单审计有 117 个可见项，会改结构的几项没有逐项结论：图片组合与层级、多图对齐、粘贴选项、段落菜单的复制/剪切/删除段落、拖动块 | 已处理：<br>• 图片的组合、层级、对齐（14 项）没有验证，按 00 号计划书 §3.3 隐藏；<br>• 段落把手（删除、复制、剪切、拖动移动）与粘贴选项（仅保留文本）新增 L5b，检查撤销重做与保存重开；<br>• 报告 §5.1 增加"保留入口 × 结论"表（可见项现为 99 个） | `layout/*-handle.json`：6 个步骤通过，撤销 6 步回到原文、重做恢复，保存重开一致 |
| G10 | 一般 | 报告 §3.3 第 3 条 | 不换行时纵向偏移为 0，但横向偏移变成 13、44、89 px（约为组合文字的全宽），报告只写了"可以消除纵向偏移" | 已修正：写明两种方式都对不齐；M6 的缓解方向改为"隐藏输入元素定位到组合开始处，字号按缩放与画布一致，不换行" | 报告 §3.3 第 3 条；`ime/*-candidate-anchor.json` |
| S1 | 建议 | 附录 A；`ime-real.ts` | 补上 Shift 切换中英文、焦点切走时未提交的组合；汇总只统计 trusted 事件，否则平台补发的事件会被误判为"相同顺序" | 已修复：记录页清单与附录 A 补上这两步；`ime-real.ts` 只统计 `isTrusted` 的事件 | 附录 A |
| S2 | 建议 | 报告 §3.3 第 1 条；C8b；05 号设计 §2 | WebKit 提交顺序的源码依据；单元格里 `/` 被吞掉只有探针证据；05 号设计说 `data://` 能通过并保存，与 C7b 不符 | 已修复：<br>• 报告写明 WebKit `Editor::setComposition` 的确认分支（先删组合文字、插入提交的文字，再派发 `compositionend`，中间没有 `compositionupdate`），已对照 WebKit 主干源码；<br>• C8b 增加"单元格里键入 `a/b`"；<br>• 05 号设计 §2 更正 | `capabilities/*-C8b-default.json`：三个浏览器在单元格里键入 `a/b` 都得到 `ab`，菜单不弹出 |
| S3 | 建议 | 报告"验证范围"、§3.3 第 4 条 | CDP 驱动的表述要加限定 | 已修正：它走的是浏览器渲染层的输入法接口，不经过系统输入法与平台视图层，也不产生 keyCode 229 的 keydown | 报告"验证范围" |
| S4 | 建议 | 报告 §2 | "撤销回到原状"比的是结构摘要，不是整份快照 | 已在报告"验证范围"写明口径 | 报告"验证范围" |
| S5 | 建议 | `doc-policy.ts` | 阅读模式下平台的 `/` 处理也会执行（被 SDK 的权限检查拦住）；守卫可补分节与页眉页脚的 3 个命令 | 已修复：先判断能否编辑；守卫补上 `doc.command.update-section`、`sidebar.operation.doc-header-footer-panel`、`doc.command.set-section-header-footer-link`（清单共 15 个） | 报告 §1.2 第 6 条、§7 |
| S6 | 建议 | `v13-paste.spec.ts` | 计划中的"带分节符的内部片段"没有执行（构造的片段里没有分节符） | 已修复：片段带一个分节符；平台的粘贴清洗去掉表格之外的分节符 | `paste/*-fragment-*.json`：表格之外的分节符 SDK 默认为 2 个，平台配置为 1 个（只剩文末） |
| S7 | 建议 | 交接单；延期登记 | macOS 真实输入法核对要写成显式待办；P5 在真实 Safari 上的复核没有登记 | 已处理：交接单写明待办、负责人、截止点与结果不符时的处理；登记 DEF-008（M6） | P5 交接单；DEF-008 |
| S8 | 建议 | 报告 §5.3 | "首屏 JS 减少 27%"是未压缩体积；性能表里的 Steady 是固定延迟 | 已修正：未压缩 −27%，gzip 1.23→0.99 MiB（−19%）；Steady 不作为指标，汇总表只列 Rendered | 报告 §5.3 第 5、7 条 |
| S9 | 建议 | 报告 §6 | 上游报告清单要补充 | 已补充：复制时 `href`、`data-rangeid` 不转义；浮动图片可以拖进单元格；隐藏父菜单后子项仍能被功能搜索找到；Worker 模式下的 Tab；候选框锚点；添加、修改链接的命令不检查地址 | 报告 §6 |
| S10 | 建议 | C5b；`reports/能力矩阵.md` | 单元格里工具栏"图片"按钮的记录值是 null（记录方式的问题）；能力矩阵（文字文档部分）没有产出 | 已修复：按钮的禁用状态改为同时读取子元素与类名（记录值 `class:disabled`）；产出 `reports/能力矩阵.md` 的文字文档部分 | `capabilities/*-C5b-*.json`；`reports/能力矩阵.md` |

## 审查者的核对（摘要）

- **约束**：没有 `@univerjs-pro`；`refer/univer` 干净，停在 v1.0.0 标签；4 个提交都没有 AI 署名；P5 用到的内部 API 已在报告 §7 登记。
- **数字与汇总脚本一致**：
  - 能力矩阵 16 项三个浏览器步骤全过，撤销重做与保存重开一致；
  - 输入法 13 种情形：WebKit 顺序加 SDK 默认时撤销只有 2/13 正确，平台配置 13/13；
  - 粘贴的偏差清单与报告 §4.3 逐条相符；
  - 提交信息里的 48、28、92 个用例数都对得上。
- **工作区的结果**：版式、命令守卫、大纲、目录块、去掉公式引擎（8/8 一致）、CSP（无强制违规，没有触发 `font-src`、`worker-src`）、包体积，都与报告一致。
- **源码核对**：`/` 的两条拦截路径与编辑权限检查；输入法控制器的异步分支；链接打开前的 `isSafeUrl` 检查与 `noopener`；阅读模式下 `InsertTextCommand` 被权限控制器拦住；文字文档用到的各包都不依赖公式引擎。
- **探针复核成立的点**：
  - 平台的 `/` 处理在段落中间、粗体、链接里都正确；
  - 全选复制再粘贴不会增加分节符；
  - Worker 模式下打开文档不被误判为有修改（5 份样本 × 两种模式）；
  - 键入后立即按方向键、回车，两种模式都正常；
  - `removeCharsFromBody` 的区间平移正确：两张图片、图片在链接或文字样式开头、图片在表格内等情况都覆盖了。
- **收尾**：临时探针已删除（备份在会话临时目录）；构建目录与输出目录已删除；端口已释放；没有改动已跟踪的文件，也没有提交。

## 修复后的复验

- **V13 全部用例**（修复后重新构建，严格 CSP）：每个浏览器 73 个用例。
  - Chromium：73 个通过；
  - Chrome 与 WebKit：一起跑时 142 个通过、1 个失败、3 个按设计跳过（WebKit 的 CDP 驱动 2 个、真实剪贴板 1 个）。失败的是 WebKit 的候选框锚点用例：测试辅助用 `addStyleTag` 插入内联样式，被严格 CSP 的探测策略拒绝。改为用 CSSOM 设置之后，三个浏览器重跑都通过；
  - 报告各表已按这一轮的结果更新，并用 `node scripts/p5-summary.ts` 逐项核对。
- **性能**（单独运行）：每个浏览器 4 个用例，全部通过。
  - WebKit 的键入延迟一度混进了合成输入法的 keyCode 229 keydown（它们的画面变化来自之后的组合更新），修正过滤之后单独重跑了 WebKit。
- **档案变体**：
  - `doc@1` 的最终组合（`without=formula&outline=1`）重跑能力矩阵、输入法与粘贴：Chromium 61 个通过；Chrome 与 WebKit 119 个通过、3 个按设计跳过；
  - `worker=1` 重跑能力矩阵：Chromium、Chrome 各 17 个通过、1 个失败（C5 的 Tab 缺陷，即 R2），WebKit 18 个通过。
- **P1–P4 回归**（同一份构建，单独端口）：
  - Chromium：168 个通过；
  - Chrome 与 WebKit：305 个通过、31 个按设计跳过。
- **运行环境**：最终一轮进行中，本机断电重启后一度没有登录桌面会话，WebKit 无法启动、Chrome 间歇崩溃；Chrome 与 WebKit 在桌面会话恢复后补跑。
- 回归产生的 P1–P4 结果文件没有入库，各 Phase 的结果文件保持合并时的版本。

## 结论与遗留

**结论：修复后通过。** V13 的结论维持"有条件通过"，条件见报告 §1.2。审查提出的 3 个重要问题、10 个一般问题与 10 条建议都已处理：需要改代码的已修复并复验；R2 按审查给出的退路处理，排版 Worker 暂不启用；其余已写进报告或登记。

**遗留**（都已登记，不影响本 Phase 的结论）：

- **M0 结束评审前**：macOS 真实输入法的人工核对（报告附录 A；交接单的待办）。
- **延期登记**：
  - DEF-005（M6）：Windows 输入法；
  - DEF-006（M6）：真实来源的粘贴；
  - DEF-007（M6）：排版 Worker 的 Tab 缺陷与重新评估；
  - DEF-008（M6）：真实 Safari 上复核 P5 的文字文档能力。
- **M1**：
  - 适配层集中封装报告 §7 登记的内部 API；
  - 加载时的规范补齐：去掉页眉页脚数据与多余的分节符；
  - 链接地址的规范写法与服务端校验共用同一实现。
- **M6**：
  - 空段落里 `/` 菜单吞字（G2）；
  - 候选框的缓解（G10）；
  - 粘贴的转换偏差；
  - 单元格里的表格按钮、向前删除键删除图片。
- **上游报告**：报告 §6 的 11 条，M1 起提交并跟踪。
