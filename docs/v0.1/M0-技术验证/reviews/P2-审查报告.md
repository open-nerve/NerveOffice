# P2 快照与插件档案：审查报告

> 审查者：独立审查代理（与主会话同为 Claude Opus）｜日期：2026-09-24｜审查范围：`518b09a..0977def`（审查）；修复见其后的提交｜结论：修复后通过

M0 的审查对象是验证结论：结论有没有可复现的证据支撑、实验设计有没有漏洞、对设计的影响是否已经回写。

## 检查项

- [x] 正确性与边界条件：三项结论的主体成立；DEF-002 的根因、V04 的能力边界、资源比较函数的两处漏洞已修正（R1、R4、R5）
- [x] 实验设计：样本自证、V03 不能充当回归门禁、V05 核对不足等问题已补上语义断言、差异基线与差异比对（R3、R10）
- [x] 证据是否充分：审查者完整重跑 123 个用例与 P1 的 33 个用例，结果可复现；审查后新增的用例与结果已入库
- [x] 安全：重复资源名可以让伪造数据生效，已纳入服务端规则；地址检查的覆盖位置已补全；保护类资源的身份风险已记录（R5、R7、R8）
- [x] 约束符合性：没有引入 Pro；`@univerjs/*` 精确锁定同一版本；`refer/univer` 未被修改
- [x] 文档：报告、插件档案、设计文档、交接单、延期登记已按审查意见更新

## 问题清单

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| R1 | 重要 | 报告 §2.3、DEF-002 | DEF-002 根因写错：自动行高其实被处理了，缺陷在于刷新路径对 `Position` 图片只在内存里保留尺寸、不回写终点锚点；切换工作表在同一会话里就会拉伸，之后的保存会写进快照；换行、36 号字、阴性对照没有可复现的证据 | 已修复：根因与行号按源码改正（`REFRESH_MUTATIONS` 125–134、`_refreshDrawingTransform` 1433–1486、刷新分支 331–338、重算位置在切表与渲染初始化）；新增 `e2e/v03-drawing-autoheight.spec.ts`，覆盖 3 种触发、2 个阴性对照、切换工作表与重开四个阶段；DEF-002 描述同步 | 三浏览器 15 个用例：触发后 120×80、切表后 120×99/186/111、重开后相同、锚点未更新；阴性对照尺寸不变（手动改行高时锚点已更新）；结果 `e2e/results/v03/drawing-autoheight/` |
| R2 | 重要 | 报告 §2.3、DEF-002、交接单 | DEF-002 触及 A02，"列为已知限制"没有标明须走范围变更；没有评估补丁的必要性与规模 | 已修复：报告与 DEF-002 写明"关联 A02，改为已知限制须走范围变更并经需求方审批"；补充三种方案的评估（上游修复约 10 行、平台插件与撤销栈及变更检测的关系、保存时修正不可行） | 报告 §2.3 第 2 条 |
| R3 | 重要 | `e2e/v03-roundtrip.spec.ts` | 保真差异只记录不断言，不能充当回归门禁；白名单取自运行时 hook，核对是同义反复；档案声明的资源名没有自动核对 | 已修复：新增差异基线 `e2e/v03-baselines.ts`（支持通配），S0→S1 的实际差异与内容变化的资源都要与基线一致；白名单改用档案声明；新增"档案声明 = 运行时 hook（按文档类型过滤）"断言 | V03 全部通过，断言生效（基线外多出任何一条差异都会失败） |
| R4 | 重要 | 报告 §3、`results/v04/corrupt/*-wrong-shape.json` | 夸大了打开自检的能力：结构错误拦不住，且有 15 条页面异常没有报告；加载错误捕获没有实验；自检忽略 `changed`、没有失败处置 | 已修复：摘要与表格改为准确的能力边界并补上页面异常；实现并实测资源加载错误捕获（`guarded-resource-manager.ts`，`guard=1`），能识别"解析抛错""非空输入被吞成空值""加载抛错"，全部损坏情况都能发现，正常样本没有误报；规则补上档案完整性核对与失败处置（只读、提示、上报告警、运维恢复） | V04 全部带断言通过；结果 `e2e/results/v04/corrupt/`、`guard-no-false-positive-*.json` |
| R5 | 重要 | `resource-guard.ts`、报告 §3.3 | 重复资源名：SDK 取第一条、比较函数取最后一条，可以让伪造地址生效；判空函数对内部字符串再解析，正文恰好是 `"[]"` 的备注会被判为空 | 已修复：比较函数与 SDK 一致只取第一条，新增 `duplicates` 与 `invalid`（`data` 非字符串）并纳入服务端拒绝；判空只在顶层解析一次；新增重复资源名用例；超链接资源要求与正文一致 | 用例复现"正文链接被改写成伪造地址"，检查识别重复并拒绝；结果 `e2e/results/v04/duplicate-*.json` |
| R6 | 一般 | 报告 §2.3 | "SDK 保存过的模板从第一次保存起就稳定"与数据矛盾 | 已修复：改为"模板须取收敛后的版本（S1 或之后）"，并说明 SDK 输出也要再经一次重开保存才逐字节稳定 | 报告 §2.3 第 1 条 |
| R7 | 一般 | 报告 §2.3、§6 | 图片与链接的检查位置不全：工作表背景图片、文字文档页眉页脚与注释、内部相对链接 | 已修复：报告列出全部位置，建议统一的地址提取器并按平台源解析相对地址，不支持的结构要求为空；背景图片入口交 P3 决定 | 报告 §2.3 第 6 条、§6；交接单 |
| R8 | 一般 | 报告 §2.5、插件档案 §3 | 保护实验的解释与源码不符（表格页当前用户是 `Owner_…`，不是空 id）；截图没有入库 | 已修复：按 `authz-io-local.service.ts:73-79、81-87` 改正，补充"设置真实用户后可能锁死"的风险；截图由用例生成并入库；当前用户 id 写入结果 | `e2e/results/v03/protection-*.json`、`protection-*.png` |
| R9 | 一般 | 报告 | 验证范围没有写明：只覆盖非 Worker 模式；3 项始终为空的资源；样本只在 Chromium 生成 | 已修复：报告摘要后写明验证范围；新增 Worker 模式等价核对（Chromium，15 个样本）；白名单逐项标注状态与服务端处置；三浏览器的 Worker 回归交给 P3、P5 | V03 Worker 用例全部一致；报告 §2.4 |
| R10 | 一般 | 样本构建与 V05 | 样本可能自证；V05 与设计不一致，没有核对其余内容不变；V04 没有记录单元格图片的去向 | 已修复：新增预期语义 `e2e/samples-expected.ts`，生成样本（`M0_BUILD_SAMPLES=check` 可在不改写样本的情况下核对）与每次重开后都用 Facade 核对；V05 新增与原文档的差异比对与语义核对；V04 记录不走资源的数据 | 构建核对 15 个样本全部符合预期；V05 三浏览器通过；去掉图片组时单元格图片保留 |
| R11 | 一般 | 交接单、报告 §6 | 遗漏：P3（打开即有修改的误报、哈希按资源名排序）、P5（目录的区间、排版 Worker、Word 元数据资源）、P6/M4（锁名、Univer 的 localStorage 回退与移动端键）、00 号计划书附录 B 与 §8.7 | 已修复：交接单与报告 §6 逐项补上；报告更正"唯一的持久化存储"的说法 | 交接单"交给后续 Phase 的事项"；报告 §4.2、§6 |
| R12 | 建议 | 报告 §5、README | 重新生成样本后必须再构建；README 没有 P2 新增的能力；P1 回归没有新证据 | 已修复：报告与构建脚本注明必须再构建，新增 `check` 模式；README 补充接口与参数；P1 回归写入下方复验证据 | 报告 §5、`spikes/m0/README.md` |
| R13 | 建议 | V04 | 损坏情况与超链接缺失没有断言；服务端检查应与损坏版比较；文字文档超链接的晚注册区分不了"补加载"与"重新生成" | 已修复：全部 V04 用例带断言；两种检查都以损坏版为"上一版"；晚注册用例注明只作参考 | V04 全部带断言通过 |

## 审查者的核对与重跑（摘要）

- 完整重跑 V03–V05 共 123 个用例，全部通过；174 个结果文件去掉时间戳后，只有手写 `doc/minimal` 的随机 `sectionId` 不同。
- 重跑 P1 的 33 个用例，全部通过。
- 已提交结果的自洽性：重算 51 个 S0/S1 哈希，0 处不一致；"空值等价"只有两类，没有掩盖真实丢失。
- 源码核对：资源加载与保存、各插件的 `parseJson`、文字文档超链接资源控制器、图片变换控制器与两处按锚点重算的位置、本地授权服务。
- 补充实验：浮动图片的四个阶段与阴性对照、去掉图片组时的单元格图片、同源相对地址的图片请求、当前用户身份、重复资源名、自动行高的跨浏览器抽查。结论均已吸收进本次修复。
- 收尾：审查者恢复了结果文件，没有修改 `refer/univer`，没有残留端口。

## 修复后的复验

- `npx playwright test`（P1 + P2 全部用例）：**192 个通过**，75 个按设计跳过（样本生成 45 个；只在 Chromium 运行的 Worker 模式 30 个）。其中 DEF-002 的 9 个触发用例按"预期失败"通过。
- 修正"先读语义再保存"的观察者效应后，单独重跑 V03：69 个通过、30 个跳过；报告 §2.2 表格中的数字与三个浏览器的结果一致。
- `M0_BUILD_SAMPLES=check`：15 个样本的构建器输出与预期语义一致，fixtures 未被改写。
- 复验中新发现一条并已处理：Facade 的部分读取方法（例如 `getDataValidations()`）会在模型中建空条目，改变下一次保存的字节。验证脚本改为先保存、再读语义，报告 §2.3 第 5 条记录了这一点。

## 结论与遗留

- 结论：**修复后通过**。V03 有条件通过（DEF-002，关联 A02）、V04 通过、V05 通过；修复后的证据补上了语义、基线、加载错误捕获与重复资源名，覆盖三个浏览器。
- 遗留：DEF-002（M5）；三浏览器的 Worker 模式回归（P3、P5）；报告 §6 列出的 00 号计划书修改在 M0 结束时写入 r3。
