# P1 SDK 基线与验证工程：审查报告

> 审查者：独立审查代理（与主会话同为 Claude Opus）｜日期：2026-09-24｜审查范围：`6808cda..c52e9dd`｜结论：修复后通过

M0 的审查对象是验证结论：结论有没有可复现的证据支撑、是否覆盖了目标浏览器、对设计的影响是否已经回写。

## 检查项

- [x] 正确性与边界条件：V01、V02 的结论本身成立；采集渠道的盲区与缺少阳性对照的问题已修复（R1）
- [x] 模块边界、依赖方向：验证工程按 `host → harness → profiles → @univerjs/*` 组织；M0 不要求生产级规范
- [x] 证据是否充分：审查者重跑全部脚本与 12 次 V02 运行，结果可复现；补充的阳性对照、网络核验与真实 Safari 自检已纳入
- [x] 安全：没有引入 Pro；`refer/univer` 未被修改；CSP 需随 Worker 脚本下发的约束已回写（R2）
- [x] 性能与资源使用：P1 只记录包体积与冒烟时间，口径已写明（R9）
- [x] 文档：报告、设计文档、计划、延期登记已按审查意见更新；M0 阶段没有架构总览

## 问题清单

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| R1 | 重要 | `src/harness/events.ts`、`e2e/v02-csp.spec.ts`、报告 §3 | WebKit 中 Worker 内的违规不走 `report-uri`、也不派发事件，原采集方式看不到；没有阳性对照，分不清"没有违规"和"看不见违规"；CSP 开启时被拦截的请求不会出现在请求记录里，"非同源请求为零"不能单独作为 V01 的动态证据 | 已修复：①违规采集增加浏览器控制台渠道（含 Worker 作用域），结果写入各渠道的明细；②新增阳性对照 `e2e/v02-csp-probe.spec.ts`，在 `off`、`full`、`html-only` 三种模式下测页面与 Worker 的跨源请求和动态代码，得出每个浏览器的渠道覆盖表；③WebKit Worker 内动态代码的盲区由静态扫描兜底（`url-scan.ts` 新增动态代码检查）；④V01 动态核验改为关闭 CSP 运行（`e2e/v01-network.spec.ts`） | `npx playwright test`：33 个用例全部通过。阳性对照在三个浏览器中结果一致；WebKit 的 Worker 动态代码违规确认在所有渠道都不可见，与审查结论相同 |
| R2 | 重要 | 报告 §3.5、§7 | 遗漏设计约束：Worker 内生效的是它自己脚本响应上的 CSP，只给 HTML 加头会让 Worker 完全失去约束；违规上报存在浏览器差异 | 已修复：服务端新增 `html-only` 模式，阳性对照复现"页面被拦、Worker 全部放行"；报告 §3.4 写入"CSP 必须随所有响应下发（含 Worker 脚本）"与"违规上报只作抽样信号"，§7 列入 00 号计划书 §11.3 的修改与 M1 事项 | `e2e/results/v02/probe/*-html-only.json`；真实 Safari 结果 `e2e/results/v02/safari/probe-html-only.json` |
| R3 | 一般 | 报告 §3.3 | "Chromium 只报告 1 次 `data:` 图片"是 `report-uri` 去重造成的假象；内联样式的来源有一处写错（实为主题变量 `ThemeSwitcherService`，不是 react-style-singleton）；"只能改上游"不严谨 | 已修复：改为以页面事件计数；内联样式来源改为主题变量、格式刷、sonner（计 2 次），并附源码行号；样式哈希代替 `'unsafe-inline'` 列为 P4 待评估项 | 报告 §3.3；`e2e/results/v02/*-sheet-main.json` 中按渠道分组的违规明细 |
| R4 | 一般 | `02-延期事项登记.md` DEF-001 | Playwright 驱动不了真实 Safari，"M1 重跑脚本"永远覆盖不到真实 Safari；而本机已有 Safari 26.5 | 已修复：新增页面自检模式与 `scripts/safari-selftest.ts`，用 `open -g -a Safari` 在后台打开自检链接，完成四个编辑场景与三种模式的阳性对照；DEF-001 缩小为只剩 Edge | `e2e/results/v02/safari/summary.json`：四个编辑场景全部通过、强制违规为零；阳性对照与 WebKit 一致 |
| R5 | 一般 | 报告 §5、`e2e/v02-csp.spec.ts` 注释 | "段落样式按钮主体会去掉加粗"无法复现，且与源码矛盾（该菜单是 `SELECTOR` 类型，点击只会弹出下拉菜单） | 已修复：删除该发现与错误注释。当初加粗核对失败的真正原因是设备预设把 UA 改成了 Windows，`Meta+A` 没有触发全选；这一点已在 §5 作为 M1 的 E2E 约束记录 | 报告 §5；`e2e/scenarios.ts` |
| R6 | 一般 | 报告 §2.2、§7 | 豁免名单是 43 条而不是 44 条；pnpm 内置的发布冷却默认值是非严格的，"保留默认值"起不到拦截作用 | 已修复：改正数字与描述，M1 事项改为"显式配置 `minimumReleaseAge`（显式配置后为严格模式），豁免名单变更纳入评审" | pnpm 12.6.0 CHANGELOG（#14409、#13687）；`spikes/m0/pnpm-workspace.yaml` |
| R7 | 一般 | 报告 §2.4 | 许可声明的分发义务没有写入：产物压缩掉了许可注释，6 个包的发布包里没有 LICENSE 文件 | 已修复：`deps-report.ts` 新增"缺少许可文本"检查；验证工程开启 Vite `build.license`（生成 107 个打包依赖的许可清单）；M1 事项写入"生成第三方许可清单并随部署包分发，补齐 6 个包的许可正文，CI 校验完整性" | `e2e/results/v01/deps.json` 的 `missingLicenseFile`；`dist/.vite/license.md` |
| R8 | 一般 | 报告 §7 | 回写 00 号计划书的清单不全：文档头、§0、§13.2 的 M5/M7、§13.4 V01、附录 C [S1] 仍按 rc 或正式版门槛书写；telemetry 应与 §11.3 对应 | 已修复：§7 表格补全上述位置，并补充"不得注册任何 `ITelemetryService` 实现" | 报告 §7 |
| R9 | 建议 | 报告多处、`deps-report.ts`、`url-scan.ts`、`bundle-size.ts`、`EditorShell.tsx` | 证据口径与可追溯性：控制台统计口径、首次渲染计时起点、URL 表漏了 `$` 模板、水印关键字没有进入扫描、"一份实例"只查了版本、Worker 证据只有消息计数、"79 块"含样本块 | 已修复：控制台口径与计时起点写明；URL 表补 `$`；扫描范围限定为编辑器产物并加入 `watermark` 与动态代码检查；单例检查改为按安装路径；Worker 证据改为 RPC 方法与响应类型；包体积把样本块单独统计（词典 77 块）；设计文档补上探测策略去掉的 `img-src blob:`；另外给页面加了空图标，消除 favicon 的 404 噪音 | `e2e/results/v01/*.json`、`e2e/results/v02/*.json`、`e2e/results/p1/bundle-size.json` |

## 审查者的核对与重跑（摘要）

- 重跑 `deps-report.ts`、`vite build`（重建前后 171 个产物文件的 SHA-256 一致）、`url-scan.ts`、`bundle-size.ts`、V02 的 12 次运行：除时间戳与毫秒级计时外，结果与提交的一致。
- 静态核对：锁文件没有同版本多实例；只有 protobufjs 带安装脚本；protocol 的运行时代码不含 grpc；telemetry 只有接口；附录 B 引用的 27 个文件在 v1.0.0 中都存在且未变化；许可数字与 `licenses.json` 一致。
- 补充实验：Worker 内的阳性对照、eval 违规的可观测性、关闭 CSP 的请求记录、只给 HTML 加头时 Worker 的表现、内联样式的来源、Worker 的 RPC 方法、段落样式按钮的行为。结论均已吸收进本次修复。
- 收尾：审查者恢复了结果文件，没有修改 `refer/univer`，没有残留端口。

## 结论与遗留

- 结论：**修复后通过**。V01、V02 的结论成立，修复后的证据覆盖了三个自动化浏览器与真实 Safari，并界定了各采集渠道的盲区。
- 复验：修复后重跑 `npx playwright test`（33 个用例全部通过）与 `node scripts/safari-selftest.ts`（7 个步骤全部通过）。
- 遗留：DEF-001（Edge 补测，目标 M1）；P4 定稿 CSP（`font-src data:`、`blob:`、样式哈希、违规上报）；M1 事项见报告 §7。
