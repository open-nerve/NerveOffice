# P3 编辑语义与性能基线：审查报告

> 审查者：独立审查代理（与主会话同为 Claude Opus），两轮｜日期：2026-09-24（第一轮）、2026-09-25（第二轮）｜审查范围：第一轮 `62b5109..31fd689`，第二轮 `31fd689..d82db46`；修复见其后的提交｜结论：修复后通过

M0 的审查对象是验证结论：结论有没有可复现的证据支撑、实验设计有没有漏洞、对设计的影响是否写对写全。

## 检查项

- [x] 正确性与边界条件：V06、V08 的主体结论成立；V07 推荐的捕获时机有漏洞（R1）、V09 推荐的防火墙有副作用（R2）、V08 端到端的测法与推荐时机不一致（R3），均已修正并复验
- [x] 实验设计：补上"快 + 慢两次修改"、编辑模式对照组、复制、运行时切换界面、观察者效应、Worker 写回时间线等实验（R1、R2、R6、R8、R9）
- [x] 证据是否充分：审查者重跑了 V07、V09、V06 大表复制、V08（表格两档）与 V10，结果与报告一致；会话内的探针脚本已改为入库的用例（R8）
- [x] 安全：阅读模式不写保护类资源、不留痕迹；真实用户身份会锁死编辑的结论经对照实验证实
- [x] 约束符合性：没有引入 Pro；`@univerjs/*` 精确锁定同一版本；`refer/univer` 未被修改；提交信息没有 AI 署名；内部 API 登记已补全（R11）
- [x] 文档：报告、插件档案 §5、Phase 文档、交接单、延期登记已按审查意见更新

## 问题清单

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| R1 | 重要 | 报告 §3.4；`e2e/p3-helpers.ts`；`e2e/v07-formula.spec.ts` | 推荐的捕获时机只在进入待捕获时等一次接口，之后的新修改只延长静默；静默窗口内再改一次、且计算超过 1 秒时，会捕获到过期的公式值，之后也不会再捕获（Worker 模式实测 200/200 过期）；"按建议时机"的用例没有从修改时刻开始，也没有在窗口内再改一次 | 已修复：捕获时机改为 `src/harness/capture-timing.ts`：每轮在最后一次修改之后发起等待；等到距最后一次修改满 1 秒；再等最近一轮计算"逐表收齐"（结果 mutation 中每张工作表都收到带 `applyFormulaCalculationResult` 的写回）；等待期间又有修改就重来；超时则标记"公式待更新"。V07 的"按规则"捕获改为从修改时刻起等待，新增"快 + 慢两次修改"用例（同时跑新旧两种规则）；V07 结论改为"有条件通过" | 报告 §3.2："按 §3.4"与"按 §3.4 补捕获"两列在 5 个场景 × 2 种模式 × 3 个浏览器中全部为 0；"静默窗口内再改一次"新规则 0/399（旧规则在 Chromium、Chrome 的 Worker 模式下 200/399）。`e2e/results/v07/`、`v07/edit-during-quiet/` |
| R2 | 重要 | 报告 §5；插件档案 §5.2；`src/harness/read-mode.ts` | 防火墙不看 `unitId`，挡住了单元格编辑器（内部文档单元）的内容同步；原地退出后经 F2 编辑会把上一个单元格的内容写进当前单元格；原地切换也换不了工具栏等界面 | 已修复：防火墙只取消作用于本文档（或没有 `unitId`）的非本地 mutation；原地进入时可以在运行时切换界面（`setUIVisible` 隐藏工具栏、`IContextMenuService.disable()`、`updateMenuConfig`）；切换用例增加"阅读模式下点选单元格 → 原地退出 → F2 编辑 A2"与运行时界面的核对 | 报告 §5.2：`combined` 方案表格 28/28、文字文档 8/8；原地切换后按 F2 编辑 A2 得到"苹果X"（三个浏览器，含公式 Worker 模式）；运行时切换界面在阅读中 0 个工具栏按钮、右键无菜单，退出后恢复。`e2e/results/v09/entries/`、`v09/switch/` |
| R3 | 重要 | 报告 §3.4、§4；`e2e/v08-capture.spec.ts` | 端到端测的是"固定等 1 秒 → 等待接口 → 捕获"的旧顺序，样本改的单元格也没有依赖公式；"+0.5 秒""只剩约 0.4 秒""要读会话状态省 0.5 秒"的结论因此不成立 | 已修复：端到端改为"修改 → 按新捕获时机等待 → 捕获、压缩、哈希"，分三种修改测量：没有依赖公式、牵动一个行合计（1 MiB 表格），牵动约 320 个公式（性能样本，主线程与 Worker）；报告 §3.4、§4 按新数据改写 | 报告 §4.2 端到端表：三种修改、两种模式、三个浏览器，p50 1.05–1.08 秒。`e2e/results/v08/capture/*-sheet-big-1m.json`（`endToEndMs`）、`v08/end-to-end/` |
| R4 | 一般 | 报告 §6.3、摘要；`e2e/v10-perf.spec.ts` | Worker 的内存论据不成立（只读了页面的堆，Worker 另有约 12 MiB）；首屏 p95 的差异来自第一次打开的冷缓存；冻结的机制是"每 500 个公式让出一次"，不是"常见公式分片、数组运算不分片" | 已修复：V10 用 CDP 分别读页面与 Worker 的堆；首屏分"第一次打开"与"之后的中位数"；报告改写冻结机制（`calculate-formula.service.ts:65、295、328-333`），并把 `intervalCount` 列为主线程模式的对照方案 | 报告 §6.2、§6.3：页面与 Worker 的堆分别列出；首屏分第一次与之后；让出间隔对比（500 / 100 / 20 / 5）证实冻结机制。复验中又发现冻结的测量本身有误并修正（见下文"修复后的复验"第 3 条）。`e2e/results/v10/`、`v10/interval/` |
| R5 | 一般 | 报告 §2.3 第 1 条；`e2e/v06-actions.spec.ts` 注释 | 自动行高的机制写错：`maxAutoHeightCount` 在 1.0.0 中没有被使用；同步计算的行数是 `ceil(10000 / 列数)`，其余交给空闲任务、每轮 500 行 | 已修复：报告与注释按源码改正（`packages/sheets/src/commands/commands/util.ts:35-43`、`packages/sheets-ui/src/services/auto-height.service.ts:77`），注明列数多的表几百行就会走空闲路径；V06 结论不受影响 | 报告 §2.3 第 1 条 |
| R6 | 一般 | 报告 §5；插件档案 §5.2；`src/harness/read-mode.ts` | 阅读模式把表格的复制也禁了（除"查看"外全部权限点置为 false） | 已修复：保留"查看"与"复制"两个权限点；V09 新增"阅读模式下复制"用例 | 报告 §5.2 四种方案表"表格复制"一列：points、firewall、combined 可以复制，facade 不行（Chromium、Chrome；无头 WebKit 不能读剪贴板，跳过）。`e2e/results/v09/copy/` |
| R7 | 一般 | 报告 §2.4、§4；`src/harness/perf.ts` | "按内容比较的口径计算哈希"的开销与口径都没有落实：规范化在主线程上 1 MiB 约 25 ms、5 MiB 约 70–85 ms；gzip 后的字节在不同浏览器中不同，不能用来做跨客户端的内容哈希 | 已修复：V08 新增规范化与规范化后 SHA-256 的测量；报告写清哈希的两种用途与口径：会话内"与上次相同就跳过"直接对 `save()` 的 JSON 文本算哈希；跨加载与服务端的"内容相同不递增修订号"用规范化口径，放进 Worker 或服务端，两端统一 | 报告 §4.2 捕获成本表的"规范化""规范化后 SHA-256"两列（1 MiB 约 18–25 ms、5 MiB 约 74–90 ms），§4.3 第 3、5 条写明两种口径与 gzip 结果因浏览器而异。`e2e/results/v08/capture/` |
| R8 | 一般 | 报告 §3.3、§8；Phase 设计 §3.3 | 探针脚本没有入库；观察者效应没有实验；运行时 `updateMenuConfig` 的核对、"第一次键盘输入被接受的时间"没有做；入口清单与计划有出入，偏差没有登记 | 已修复：新增 `e2e/v07-worker-timeline.spec.ts`（写回时间线与主线程采样）、`e2e/v06-observer.spec.ts`；运行时切换界面并入 V09 切换用例；V10 新增首次键入；V09 新增图片缩放、拖动行高两个入口与编辑模式对照组；偏差写入报告与交接单，报告说明完备性依靠防火墙按构造拦截，入口清单是抽样，并列出已知例外 | `e2e/v07-worker-timeline.spec.ts`、`e2e/v06-observer.spec.ts` 已入库，结果在 `v07/timeline/`、`v06/observer/`；报告 §2.2（观察者效应）、§3.3 第 2 条（写回时间线）、§5.2（入口清单与对照组）、§6.2（第一次输入） |
| R9 | 一般 | `e2e/v07-formula.spec.ts`；报告 §3 | 易变函数场景只核对内部一致性，过期值也能通过；"修改无关单元格后也会重算"没有证据；跨表!A3 的期望取自快照；"超时在主线程模式下形同虚设"对 chain、volatile 不成立 | 已修复：易变场景比较修改前后的 `RAND`；A3 期望改为 `SUM(B) − B1`（独立推算），A1 期望改为 `(A1 + 199) × 2`；改正超时的表述 | 报告 §3.2：判定分"独立推算"与"强制重算"两层；易变函数在两种模式、三个浏览器下修改无关单元格后都被重算。`e2e/results/v07/`（`volatileRecalculated`） |
| R10 | 一般 | 报告 §2.2 大表复制表 | "懒执行最后一条"实际包括公式结果写回；WebKit 那次 1.13 秒多一次公式写回；"17 条 syncOnly"只适用于 5 万单元格的样本 | 已修复：列名改为"本地 mutation 最后一条（含公式写回）"；syncOnly 条数按样本分别写；关掉拆分的理由改用源码（懒执行跑在 `requestIdleCallback` 上、超时 60 秒，忙碌或后台的标签页可以任意推迟，`lazy-execute-schedule.service.ts:141-145`），并注明代价（大表复制、删除的撤销记录会带上整表数据） | 报告 §2.2 |
| R11 | 一般 | 报告 §7 | 内部 API 登记不全；有等价公开 API 的地方用了内部 API | 已修复：补登 `Univer.__getInjector()`、工作表权限点的类与列表函数、`applyFormulaCalculationResult` 执行选项与公式 mutation 的 id、`IContextMenuService`、`IMenuManagerService`、`UnitAction.Edit` 的硬编码；变更检测改用 Facade 的 `CommandExecuted` 事件，文字文档的阅读模式改用公开的 `setReadOnly()` | 报告 §7 |
| R12 | 建议 | 报告 §2.1；插件档案 §5.3 | 排除名单的收录依据与 Phase 设计不一致；`onlyLocal` 不等于不改持久化内容 | 已修复：写明两条排除项的收录依据与 DEF-002 的关系；写明"排除 `onlyLocal`"成立的前提（以 `onlyLocal` 改持久化内容的路径都紧随一次被检测到的修改） | 报告 §2.1；插件档案 §5.3 |
| R13 | 建议 | 报告多处 | 措辞与环境：样本与组合的数量、V10 的键入字符、无头模式、n = 5 时 p95 就是最大值 | 已修复 | 报告摘要、§1、§6 |
| R14 | 建议 | 报告；延期登记 | 真实 Safari 没有覆盖，也没有登记延期；上游报告没有跟踪 | 已修复：登记 DEF-003（M4 前在真实 Safari 上复核）；交接单跟踪三项上游报告 | `02-延期事项登记.md` DEF-003；交接单 |

## 审查者的核对与重跑（摘要）

- 用仓库中的用例在 Chromium 上重跑 V07（10/10）、V09 与 V06 大表复制（22/22）、V08 的表格两档与 V10，数字与报告一致。
- 源码核对：Worker 写回的根因（`sheet-formula-calculation-result-apply.controller.ts:30-31`、`calculate-result-apply.controller.ts:47-99`、`data-sync-replica.controller.ts`、`remote-instance.service.ts:40-47`）；自动行高、大表拆分与删除的计数；对象权限的配置开关；本地授权的角色判断。
- 补充实验（E1–E15）：阅读模式下的单元格编辑器同步与原地退出后的编辑、forced 基准的有效性、两种捕获顺序的端到端、身份归因、高度过期的自适应行、Worker 模式下阅读模式不挡公式结果、Worker 的堆、规范化哈希的开销、观察者效应、静默窗口内的第二次修改、收窄后的防火墙、阅读模式下复制、首次键入。结论均已吸收进本次修复。
- 收尾：审查者恢复了结果文件，没有修改 `refer/univer`，没有残留端口。

## 第二轮审查（`31fd689..d82db46`）

第一轮修复之后，主会话在复验中自己又发现并修了几处问题（见下文"修复后的复验"第 2 条），结论也有实质变化：主线程冻结的数字被改正，表格改为默认启用公式 Worker。因此请另一个独立审查代理复核第一轮之后的全部变化。审查者复核了测量工具、捕获时机、V08–V10 的数字与结论、V09 的 Worker 变体，以及各文档之间的一致性。它用临时探针复现了 S1，并验证了修正思路。

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| S1 | 重要 | `src/harness/capture-timing.ts`、`change-detector.ts`；报告 §3 | 规则只等"最近一轮"收齐。计算进行中再改一处时，SDK 不把新修改并进正在进行的一轮：不相交就排队，相交就发 stop，但计算常常照样算完。规则会在排队的新一轮开始之前就捕获，存下过期值；遇到 stop 还直接视为收齐。过期值之后不会补捕获 | 已修复：<br>• 检测器按 SDK 触发服务的口径记录"最近一轮开始之后会触发计算的命令"，有就不算收齐。口径是 `IActiveDirtyManagerService` 的脏区转换：`shouldTrigger` 通过、`getDirtyData` 非空。<br>• 被 stop 的一轮也不算收齐。<br>• V07 新增"计算进行中再改一次"：5 种情形 × 2 种等待起点，新旧规则对照。<br>• 报告 §3、§6.3、§7、§9，插件档案、交接单同步修改；上游报告增加一条 | 报告 §3.2：<br>• 从第二次修改开始等时，旧规则在情形 A 存下 204/406 个过期值、情形 C 201/406（Chromium、Chrome）；<br>• 新规则 5 种情形 × 2 种起点 × 3 个浏览器全部 0/406，没有超时。<br>`e2e/results/v07/edit-during-calc/` |
| S2 | 一般 | 报告 §6.2、§6.3 第 5 条；`e2e/v10-perf.spec.ts` | Chromium 主线程模式的"第一次打开"其实是热启动：样本在同一个浏览器上下文里生成，编辑器代码已经缓存 | 已修复：样本改在另一个浏览器上下文里生成，报告按重跑数据改写 | 报告 §6.2 首屏表：Chromium 两种模式第一次打开 707 / 709 ms |
| S3 | 一般 | 报告摘要、§4.1、§4.4、§9 | 摘要与 §9 漏了 WebKit Worker 模式 1.77 秒的例外（Worker 是默认配置）；§12.2 的 2 秒目标对慢计算的文档不成立 | 已修复：写明例外（本机写入与加密只剩约 0.23 秒）与样本条件（公式计算不超过约 0.9 秒）；交接单 P6 按最坏值说明 | 报告摘要 V08、§4.1、§4.4、§9 |
| S4 | 一般 | 报告 §4.4 | 章节号写错（§7.2 应为 §7.4） | 已修复，并注明 §7.2 的会话内去重口径不变 | 报告 §4.4 |
| S5 | 一般 | 报告 §4.3 第 3 条 | "同一次加载里没有 mutation 时文本不变"说过了头（缩放等视图状态不产生 mutation 也会改字节） | 已修复：论据改为"这种口径的误差只会多上传一次，不会漏传" | 报告 §4.3 第 3 条 |
| S6 | 一般 | 报告 §4.3 第 2 条；交接单 | 把压缩交给 Worker 时，主线程仍要做 `save()` 加序列化，不只是 `save()` | 已修复：按同步段 p95 66–82 ms 估算 | 报告 §4.3 第 2 条、交接单 P6 |
| S7 | 一般 | 插件档案、报告 §3.3、§6.3、交接单 | 几处数字与数据不一致 | 已修复：按重跑后的 `p3-summary.ts` 逐条统一 | 报告与插件档案的相应条目 |
| S8 | 建议 | 报告 §6.3 第 3 条 | "`setTimeout` 探针比帧间隔长"的机制是推断 | 已采纳：让出间隔对比补上 Long Tasks。最长长任务与帧间隔一致、明显短于探针，证实是 `setTimeout` 被推后，不是主线程被连续占住 | 报告 §6.2 让出间隔表、§6.3 第 3 条 |
| S9 | 建议 | 报告 §6.3 取舍表 | Worker 模式的"到结果收齐"多含最多 20 ms 的轮询延迟 | 已采纳：取舍表注明 | 报告 §6.3 |
| S10 | 建议 | `e2e/v08-capture.spec.ts` | Long Tasks 的 `stop()` 改为异步后，端到端多计约一帧 | 已采纳：`measureCapture` 返回哈希完成的时刻，端到端按它计时 | `src/harness/perf.ts`；报告 §4.2 端到端表 |
| S11 | 建议 | 插件档案 §1、报告 §6.3 | Worker 内默认每 500 个公式才让出一次，stop 基本不生效，计算进行中再改一处时最新结果约晚一倍 | 已采纳：写进报告与插件档案，列为 M4 的评估项 | 报告 §6.3：情形 A 的新规则 4.7–4.8 秒才收齐，单独一次约 2.3–2.4 秒 |
| S12 | 建议 | 报告 §6.3 取舍表；交接单 | V06 动作矩阵没有 Worker 变体，验证工程默认仍是主线程模式 | 已采纳：表格的 42 种动作增加 Worker 变体；交接单写明表格验证要带 `worker=1` | 126 个用例全部通过；报告 §2.1 |
| S13 | 建议 | 报告 §3.4 | "写回先于完成通知"的引用不全 | 已采纳：补全引用，注明写回时间线用例是这一顺序的升级回归 | 报告 §3.4 第 6 条 |
| S14 | 建议 | 报告 §6.3、§6.4 | 内存数字只代表本样本；Worker 起不来时主线程模式要销毁重建 | 已采纳：取舍表标注样本并列入 M7；写明退路要销毁重建，由 M4 设计 | 报告 §6.3、插件档案 §1 |

第二轮审查者确认无误的要点：
- perf.ts 的修正正确，`measureCapture` 不受影响；
- "只有完成通知就视为收齐"成立：两种模式下，主线程收到的顺序都是"结果 → 各表写回 → 完成通知"，旧的完成通知不会串到新一轮；
- V08、V09、V10 的数字与汇总脚本一致；V10 的机制解释正确；"表格默认启用公式 Worker"有数据支撑；
- 用临时用例在 Chromium 的 Worker 模式下跑了全部 42 种表格动作，全部通过（已由 S12 入库）。

## 修复后的复验

1. **第一轮修复**（`375099c`）后，V06、V07、V09 三个浏览器共 406 个用例通过、2 个按设计跳过（`257fa19`）。
2. **主会话在复验中自己发现并修复的问题**：
   - **捕获时机永远"未收齐"**（`3ef831f`）：修改没有牵动任何公式时，这一轮只有完成通知、没有结果 mutation，旧判定一直等到 15 秒超时。第一次复验因此跑了 2.5 小时，还有 3 个撤销重做用例超时。改为"只有完成通知即视为收齐"后全部通过。
   - **V10 全量重算的计时多了约 500 ms**：用例在捕获等待之前又单独调了一次等待接口，已去掉。
   - **阻塞测量的两处错误**（`d82db46`）：事件循环探针与 Long Tasks 观察器在"计算刚结束、后续代码与长任务同属一个任务"时停止采集，前者漏记最后一段阻塞，后者把记录算进下一次测量。
     - 修正后，主线程模式的冻结从错记的 6–63 ms 改为 0.66–0.89 秒。三种口径（探针、帧间隔、Long Tasks）互相印证，并增加了让出间隔对比。
     - 据此决定表格默认启用公式 Worker，V09 的 `combined` 方案补测了 Worker 模式。
3. **第二轮修复**（S1–S14，本次提交）。
4. **最终复验**，全部在最终代码上运行：
   - 功能用例在不同端口上并行运行，全部通过：
     - V06：312 个；
     - V06 表格动作的 Worker 变体：126 个；
     - V07：72 个；
     - V09：55 个，另有 2 个按设计跳过（无头 WebKit 不能读剪贴板）。
   - 报告中的计时数字取单独运行的结果：
     - V07、V08、V10 共 105 个，全部通过；
     - V06 中与计时有关的自动行高与大表复制共 48 个，全部通过。
   - P1、P2 回归（验证工程的检测器改动会影响所有页面）：192 个通过、75 个按设计跳过，与此前一致。
   - 并行负载下还观察到一例：WebKit 的慢计算被拖慢，情形 A 的旧规则也存下了 201/406 个过期值，新规则仍为 0。WebKit 单独运行时，旧规则没有出错。

## 结论与遗留

**结论**：两轮审查的问题（R1–R14、S1–S14）全部处理并复验通过。P3 的结论如下：

| 验证项 | 结论 |
|---|---|
| V06 | 通过 |
| V07 | 有条件通过（捕获时机按报告 §3.4） |
| V08 | 通过 |
| V09 | 通过 |
| V10 | 通过，并据此决定表格默认启用公式 Worker |

**遗留**（都已登记或写入交接单）：
- **DEF-003**：在真实 Safari 上复核（M4 前）。
- **上游报告 4 项**（M4 实现管道时提交）：
  - Worker 模式下公式会话过早标记完成；
  - 等待接口不覆盖排队的下一轮；
  - 删除工作表的"大表"计数不一致；
  - `clear-drawing-transformer` 的类型声明。
- **M4**：
  - 按报告 §3.4 实现捕获时机，登记 `IActiveDirtyManagerService` 等内部 API；
  - Worker 创建失败时的判定与销毁重建；
  - Worker 内让出间隔的评估。
- **M7**：
  - 性能目标值（建议"计算期间帧间隔 p95 ≤ 100 ms"）；
  - 5 MiB 文档在 Worker 模式下的内存；
  - 用真实样本复核压缩率。
- **P5**：中文输入的响应。
- **P6**：本机写入与加密的预算（约 0.9 秒，最坏约 0.23 秒）。
