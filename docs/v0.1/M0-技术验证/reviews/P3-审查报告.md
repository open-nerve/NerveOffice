# P3 编辑语义与性能基线：审查报告

> 审查者：独立审查代理（与主会话同为 Claude Opus）｜日期：2026-09-24｜审查范围：`62b5109..31fd689`；修复见其后的提交｜结论：修复后通过

M0 的审查对象是验证结论：结论有没有可复现的证据支撑、实验设计有没有漏洞、对设计的影响是否写对写全。

## 检查项

- [x] 正确性与边界条件：V06、V08 的主体结论成立；V07 推荐的捕获时机有漏洞（R1）、V09 推荐的防火墙有副作用（R2）、V08 端到端的测法与推荐时机不一致（R3），均已修正并复验
- [x] 实验设计：补上"快 + 慢两次修改"、编辑模式对照组、复制、运行时切换界面、观察者效应、Worker 写回时间线等实验（R1、R2、R6、R8、R9）
- [x] 证据是否充分：审查者重跑了 V07、V09、V06 大表复制、V08（表格两档）与 V10，结果与报告一致；会话内的探针脚本已改为入库的用例（R8）
- [x] 安全：阅读模式不写保护类资源、不留痕迹；真实用户身份会锁死编辑的结论经对照实验证实
- [x] 约束符合性：没有引入 Pro；`@univerjs/*` 精确锁定同一版本；`refer/univer` 未被修改；提交信息没有 AI 署名；内部 API 登记已补全（R11）
- [x] 文档：报告、插件档案 §5、Phase 文档、交接单、延期登记已按审查意见更新

## 问题清单

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| R1 | 重要 | 报告 §3.4；`e2e/p3-helpers.ts`；`e2e/v07-formula.spec.ts` | 推荐的捕获时机只在进入待捕获时等一次接口，之后的新修改只延长静默；静默窗口内再改一次、且计算超过 1 秒时，会捕获到过期的公式值，之后也不会再捕获（Worker 模式实测 200/200 过期）；"按建议时机"的用例没有从修改时刻开始，也没有在窗口内再改一次 | 已修复：捕获时机改为 `src/harness/capture-timing.ts`：每轮在最后一次修改之后发起等待；等到距最后一次修改满 1 秒；再等最近一轮计算"逐表收齐"（结果 mutation 中每张工作表都收到带 `applyFormulaCalculationResult` 的写回）；等待期间又有修改就重来；超时则标记"公式待更新"。V07 的"按规则"捕获改为从修改时刻起等待，新增"快 + 慢两次修改"用例（同时跑新旧两种规则）；V07 结论改为"有条件通过" | （待填） |
| R2 | 重要 | 报告 §5；插件档案 §5.2；`src/harness/read-mode.ts` | 防火墙不看 `unitId`，挡住了单元格编辑器（内部文档单元）的内容同步；原地退出后经 F2 编辑会把上一个单元格的内容写进当前单元格；原地切换也换不了工具栏等界面 | 已修复：防火墙只取消作用于本文档（或没有 `unitId`）的非本地 mutation；原地进入时可以在运行时切换界面（`setUIVisible` 隐藏工具栏、`IContextMenuService.disable()`、`updateMenuConfig`）；切换用例增加"阅读模式下点选单元格 → 原地退出 → F2 编辑 A2"与运行时界面的核对 | （待填） |
| R3 | 重要 | 报告 §3.4、§4；`e2e/v08-capture.spec.ts` | 端到端测的是"固定等 1 秒 → 等待接口 → 捕获"的旧顺序，样本改的单元格也没有依赖公式；"+0.5 秒""只剩约 0.4 秒""要读会话状态省 0.5 秒"的结论因此不成立 | 已修复：端到端改为"修改 → 按新捕获时机等待 → 捕获、压缩、哈希"，分三种修改测量：没有依赖公式、牵动一个行合计（1 MiB 表格），牵动约 320 个公式（性能样本，主线程与 Worker）；报告 §3.4、§4 按新数据改写 | （待填） |
| R4 | 一般 | 报告 §6.3、摘要；`e2e/v10-perf.spec.ts` | Worker 的内存论据不成立（只读了页面的堆，Worker 另有约 12 MiB）；首屏 p95 的差异来自第一次打开的冷缓存；冻结的机制是"每 500 个公式让出一次"，不是"常见公式分片、数组运算不分片" | 已修复：V10 用 CDP 分别读页面与 Worker 的堆；首屏分"第一次打开"与"之后的中位数"；报告改写冻结机制（`calculate-formula.service.ts:65、295、328-333`），并把 `intervalCount` 列为主线程模式的对照方案 | （待填） |
| R5 | 一般 | 报告 §2.3 第 1 条；`e2e/v06-actions.spec.ts` 注释 | 自动行高的机制写错：`maxAutoHeightCount` 在 1.0.0 中没有被使用；同步计算的行数是 `ceil(10000 / 列数)`，其余交给空闲任务、每轮 500 行 | 已修复：报告与注释按源码改正（`packages/sheets/src/commands/commands/util.ts:35-43`、`packages/sheets-ui/src/services/auto-height.service.ts:77`），注明列数多的表几百行就会走空闲路径；V06 结论不受影响 | 报告 §2.3 第 1 条 |
| R6 | 一般 | 报告 §5；插件档案 §5.2；`src/harness/read-mode.ts` | 阅读模式把表格的复制也禁了（除"查看"外全部权限点置为 false） | 已修复：保留"查看"与"复制"两个权限点；V09 新增"阅读模式下复制"用例 | （待填） |
| R7 | 一般 | 报告 §2.4、§4；`src/harness/perf.ts` | "按内容比较的口径计算哈希"的开销与口径都没有落实：规范化在主线程上 1 MiB 约 25 ms、5 MiB 约 70–85 ms；gzip 后的字节在不同浏览器中不同，不能用来做跨客户端的内容哈希 | 已修复：V08 新增规范化与规范化后 SHA-256 的测量；报告写清哈希的两种用途与口径：会话内"与上次相同就跳过"直接对 `save()` 的 JSON 文本算哈希；跨加载与服务端的"内容相同不递增修订号"用规范化口径，放进 Worker 或服务端，两端统一 | （待填） |
| R8 | 一般 | 报告 §3.3、§8；Phase 设计 §3.3 | 探针脚本没有入库；观察者效应没有实验；运行时 `updateMenuConfig` 的核对、"第一次键盘输入被接受的时间"没有做；入口清单与计划有出入，偏差没有登记 | 已修复：新增 `e2e/v07-worker-timeline.spec.ts`（写回时间线与主线程采样）、`e2e/v06-observer.spec.ts`；运行时切换界面并入 V09 切换用例；V10 新增首次键入；V09 新增图片缩放、拖动行高两个入口与编辑模式对照组；偏差写入报告与交接单，报告说明完备性依靠防火墙按构造拦截，入口清单是抽样，并列出已知例外 | （待填） |
| R9 | 一般 | `e2e/v07-formula.spec.ts`；报告 §3 | 易变函数场景只核对内部一致性，过期值也能通过；"修改无关单元格后也会重算"没有证据；跨表!A3 的期望取自快照；"超时在主线程模式下形同虚设"对 chain、volatile 不成立 | 已修复：易变场景比较修改前后的 `RAND`；A3 期望改为 `SUM(B) − B1`（独立推算），A1 期望改为 `(A1 + 199) × 2`；改正超时的表述 | （待填） |
| R10 | 一般 | 报告 §2.2 大表复制表 | "懒执行最后一条"实际包括公式结果写回；WebKit 那次 1.13 秒多一次公式写回；"17 条 syncOnly"只适用于 5 万单元格的样本 | 已修复：列名改为"本地 mutation 最后一条（含公式写回）"；syncOnly 条数按样本分别写；关掉拆分的理由改用源码（懒执行跑在 `requestIdleCallback` 上、超时 60 秒，忙碌或后台的标签页可以任意推迟，`lazy-execute-schedule.service.ts:141-145`），并注明代价（大表复制、删除的撤销记录会带上整表数据） | 报告 §2.2 |
| R11 | 一般 | 报告 §7 | 内部 API 登记不全；有等价公开 API 的地方用了内部 API | 已修复：补登 `Univer.__getInjector()`、工作表权限点的类与列表函数、`applyFormulaCalculationResult` 执行选项与公式 mutation 的 id、`IContextMenuService`、`IMenuManagerService`、`UnitAction.Edit` 的硬编码；变更检测改用 Facade 的 `CommandExecuted` 事件，文字文档的阅读模式改用公开的 `setReadOnly()` | 报告 §7 |
| R12 | 建议 | 报告 §2.1；插件档案 §5.3 | 排除名单的收录依据与 Phase 设计不一致；`onlyLocal` 不等于不改持久化内容 | 已修复：写明两条排除项的收录依据与 DEF-002 的关系；写明"排除 `onlyLocal`"成立的前提（以 `onlyLocal` 改持久化内容的路径都紧随一次被检测到的修改） | 报告 §2.1；插件档案 §5.3 |
| R13 | 建议 | 报告多处 | 措辞与环境：样本与组合的数量、V10 的键入字符、无头模式、n = 5 时 p95 就是最大值 | 已修复 | 报告摘要、§1、§6 |
| R14 | 建议 | 报告；延期登记 | 真实 Safari 没有覆盖，也没有登记延期；上游报告没有跟踪 | 已修复：登记 DEF-003（M4 前在真实 Safari 上复核）；交接单跟踪三项上游报告 | `02-延期事项登记.md` DEF-003；交接单 |

## 审查者的核对与重跑（摘要）

- 用仓库中的用例在 Chromium 上重跑 V07（10/10）、V09 与 V06 大表复制（22/22）、V08 的表格两档与 V10，数字与报告一致。
- 源码核对：Worker 写回的根因（`sheet-formula-calculation-result-apply.controller.ts:30-31`、`calculate-result-apply.controller.ts:47-99`、`data-sync-replica.controller.ts`、`remote-instance.service.ts:40-47`）；自动行高、大表拆分与删除的计数；对象权限的配置开关；本地授权的角色判断。
- 补充实验（E1–E15）：阅读模式下的单元格编辑器同步与原地退出后的编辑、forced 基准的有效性、两种捕获顺序的端到端、身份归因、高度过期的自适应行、Worker 模式下阅读模式不挡公式结果、Worker 的堆、规范化哈希的开销、观察者效应、静默窗口内的第二次修改、收窄后的防火墙、阅读模式下复制、首次键入。结论均已吸收进本次修复。
- 收尾：审查者恢复了结果文件，没有修改 `refer/univer`，没有残留端口。

## 修复后的复验

（待填）

## 结论与遗留

（待填）
