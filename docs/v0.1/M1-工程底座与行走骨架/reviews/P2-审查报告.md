# P2 服务端基础设施：审查报告

> 审查者：独立的审查子 Agent（与主会话同一个模型，只读审查；探针放在临时目录）｜日期：2026-09-26｜审查范围：`13e4e24` 到合并前的最后一个提交（初审 `13e4e24..a9facf2`；第一轮修复 `a9facf2..bc279dc`；第二轮 `bc279dc..5b14564`；第三轮 `5b14564..08444ae`；第四轮 `08444ae..7b17ce1`）｜结论：修复后通过

审查分两路并行，之后三轮复验，每轮都是新的独立子 Agent：
- **初审 A（后端运行时）**：阻断 1 项、重要 4 项、一般 8 项、建议 8 项；
- **初审 B（工具链、门禁、测试与文档）**：重要 3 项、一般 8 项、建议 5 项；
- **第一轮复验**：初审各项中 A8、B11、B12 部分修复，其余已修复或按计划留给 P5。新发现 N1–N7：重要 1 项、一般 4 项、建议 2 项。主会话追查 N2 的附带影响时，另发现 N8（重要）；
- **第二轮复验**：N1–N7 中 N2、N6 部分解决。新发现 F1–F8：一般 5 项、建议 3 项；
- **第三轮复验**（合并前，重点审 N8）：F1–F8 与 N8 已解决，结论"可以合并"。另有 4 条小发现 G1–G4（一般 1 项、建议 3 项），合并前已处理。

每轮复验都对修复做变异验证：把代码临时改回修复前的写法，确认对应的测试失败，再恢复（第一轮覆盖初审的每个修复点，第二轮 15 次，第三轮 12 次；主会话对 G1、G3 的修复另做 3 次）。存活的变异都登记成了问题并补上测试（N3、F7）；唯一的例外是与其他规则重复的一项配置，删掉它不影响行为。

初审的共同点：
- 基础设施对"正常路径"都有测试；问题集中在异常路径：连接在借出期间断开、查询失败、客户端中途断开、压缩内容损坏。
- 其次是"自动"约束的绕过：注入 `DATABASE`、`import { env }`、`.raw` 的别名、不按文件名写控制器。
- 修复的原则：从根因处理，并给每一条补上能复现原问题的测试。A1 的回归测试已确认：去掉修复时，测试以"未处理的错误：terminating connection due to idle-in-transaction timeout"失败。

## 检查项

- [x] 正确性与边界条件
- [x] 模块边界、依赖方向与 SOLID
- [x] 测试是否充分：单元、集成覆盖 M1 总设计 §5 的 P2 验收（本 Phase 没有用户故事与 E2E）
- [x] 安全：CSP 与安全头、错误响应不带内部细节、日志脱敏（含数据库错误）、SQL 参数化、审计只追加、配置里的机密
- [x] 性能与资源使用：就绪检查最多占一个连接；连接池的超时与 keepalive；事务的连接不泄漏（第三轮复验：400 个并发的混合事务）；本机 `pnpm verify` 约 30 秒
- [x] 文档：架构总览、ADR-004/005/006、交接单已更新

## 问题清单

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| A1 | 阻断 | `database/pool.ts` | 连接借出期间被数据库断开时，pg 客户端的 error 事件没有监听者，进程退出 | 已修复：每个连接在 `connect` 时挂常驻的 error 监听，只记日志；迁移连接同样挂上 | 集成测试"借出期间连接被数据库断开"；去掉修复时该测试以未处理的错误失败 |
| A2 | 重要 | `logging/*`、`error-filter.ts` | 查询失败时，绑定参数与行里的值经异常序列化进日志 | 已修复：自己的 `serializeError`，数据库错误只留类型、带占位符的 SQL、SQLSTATE 与约束、表、列名；pino-http 显式使用同一个序列化 | 单元测试 `error-serializer.test.ts`；集成测试"数据库报错时……日志不带参数" |
| A3 | 重要 | `shutdown.ts`、`in-flight-requests.ts` | 关闭应用不受时限约束；客户端断开后处理器仍在执行时就关闭连接池 | 已修复：处理器由全局拦截器计数；关闭应用受退出时限约束，超时记为强制 | 单元测试（拦截器、关闭应用超时）；集成测试"客户端中途断开、处理器仍在执行" |
| A4 | 重要 | `http-logger.ts`、`error-filter.ts` | 中断的请求记成 200"请求完成"；之后的意外错误不进日志 | 已修复：中断记为 warn、aborted、不记状态码；过滤器把意外错误记进这个请求的日志；`levelFor` 看 `response.err` | 单元测试；集成测试"客户端中途断开：记一条请求中断……" |
| A5 | 重要 | `config.ts` | `_FILE` 内容不合法时问题列表为空 | 已修复：问题记在 `_FILE` 变量上并说明原因，空文件单独说明 | 单元测试"文件内容不合法时……" |
| A6 | 一般 | `json-body.ts` | 损坏的压缩请求体得到 500；压缩炸弹没有测试 | 已修复：没有类型的 4xx 解析错误按状态映射；设计写明接受压缩请求体、上限按解压后计算 | 集成测试"压缩的请求体：损坏时 400；……413" |
| A7 | 一般 | `migrations.ts` | 迁移命令不复核结果 | 已修复：读取时校验序号与时间戳；执行后复核必须一致 | 单元测试"时间戳没有递增、序号不连续" |
| A8 | 一般 | `pool.ts`、`migrations.ts` | 没有 TCP keepalive 与客户端侧的查询时限；迁移连接没有连接时限 | 已修复：keepAlive、`query_timeout`（语句超时加 5 秒）；迁移连接 10 秒的连接时限 | 代码核对；连接参数的集成测试 |
| A9 | 一般 | `database-readiness.ts` | 并发的探针各占一个连接 | 已修复：同一时间只做一次检查，并发共享，结果缓存 1 秒，超时后不再另开 | 单元测试"检查超过时限……不会再开一次""并发的探针共享" |
| A10 | 一般 | `root-logger.ts`、`config.ts` | 脱敏的键名覆盖不到配置里的连接串 | 已修复：连接串包成 `Secret`（转 JSON、转字符串、打印时都是脱敏值）；脱敏清单补上 P3、P4 会用的键名 | 单元测试 `secret.test.ts`、配置"整个配置被序列化时不带密码" |
| A11 | 一般 | `database-readiness.ts` | 就绪检查的超时、查询失败等分支没有测试 | 已修复：单元测试覆盖一致、待执行、不可达、查询失败、超时、并发、缓存。"正在退出"经真实应用观察不到（开始退出时立即停止接收新连接），由单元测试覆盖；是否留出"只让就绪失败"的时间见 A16 | 单元测试 `database-readiness.test.ts` |
| A12、B1 | 重要 | `.gitignore` | `.env.development` 没入库 | 已修复：放行并入库 | `git ls-files` |
| A13、B9 | 一般 | `apps/api/drizzle.config.js` | 构建产物被提交 | 已修复：删除；构建加上 `noEmitOnError`（错误的构建配置下 tsc 带着错误照样输出） | `git ls-files` |
| A14 | 建议 | `migrations.ts` | 解锁失败会掩盖原来的错误 | 已修复：解锁失败忽略（会话结束时锁自动释放） | 代码核对 |
| A15、B14 | 建议 | `tests/integration/src/support/*` | 没有清理遗留的测试库；部分测试用维护库；进程测试在 exit 上判定退出；可能用旧产物测 | 已修复：遗留的库在下次运行时清理；启动应用必须给出自己的库；改为 close 事件；产物比源码旧时报错 | 集成测试全绿；遗留库已被清理 |
| A16 | 建议 | 就绪探针与退出 | 与滚动发布的兼容、503 暴露迁移名、退出时看不到"就绪 503" | 留给 P5：部署形态定下后决定，写入交接单 | — |
| A17 | 建议 | `validation.ts` | 校验说明没有条数与长度上限 | 已修复：最多 5 处，每处 64 个字符 | 单元测试 |
| A18 | 建议 | `config.ts` | 请求时限越界时多报一条 | 已修复：交叉检查只在各变量合法之后做 | 单元测试 |
| A19 | 建议 | `request-origin.ts` | 不是合法 IP 的地址让审计写入失败 | 已修复：存不进数据库的地址不写 | 单元测试 `request-origin.test.ts` |
| A20 | 建议 | 审计 | 审计动作与数据库 CHECK 之间没有漂移检查 | 已修复：集成测试把每个审计动作写入一次；另有门禁 `schema`（B12） | 集成测试"每个登记的审计动作都能写入" |
| A21 | 建议 | 数据库角色 | 生产上应用的角色不能是表的所有者 | 留给 P5：写入交接单 | — |
| B2 | 重要 | `eslint.config.ts` | "只有仓储访问数据库"可以绕过 | 已修复：`DATABASE`、`executorOf` 与数据库类型只在仓储与 database 模块；表定义只在仓储；禁止动态导入数据库的库；服务用 `TransactionRunner`，拿到不透明的 `Transaction`；控制器不引用 `TransactionRunner` | lint 自测（后端） |
| B3 | 重要 | `eslint.config.ts` | "只有 config 读环境变量"可以绕过 | 已修复：`import { env }`、解构、`globalThis.process.env` 都拦下 | lint 自测 |
| B4、B6 | 一般 | `migrations-gate.ts`、`ci.yml` | CI 只比较最后一个提交；环境变量占用 `NERVE_` 命名空间 | 已修复：推送事件以推送之前的提交为基准（完整历史）；改名为 `GATE_MIGRATIONS_BASE` | 临时 git 仓库的测试"一次推送含多个提交" |
| B5 | 一般 | `migrations-gate.ts` | 非 ASCII 的迁移名被 git 转义 | 已修复：`-z`；迁移名必须是"四位序号_名称" | 临时 git 仓库的测试；`migrations.test.ts` |
| B7 | 一般 | `eslint.config.ts` | SQL 拼接规则比文档说的弱 | 已修复：任何 `.raw`（含解构与别名）、对象写法的 `text` 都拦下；先拼成变量再传入的写法写明由审查保证 | lint 自测 |
| B8 | 一般 | `eslint.config.ts` | 控制器规则只按文件名生效；没覆盖 `@Headers` 等 | 已修复：参数规则对所有后端文件生效；`@Controller` 只写在 `*.controller.ts`；禁止不经校验的装饰器 | lint 自测 |
| B10 | 一般 | `run.ts` | 从 git 读取基准的代码没有测试 | 已修复：装配拆成可注入的模块，用临时 git 仓库测试 | `migrations-gate.test.ts` |
| B11 | 一般 | 文档 | 六处不一致 | 已修复 | 文档核对 |
| B12 | 建议 | — | 没有"表定义与迁移不同步"的检查 | 已采纳：门禁 `schema` | `schema-gate.test.ts`；门禁对仓库现状通过 |
| B13 | 建议 | `migrations.ts` | 不检查快照的 prevId 链 | 已采纳 | `migrations.test.ts` |
| B15 | 建议 | `eslint.config.ts` | 覆盖块各自重复限制；忽略整个迁移目录过宽；ADR-004 补一句 | 已修复：`apiRules()` 组合，自测断言每类文件都保留 Univer、Pro 与 Nest Logger 的限制；只忽略迁移目录里生成的 JSON；ADR-004 已补 | lint 自测 |
| B16 | 建议 | `README.md` | 当前阶段的说明过时 | 已修复 | — |

## 第一轮复验

第一轮复验（独立的审查子 Agent，与主会话同一模型）逐项核实初审的修复：A8、B11、B12 部分修复，其余已修复或按计划留给 P5（逐项结论与证据见上表的"复验证据"一列）。新发现 7 项：

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| N1 | 重要 | `gates/schema-gate.ts` | 改列名时门禁静默通过：drizzle-kit 遇到"新建还是改名"要交互确认，没有终端时只往标准错误写一行、退出码 0、不生成文件，门禁把"没有生成文件"当作同步（B12 的残余） | 已修复：没有生成文件时，还要求标准输出里有"没有变化"的结论、标准错误为空，否则报 `schema/unknown`；`commandOutput` 同时返回标准输出与标准错误 | `schema-gate.test.ts`（只写标准错误、没有任何输出的样例）；实测把 `request_id` 改名后门禁失败 |
| N2 | 一般 | `database/pool.ts`、`migrations.ts` | keepalive 只是名义上开启：没设探测延迟，pg 传 0，用系统默认（2 小时）（A8 的残余） | 已修复：两处都从空闲 10 秒开始探测 | `pool.test.ts`（连接参数） |
| N3 | 一般 | `logging/root-logger.test.ts` | 根日志的异常序列化没有测试：删掉后单元与进程内集成测试照样全绿 | 已补：根日志与子日志写入带参数的数据库错误，输出里没有参数 | 删掉根日志的序列化时该测试失败 |
| N4 | 一般 | `logging/nest-logger.ts` | Nest 日志适配把异常的原始消息当作日志消息，数据库错误的参数会进日志；Nest 传进来的堆栈开头也是这条消息 | 已修复：Error 的消息用不带值的说明（`safeErrorMessage`，与 `serializeError` 同一套规则），堆栈参数只留调用帧；写明 Nest 拼好的字符串消息无法识别，意外错误由全局异常过滤器自己记 | `nest-logger.test.ts`；改回原来的写法时该测试失败 |
| N5 | 一般 | 文档 | `fetch-depth: 2` 的旧说法（设计、计划）；ADR-005 的连接池与事务过时；架构总览 §4 没写收紧后的规则；ADR-006 没写中断请求的日志（B11 的残余） | 已修复；另订正设计 §3.8 里审计接口的旧写法（`executor` 改为 `transaction`） | 文档核对 |
| N6 | 建议 | `eslint.config.ts` | lint 的残留写法：动态导入 pg 的子路径；app 层整个目录都能拿数据库句柄；给 `process` 改名；`concat()` 拼接 SQL；改名引用 `Headers` 等；`@UploadedFile` 不在清单里 | 已修复：动态导入与静态引用同一个范围（含子路径与 `pg-*`）；只有 `app/index.ts` 能转出数据库句柄；禁止给 `process` 改名与命名空间引用；`concat()` 同样拦下；不经校验的装饰器从 `@nestjs/common` 引用时就拦下，清单加上上传装饰器。其余写法（先拼成变量、先赋给别的变量、经命名空间调用装饰器）写明由审查保证 | lint 自测新增 4 个用例，原有用例补充断言 |
| N7 | 建议 | 设计、交接单 | 处理器计数不含守卫与中间件（P3 的会话守卫查库时会重现 A3）；A16、A21 没有登记；A21 在设计 §3.8 与 ADR-005 缺少前提 | 已处理：设计 §3.9 写明守卫与中间件不在计数之内，关闭连接池时 `pool.end()` 会等借出的连接，受退出时限约束；A16、A21 写进交接单给 P5 的备注；A21 的前提写进设计 §3.8 与 ADR-005 | 文档核对 |

主会话追查 N2 的附带影响时另发现一项：

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| N8 | 重要 | `database/transaction-runner.ts` | drizzle 0.45.3 的 `transaction()` 在连接池上自己借连接，而 `BEGIN` 写在 `try` 之外：`BEGIN` 失败（连接刚断开、查询超时）时连接永不归还，几次网络故障就能耗尽连接池，且不会自己恢复。它归还连接时也不说明是否出错：查询超时后，没能发出 `ROLLBACK` 的连接会带着未结束的事务回到池里，下一个事务提交时把它一起提交（第三轮复验实测） | 已修复：`TransactionRunner` 自己借出、归还连接，在这个连接上执行 drizzle 的事务。以 `AppError` 结束时回滚已经成功，连接照常放回；其他失败（含 `BEGIN`、`COMMIT`、回滚失败）一律丢弃连接，与 pg 连接池自己的 `query()` 一致；连接不是空闲状态时同样丢弃（G3） | 单元测试覆盖 `BEGIN`、`COMMIT`、回滚失败与业务错误；集成测试在上限为 1 的连接池上确认：业务错误后仍是同一个连接，其他失败后换了连接 |

## 第二轮复验

第二轮复验（另一个独立的审查子 Agent，与主会话同一模型）核实 N1–N7：N1、N3、N4、N5、N7 已解决，N2、N6 部分解决（迁移连接的 keepalive 没有测试；lint 有新的误伤与残余写法）。15 次变异验证后工作区都已恢复。新发现 8 项：

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| F1 | 一般 | `eslint.config.ts` | 数据库库的静态规则按 gitignore 语义匹配，`pg-*` 会误拦本地的 `./pg-errors.ts`；静态与动态的范围也不一致（N6 修复引入） | 已修复：静态规则改为按包名锚定开头的正则 `^(?:pg\|drizzle-orm)(?:$\|\W)`；动态导入另由 F3 整体禁止 | lint 自测：包本身、子路径与 `pg-*` 拦下，`pgx-utils`、本地的 `pg-*.ts` 不拦 |
| F2 | 一般 | `eslint.config.ts` | 从 `@nestjs/common` 的深层路径引用可以绕过装饰器与 `Logger` 的限制（`exports` 开放了 `./*`） | 已修复：后端只从包的入口引用 Nest | lint 自测：深层路径的 `Req`、`Logger` 与命名空间引用都拦下 |
| F3 | 一般 | `eslint.config.ts` | 用 `.js` 扩展名写相对引用（TypeScript 同样解析到源文件）就能绕过数据库句柄与 `TransactionRunner` 的限制（B2 就有）；动态导入也能绕过 | 已修复：后端的相对引用只能写 `.ts`；后端不用动态导入（受限导入与模块边界只检查静态引用），测试里唯一的动态导入改为静态 | lint 自测 |
| F4 | 一般 | `logging/root-logger.ts` | 只传异常、不给消息时，pino 用异常原来的消息作 `msg`，数据库错误的值会进日志（目前没有调用点这样写） | 已修复：根日志的 `logMethod` 钩子在这种写法下用 `safeErrorMessage` 补上消息，子日志（含 pino-http 的）同样经过它 | `root-logger.test.ts`：两种写法与子日志；其他写法不变 |
| F5 | 建议 | `eslint.config.ts` | `concat` 规则对 `query()`、`execute()` 的每个参数都生效，误拦参数数组 | 已修复：拼接的三条检查都只看第一个参数（SQL 文本，含对象写法的 `text`） | lint 自测；去掉"第一个参数"的限定时自测失败 |
| F6 | 建议 | 设计 §3.1、架构总览 | "由审查保证"的说明不完整，其中"经命名空间调用装饰器"的说法不对（命名空间引用已被拦下） | 已修复：每一行写明残余写法；新增"引用的写法唯一"一行；总览同步 | 文档核对 |
| F7 | 一般 | 测试 | 变异存活：迁移连接的 keepalive、schema 门禁的标准错误条件；`commandOutput` 没有测试，被信号结束时报"退出码 null" | 已修复：迁移连接的参数抽成 `migrationClientConfig` 并测试；补"说没有变化但标准错误有内容"的用例；`commandOutput` 补正常、退出码、信号、无法执行四个用例，被信号结束时写明信号 | 新增的单元测试 |
| F8 | 建议 | 设计 §3.9 | 第 4 步"此时已经没有在途的请求与处理器"与第 0 步矛盾：停在守卫里的请求可能晚到，拿连接时报错 | 已修复：第 4 步写明例外与日志的样子；交接单的 P3 备注同步 | 文档核对 |

## 第三轮复验

第三轮复验用真实数据库逐个情况验证了 N8：成功、业务错误、其他错误、`BEGIN` 失败、`COMMIT` 失败（延迟检查的唯一约束）、业务错误之后 `ROLLBACK` 失败、work 同步抛出、取不到连接、事务中途被数据库断开、嵌套事务（保存点），以及 4 个连接、400 个混合事务的并发。连接都按规则放回或丢弃，没有泄漏，数据库端也没有"事务中空闲"的连接。

它还实测了 drizzle 原实现的后果，比 N8 当初描述的更重：
- `BEGIN` 失败后，上限为 1 的连接池再也取不到连接；
- 客户端查询超时后，`ROLLBACK` 没能发出，连接带着未结束的事务回到池里，下一个事务的 `COMMIT` 把前一个失败事务的写入一起提交了。

新发现 4 项：

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| G1 | 一般 | `logging/root-logger.ts` | F4 的钩子在对象自带 `msg` 时又补一个消息，原始输出里出现两个 `msg` 键，调用方给的消息被覆盖 | 已修复：与 pino 自己的判断一致，对象里的 `msg` 已定义时原样透传 | `root-logger.test.ts` 检查原始行里只有一个 `msg`；去掉修复时失败 |
| G2 | 建议 | 设计 §3.1 | "控制器不引用仓储与 `TransactionRunner`"一行没写残余写法：经模块的入口转出之后再引用，lint 不报 | 已补：由审查保证 | 文档核对 |
| G3 | 建议 | `database/transaction-runner.ts` | work 吞掉失败的语句却正常返回时，事务已中止，`COMMIT` 被数据库静默当作回滚，`run()` 却报告成功（原有的风险，不是 N8 引入） | 已修复：work 返回后事务状态为"中止"就抛错并回滚；归还时连接不是空闲状态一律丢弃，不依赖 drizzle 是否发出了 `ROLLBACK`。设计写明：预期会失败的语句放进保存点，或者改用 `ON CONFLICT` | 单元测试（中止、非空闲）；集成测试（吞掉错误时报错，保存点里的失败照常提交）；去掉任一处修复时测试失败 |
| G4 | 建议 | 文档 | N8 的后果写得不准 | 已改：设计 §3.7、ADR-005 与本报告的 N8 | 文档核对 |

## 结论与遗留

**结论：修复后通过。**
- 初审 37 项：35 项已修复或采纳；A16、A21 按计划留给 P5，写进交接单。A21 的前提（应用使用的数据库角色不能是表的所有者或超级用户）已写进设计 §3.8 与 ADR-005。
- 三轮复验的新发现 N1–N8、F1–F8、G1–G4 全部处理。

**合并前的最终状态**（`7b17ce1`，本机 `pnpm verify`）：
- 全部步骤通过：lint、类型检查、静态检查（含 `migrations` 与 `schema`）、单元与集成测试、构建、依赖与产物检查、E2E（Chromium、Chrome、WebKit）。
- 单元与集成测试合计 57 个文件、595 个用例；覆盖率：语句 98.33%、分支 92.23%、函数 96.98%、行 98.38%。

**遗留**：
- 没有阻断项。
- 自动检查覆盖不到、由审查保证的写法，逐条列在设计 §3.1 的写法限制表里，P3 起的代码审查按它核对。
- A16（就绪探针与滚动发布）、A21（生产上的数据库角色）在 P5。
