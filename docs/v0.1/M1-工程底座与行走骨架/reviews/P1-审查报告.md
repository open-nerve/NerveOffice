# P1 工程底座：审查报告

> 审查者：独立的审查子 Agent（与主会话同一个模型，只读审查，实验在临时目录与独立副本中进行）｜日期：2026-09-26｜审查范围：`7ef14e2` 到合并前的最后一个提交（初审 `7ef14e2..ae6d7e8`；第一轮修复 `46091a6`；第二轮修复 `db37be2`；第三轮修复见下文 N9–N11）｜结论：修复后通过

审查共三轮：
- **初审**：发现 2 项阻断、7 项重要、6 项一般，另有 8 条建议；
- **复验**：确认上述问题都已修复，但修复引入 1 项重要问题（N1），另有 4 项一般问题（N2–N5）；
- **针对性复验**：确认 N1–N5 与建议项都已修复；第二轮修复又引入 1 个会误删文件的小问题（N9），审查者的结论是"修完 N9 即可合并"，只需复跑 lint 自测并确认目录清理的行为。

初审各项检查的共同点：门禁规则的自测样例都能拦下，但 pnpm 12、Vite 8、ESLint 的真实输出与样例不一致，门禁对真实的违规漏报。修复的原则是：
- 外部工具的输出先经 zod 校验；
- 把真实输出存成样例（`tools/fixtures/`），检查规则用真实样例测试。

## 检查项

- [x] 正确性与边界条件
- [x] 模块边界、依赖方向与 SOLID
- [x] 测试是否充分：单元、集成、E2E 覆盖本 Phase 的用户故事（US-M1-11：门禁自测）
- [x] 安全：供应链（发布冷却期、可信度策略、安装脚本、pnpmfile、漏洞扫描）、产物中的动态代码与外部请求
- [x] 性能与资源使用：本机 `pnpm verify` 约 20 秒（不含 E2E）；门禁的真实大依赖图（677 个实例）没有误报
- [x] 文档：架构总览、ADR、交接单已更新

## 问题清单

| 编号 | 等级 | 位置 | 问题 | 处理 | 复验证据 |
|---|---|---|---|---|---|
| R1 | 阻断 | `eslint.config.ts` | import-x 没有声明 TypeScript 的扩展名与解析器，`no-cycle` 看不到 `.ts` 之间的引用，循环依赖检查完全不生效 | 已修复：声明扩展名与解析器，`ignoreExternal` | lint 自测"TypeScript 文件之间的循环依赖会失败"；复验中 `.ts`、`.tsx` 与功能模块经入口的循环都被拦下 |
| R2 | 阻断 | `dependency-graph.ts`、`run.ts` | 生产依赖图漏掉项目层的可选依赖；`licenses list --prod` 排除可选依赖。Pro 包、第二份 react、不合规的许可经 `-O` 引入都能通过 | 已修复：依赖图纳入可选依赖；生产依赖的许可按安装实例的路径到全量许可清单里查 | 真实输出样例 `ls-optional-dependencies.json`；复验中可选依赖里的 Pro、别名引入的 react、MPL 都被拦下 |
| R3 | 重要 | `licenses.ts` | 开发依赖的许可检查放过 pnpm 12 输出的 `Unknown`，以及 `GPLv3`、`gpl-3.0` 等写法 | 已修复：许可标识不区分大小写；识别未声明与强 copyleft 的各种写法 | 真实输出样例 `licenses-list.json`；复验 7 种写法全部拦下 |
| R4 | 重要 | `license-bundle.ts`、`vite.config.ts` | Vite 的 `build.license` 不收集 Worker 的产物；缺许可的标题解析错误会掩盖缺正文 | 已修复：改用自己的许可收集插件（主构建与 Worker 各挂一份），门禁读插件的清单；00 号计划书记为 r5 | 插件测试用"第三方包只在 Worker 里"的样例工程真实构建，并有对照；复验中 Worker 里再起的 Worker 也被收集 |
| R5 | 重要 | `pnpm-config.ts`、`audit.ts` | 不查削弱策略的设置（`dangerouslyAllowAllBuilds`、`minimumReleaseAgeStrict: false`、`trustPolicyIgnoreAfter` 等）；`auditConfig` 能把漏洞从清单里藏起来 | 已修复：顶层设置白名单；`overrides` 只能是精确版本或移除并写原因；清单与 metadata 的计数不一致时失败 | 真实输出样例 `audit-with-ignored-advisories.json`；复验中 5 项削弱策略的设置都被拦下 |
| R6 | 重要 | `eslint.config.ts` | 不属于任何元素的文件不受约束，平台入口可以借中转文件引用编辑器 | 已修复：`no-unknown-files`、`no-unknown-dependencies`；跨元素只经公开入口 | lint 自测"不能借无主文件中转绕过边界"；复验中经 `index`、`src/lib`、`stray.js` 中转都被拦下 |
| R7 | 重要 | `artifacts.ts` | 产物扫描漏报 `self.eval`、`Function(变量)`、`wss://`、协议相对地址、大小写变体、内联 Worker 等 | 已修复：审查的 21 个样例中拦下 19 个；另外 2 个是运行时拼出来的写法，不处理，由 CSP 兜底（P1 设计 §3.3，审查认可） | `artifacts.test.ts` 45 个用例 |
| R8 | 重要 | `stories.ts`、`eslint.config.ts` | 故事对照从源码文本抽标题，把注释掉的、跳过的用例算作覆盖；故事用例可以跳过 | 已修复：改用 `vitest list` 与 `playwright test --list` 的真实列举；lint 禁止 `skip`、`todo`、`fixme` | 真实输出样例 `playwright/list-with-skips.json`；复验中 skip、todo、skipIf、fixme 都不计入 |
| R9 | 重要 | `lint-rules.test.ts` | lint 自测不足以证明【自动】规则生效 | 已修复：18 个用例，覆盖规范 §1.2、§2.1 的每条【自动】规则 | 自测全绿 |
| R10 | 一般 | `eslint.config.ts` | 受限导入管不住动态导入 | 已修复：`no-restricted-syntax`；动态导入的路径必须是字面量 | lint 自测 |
| R11 | 一般 | `dependency-graph.ts` | 单例等检查用依赖的键名，不是真实包名（别名可以绕过） | 已修复：包名取 `from` | 真实输出样例 `ls-alias.json` |
| R12 | 一般 | `dependency-graph.ts` | 去重占位可能让后面完整的子树被跳过 | 已修复：占位不标记为已展开；只以占位出现的子树判为依赖树不完整 | 真实输出样例 `ls-deduped.json`；复验中大依赖图没有误报 |
| R13 | 一般 | `strip-ai-trailers.ts` | 误删名叫 Claude、Devin 的人类合作者，漏掉 `Assisted-by`、`Signed-off-by` 等 AI 署名；正文里举例的行也被删 | 已修复：只处理末尾署名段，按 AI 专用邮箱或"整个名字就是 AI 产品名"判断 | 复验中人类合作者全部保留，AI 署名全部去掉 |
| R14 | 一般 | `run.ts`、`repo.ts` | 外部数据用 `as` 断言；没有保存真实输出；`run.ts` 覆盖率低 | 已修复：zod 结构校验；真实输出样例；产物与漏洞门禁的装配可注入并有测试 | 单元测试 265 个、集成测试 3 个 |
| R15 | 一般 | 文档 | P1 设计写"`tsc -b` 检查全部"；"只经公开入口"的机制没有落地 | 已修复：文档改为实际做法；公开入口用 `fileInternalPath` 落地，功能模块之间经对方入口 | ADR-003 与 P1 设计已同步 |
| S1 | 建议 | `playwright.config.ts` | E2E 复用已在运行的预览服务，多个 worktree 并行时可能测到别人的构建 | 已修复：不复用 | — |
| S2 | 建议 | `ci.yml` | 报告只在失败时上传，重试后通过的用例查不到报告 | 已修复：总是上传 | — |
| S3 | 建议 | `license-bundle.ts` | 第三方许可清单没有采用许可例外清单 | 已修复：与依赖许可检查同一口径 | 单元测试 |
| S4 | 建议 | 许可插件 | 补齐的许可正文是否随部署包分发 | 已修复：`THIRD-PARTY-LICENSES.md` 含补齐的正文，随产物分发 | 插件测试 |
| S5 | 建议 | 各测试包 | vitest、@types/node 没有在各自的 package.json 里声明 | 已修复 | — |
| S6 | 建议 | `eslint.config.ts` | 残留的 `tools/.gates/**` 忽略项 | 已修复 | — |
| S7 | 建议 | P2 | `rewriteRelativeImportExtensions` 与 NestJS 12 的构建链是否兼容 | 列入 P2 开头的验证（交接单） | — |
| S8 | 建议 | 文档 | 修订了已审批的 M1 总设计与已定稿的规范，要让需求方知悉 | 交接单与 M1 收尾汇报中单独列出 | — |
| N1 | 重要 | `licenses.ts` | 第一轮修复引入：本机没装的平台专属可选依赖被误报"许可清单里找不到" | 已修复：本机路径不存在的实例跳过，以 CI（Linux x64）为准 | 真实输出样例 `ls-platform-optional.json`（sharp，28 个实例、本机装了 6 个），只报 LGPL 的 libvips |
| N2 | 一般 | `strip-ai-trailers.ts` | 第一轮修复的回归：`git commit -v` 时 AI 署名去不掉 | 已修复：按剪刀线切开，只处理之前的部分 | 单元测试；真实仓库加钩子 `git commit -v` 验证 |
| N3 | 一般 | `pnpm-config.ts` | 冷却期豁免可以写通配，一行就能关掉整个冷却期 | 已修复：只接受"包名@精确版本" | 单元测试 |
| N4 | 一般 | 门禁 | 没有检查 pnpmfile（安装时执行任意代码） | 已修复：出现即失败 | 单元测试 |
| N5 | 一般 | `artifacts.ts` | 产物里的 `.json`、`.md` 按扩展名一律放行 | 已修复：`.json` 扫描内容，只放行三个清单文件 | 单元测试与装配测试 |
| N6 | 建议 | `lint-rules.test.ts` | 探针文件删掉后留下空目录 | 已修复：删除新建的最外层目录 | 运行后 `apps/web/src/` 下没有残留 |
| N7 | 建议 | 许可插件 | 只收第一个许可文件，双许可的包漏一份 | 已修复：排序后合并全部 | 单元测试 |
| N8 | 建议 | 00 号计划书 | 审批后的修订原地追加到 r4 | 已修复：另记为 r5，r4 恢复为审批时的内容 | — |
| N9 | 一般 | `lint-rules.test.ts` | 第二轮修复引入：清理时递归删除新建的 `apps/web/src/editor/`，会连带删掉同一时间别人写进去的文件；同一 worktree 里并发的两次测试会互相删掉探针 | 已修复：先删探针文件，再由深到浅对新建的目录做不递归的删除，目录不空就放过 | 复现审查者的实验：测试运行期间写入 `apps/web/src/editor/` 的文件在测试结束后仍在；lint 自测 18 个通过 |
| N10 | 建议 | `commit-msg.ts` | 设置了 `core.commentChar` 或 `core.commentString` 时去不掉 AI 署名 | 已修复：从 git 配置读取注释符 | 单元测试（`;`、`//`）；真实仓库设 `core.commentChar=;` 后 `git commit -v` 验证 |
| N11 | 建议 | 交接 | "平台专属包以 CI 为准"的前提是 CI 与生产镜像同一平台 | 写进交接单给 P5 的备注：镜像只构建 linux/amd64，或在镜像构建里再执行 licenses 门禁 | — |

复验中审查者误改了 worktree 的 `node_modules/.bin` 垫片（副本的 node_modules 软链接回 worktree，pnpm 顺着链接改写）。已删除全部 node_modules，用 `pnpm install --frozen-lockfile` 重新生成；受版本控制的文件没有改动。

## 结论与遗留

- 结论：修复后通过。三轮审查共 34 项（阻断 2、重要 8、一般 11、建议 13），除了登记为"不处理"、由 CSP 兜底的两种运行时写法（R7），全部修复。
- 遗留：
  - Edge 只在 CI 上跑（需求方确认，规范 §8.2 已修订），以合并后第一次 CI 的结果为准；
  - CI 在合并后第一次运行时确认 Linux 上的浏览器安装与全部步骤。
