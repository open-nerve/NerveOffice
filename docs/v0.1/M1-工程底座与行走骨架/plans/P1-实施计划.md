# P1 工程底座：实施计划

> 状态：进行中｜日期：2026-09-26｜所属：M1-P1｜依据：`01-P1-工程底座.md`

## 目标

按 P1 设计 §6 的五个 Step，建立 workspace、工具链、三层测试框架、A01 检查、故事对照、本机门禁与 CI。

## 涉及文件

- 根目录：`package.json`、`.node-version`、`pnpm-workspace.yaml`、`tsconfig.base.json`、`tsconfig.json`、`eslint.config.ts`、`vitest.config.ts`、`lefthook.yml`、`.gitignore`
- `packages/contracts/`、`apps/web/`、`tools/`、`tests/integration/`、`tests/e2e/`、`tests/stories.json`
- `deploy/dev/compose.yaml`
- `.github/workflows/ci.yml`
- 文档：`docs/adr/`、`docs/architecture/overview.md`、规范、M1 总设计、`CLAUDE.md`

## 任务

### S1 工作区与工具链

- [x] 1. 根 `package.json`（`packageManager`、`engines`、脚本）、`.node-version`、`pnpm-workspace.yaml`（发布冷却期、`trustPolicy`、`allowBuilds`、`engineStrict`、精确版本）、`.gitignore`
- [x] 2. `tsconfig.base.json` 与根 `tsconfig.json`（只放引用）
- [x] 3. contracts：`package.json`（带 `@nerve-office/source` 条件的导出）、构建用的 `tsconfig`；先写错误响应结构的测试，再实现
- [x] 4. web：`package.json`、`vite.config.ts`（`build.license`、清单、构建目标）、`index.html`、平台入口与占位组件；先写组件测试
- [x] 5. tools：`package.json`、`tsconfig`；先写提交说明清理的测试，再实现（commit-msg 钩子用）
- [x] 6. `eslint.config.ts`：@antfu/eslint-config、规范 §2 的规则、模块边界、循环依赖、受限导入、测试文件禁止 `.only`
- [x] 7. `vitest.config.ts`：`unit`、`unit-web` 两个项目，覆盖率下限
- [x] 8. lefthook 与 lint-staged；`prepare` 脚本；逐个验证三个钩子
- [x] 9. lint、类型检查、单元测试全绿后提交

### S2 集成与 E2E 框架、开发数据库

- [x] 1. `deploy/dev/compose.yaml`：`postgres:18.6-alpine` 按摘要锁定，`builtin` + `C.UTF-8`，`127.0.0.1:54318`；`db:up`、`db:down`
- [x] 2. tests/integration：`integration` 项目的配置；全局准备步骤（连不上数据库时提示先执行 `pnpm db:up`）；先写数据库版本与排序规则的测试
- [x] 3. tests/e2e：`playwright.config.ts`（四个项目、`forbidOnly`、重试、被测服务）；框架冒烟用例
- [x] 4. 安装 Playwright 的 Chromium 与 WebKit；确认本机 Chrome；Edge 只在 CI 上跑
- [x] 5. 集成测试与本机已有浏览器上的 E2E 通过后提交

### S3 A01 的依赖与产物检查、故事对照

- [x] 1. 核实 pnpm 12 的 `ls --prod --json`、`licenses list --json`、`audit --json` 的输出格式，保存成样例
- [x] 2. `tools/src/gates/`：精确版本、包管理配置、依赖图（Univer 版本、Pro、单例）、许可、产物扫描、第三方许可清单、漏洞。每项先写合规与违规样例的测试，再实现规则
- [x] 3. `tests/stories.json` 与 `tools/src/stories/`：先写测试，再实现对照检查；US-M1-11 改为 `active`
- [x] 4. `gate:*` 命令，对仓库现状运行并通过
- [x] 5. 提交

### S4 本机门禁与 CI

- [x] 1. `tools/src/verify/`：步骤编排、`--fast`、`--ci`、结果汇总；先写编排逻辑的测试
- [x] 2. `.github/workflows/ci.yml`：各 Action 按 SHA 锁定、PostgreSQL 服务容器、四个浏览器、`verify`、漏洞扫描、失败时上传报告、每周定时
- [ ] 3. 本机 `pnpm verify` 全绿（本机三个浏览器）
- [x] 4. 提交

### S5 文档与收尾

- [x] 1. ADR：版本基线；仓库结构与模块边界。架构总览初版
- [x] 2. 修订规范 §1.1、§2.2 与 M1 总设计 §2.2、§6.1，写修订记录；`CLAUDE.md` 的常用命令
- [x] 3. 独立审查 → 修复 → 复验，写 `reviews/P1-审查报告.md`
- [ ] 4. `handoffs/P1-交接单.md`；复核延期登记
- [ ] 5. 以 `--no-ff` 合并，推送 main，确认 CI 全绿

## 验证命令

- `pnpm install --frozen-lockfile`
- `pnpm lint`、`pnpm typecheck`、`pnpm test`
- `pnpm db:up && pnpm test:integration`
- `pnpm test:e2e`
- `pnpm gate:pins`、`pnpm gate:config`、`pnpm gate:deps`、`pnpm gate:artifacts`、`pnpm gate:stories`、`pnpm gate:audit`
- `pnpm verify`

## 完成标准

P1 设计 §8 的完成定义全部满足。
