# v0.1 延期事项登记

> 文档编号：02｜所属版本：v0.1

这里登记所有被推迟处理的需求与问题，是延期事项的**唯一来源**。各 Phase 交接单中提到的延期项，都必须先登记在这里；每个 M 结束时复核一次。

**规则**

- 只能延期功能，不能延期质量门槛。
- 触及 00 号计划书的范围或上线门槛（A01–A20）的延期，必须走范围变更，并更新 00 号计划书的修订记录。
- "目标"一栏只能填两类：v0.1 中的某个 M，或者 v0.2。
- 状态分为：待处理、已排期、已完成、已取消（需写明原因）。

| 编号 | 登记日期 | 来源 | 事项 | 原因 | 目标 | 触发条件或前提 | 状态 |
|---|---|---|---|---|---|---|---|
| DEF-001 | 2026-09-24 | M0-P1（V02） | 在 Microsoft Edge 上补测 CSP 与编辑冒烟：用 Playwright 的 `msedge` 通道重跑 `spikes/m0/e2e/v02-csp.spec.ts` 与 `v02-csp-probe.spec.ts` | 本机未安装 Edge。真实 Safari 已在 P1 用页面自检补测，本项只剩 Edge；Edge 与 Chrome 同为 Chromium 内核，结论可以类推 | M1 | 随 US-M1-09（CSP 与编辑器在策略下工作）在 CI 的 Edge 上通过后关闭。M1-P1 已把 Edge 加入 CI 的浏览器矩阵，框架冒烟在 Edge 上通过（run 36235749514），但不含 CSP 与编辑 | 已排期 |
| DEF-002 | 2026-09-24 | M0-P2（V03） | 浮动图片（默认 `Position` 锚点）所跨的行因自动行高变高后（换行文字、放大字号、单元格图片），SDK 只在内存里保留尺寸、不回写终点锚点；之后切换工作表或重新打开时按旧锚点重算，图片被拉伸，下一次保存就把拉伸后的尺寸写进快照。修复：优先向上游报告并以补丁（`pnpm patch`）修正刷新路径：`_refreshDrawingTransform` 重算终点锚点，并让公共的 `refreshTransform`（`packages/drawing`，只复制 `transform` 等字段）写回锚点，或在表格侧另行写回，规模待 M5 按源码确认；也可以用平台插件回写锚点。用 `e2e/v03-drawing-autoheight.spec.ts` 验证（修复后把缺陷特征断言翻转为"尺寸不变"） | SDK 1.0.0 缺陷（`sheet-drawing-transform-affected.controller.ts` 的刷新路径）。**关联上线门槛 A02**：若改为"已知限制"，必须走范围变更并经需求方审批 | M5 | M5 验收表格图片能力时 | 待处理 |
| DEF-003 | 2026-09-24 | M0-P3（V06–V09） | 在真实 Safari 上复核 P3 的编辑语义：变更检测中空闲任务的时序（延迟的自动行高、大表懒执行）、公式 Worker 的写回时序与捕获时机、阅读模式的入口拦截与界面切换。可以沿用 P1 的页面自检方式，或在 M1 建立浏览器矩阵后直接跑 `spikes/m0/e2e/v06-*.spec.ts`、`v07-*.spec.ts`、`v09-read-mode.spec.ts` 的对应部分。（M0 结束评审把原来的一项拆成两项：编辑语义随变更检测、捕获时机与阅读模式放在 M3；性能部分见 DEF-012） | P3 的交互实验以 Playwright 的 WebKit 26.6（无头）代表 Safari；空闲回调、Worker 调度与计时精度在真实 Safari 上可能不同，而本 Phase 的规则（捕获时机、懒执行）依赖这些时序 | M3 | M3 实现变更检测、捕获时机与阅读模式之前 | 待处理 |
| DEF-004 | 2026-09-25 | M0-P4（V11、V12） | 在真实剪贴板、真实文件拖放与真实 Safari 下复核 P4 的图片路径：浏览器"复制图片"后粘贴到表格与文字文档、从 Word 或网页复制带图片的内容粘贴到文字文档、从 Excel 复制粘贴到表格、右键菜单粘贴（异步 Clipboard API）、GIF 文件、表格复制补救在各浏览器剪贴板授权下的行为（尤其 Safari）、把图片文件拖到编辑器上（确认浏览器是否直接打开文件、宿主拦截 `drop` 后能否插入）、真实 Safari 的读取与 CSP。可以用 P1 的页面自检方式，或在 M1 建立浏览器矩阵后跑 `spikes/m0/e2e/v11-*.spec.ts`、`v12-*.spec.ts` 的对应部分 | P4 的粘贴用合成的 `paste` 事件（与快捷键粘贴走同一条路径），拖放只能派发合成事件，合成事件不会触发浏览器打开文件的默认行为；真实 Safari 没有覆盖 | M5 | M5 实现资源服务与宿主的拖放处理之前 | 待处理 |
| DEF-005 | 2026-09-25 | M0-P5（V13） | 在 Windows 上核对文字文档的中文输入：微软拼音、微软五笔（及常用的第三方输入法）在 Chrome 与 Edge 中的组合事件顺序、撤销重做、候选框位置、段落数与光标；可以用输入法记录页（`spikes/m0/scripts/ime-real.ts`、页面参数 `imelog=1`）记录事件后回放 | M0 总设计 §2：没有 Windows 环境时，Windows 输入法登记为 M6 开始前的补充验证项。macOS 的真实输入法核对见 P5 报告附录 A（M0 结束评审前完成） | M6 | M6 开始前 | 待处理 |
| DEF-006 | 2026-09-25 | M0-P5（V13） | 用真实应用与真实剪贴板复核文字文档的粘贴：Word（Windows、macOS）、WPS、Google Docs、飞书与常见网页，在 Chrome 与 Safari 中复制后粘贴，对照 P5 的剪贴板样本结论（`spikes/m0/fixtures/paste/`、P5 报告 §4.3）；据此决定平台侧的 HTML 规范（`<br>`、Google Docs 外层 `<b>`、`<em>`、`<h6>`、Word 与 WPS 的标题样式、列表标记）哪些实现、哪些提交上游，并补全产品内的已知限制 | P5 的剪贴板样本按真实结构编写，不是真实截取；合成粘贴不经过浏览器与系统剪贴板的转换 | M6 | M6 实现文字文档模块的粘贴之前 | 待处理 |
| DEF-007 | 2026-09-25 | M0-P5（V13） | 重新评估文字文档的排版 Worker（`UniverDocsLayoutWorkerPlugin`）是否默认启用。先修复 Worker 模式下的 Tab 缺陷：在表格单元格里键入后立即按 Tab，Tab 被吞掉，后面的字写进同一格；等 100–200 ms 再按就正常，主线程模式没有这个问题（P5 报告 §5.3 第 4 条）。修复途径：优先向上游报告；平台侧可以在排版结果发布之前，把 Tab 等导航键排到发布之后再执行。启用前还要：<br>• 补上三个浏览器的表格导航用例（键入后立即按 Tab、Shift+Tab、方向键、回车）；<br>• 在 `worker=1` 下回归 V06 的文字文档动作与 V13 能力矩阵；<br>• 按"画出来"的口径复测 `doc-20k-full` 的键入与输入法响应 | 约 2 万字的样本上（三个浏览器），Worker 模式把快速键入时主线程的最长阻塞从约 81–123 ms 降到 25–51 ms，但画出来的延迟（p50）只从约 94–110 ms 降到约 64–71 ms，而且有上面的缺陷（Chromium 与 Chrome 上复现）。v0.1 先用主线程排版（插件档案 v1 §2） | M6 | M6 验收文字文档的性能之前 | 待处理 |
| DEF-008 | 2026-09-25 | M0-P5（V13） | 在真实 Safari 上复核 P5 的文字文档能力：<br>• 能力矩阵各项的入口、撤销重做与保存重开；<br>• `/` 键的两条路径；<br>• 粘贴清洗与"仅保留文本"；<br>• 浮动图片的拖动与环绕；<br>• 捕获时机的组合输入感知。<br>可以用 P1 的页面自检方式，也可以在 M1 建立浏览器矩阵后，跑 `spikes/m0/e2e/v13-*.spec.ts` 的对应部分。真实系统输入法的核对不在本项，见 DEF-013 | P5 以 Playwright 的 WebKit 26.6（无头）代表 Safari；真实 Safari 的输入法、剪贴板、指针事件与计时可能不同 | M6 | M6 实现文字文档模块之前 | 待处理 |
| DEF-009 | 2026-09-25 | M0-P6（V14） | 在真实浏览器上复核本机发件箱依赖的存储行为：<br>• 真实 Chrome、Edge、Safari 的 `navigator.storage.persist()` 授予条件与配额，写满时的错误与提示；<br>• Safari 的存储清理策略（长期不访问的站点数据会被清除）对"未同步修改"的影响；<br>• 断电与系统崩溃时，IndexedDB 默认持久性与 `durability: 'strict'` 的差别；<br>• 真实 Safari 的 Web Locks 与 IndexedDB 行为。<br>可以在 M1 建立浏览器矩阵后跑 `spikes/m0/e2e/v14-*.spec.ts`，断电部分按浏览器文档与人工断电实验确认 | P6 用无头浏览器与 SIGKILL 验证：无头浏览器不授予持久化存储，配额也与真实浏览器不同；Playwright 不能模拟断电与系统崩溃 | M4 | M4 实现本机发件箱之前 | 待处理 |
| DEF-010 | 2026-09-25 | M0-P6（V15） | mutation 增量日志（00 号计划书 §7.5 的"可选增强"）：两次快照之间逐条记录 mutation，崩溃后"快照 + 日志"恢复，把丢失窗口从约 1–3 秒降到约 0.1 秒。纳入时的要求（P6 报告 §3.3、§3.4）：<br>• 适配层封装协同钩子 `onMutationExecutedForCollab`（不是公开的稳定 API）；<br>• 按补全清单实现：样式附带记录（`set-range-values`、`set.numfmt`、`remove.numfmt`）、批注 id 回填（`update-note`）；<br>• 日志与快照一样加密、按用户隔离、随快照截断；<br>• 随机编辑加撤销重做的重放测试；升级 SDK 时重新排查全部 mutation 并先跑 V15 回归 | V15 验证可行，但依赖不公开的钩子，每次升级 SDK 都要重新排查；重放出错会悄悄写坏文档。快照已满足 §7.8 的丢失边界。M0 结束评审（2026-09-26）确认 v0.1 不纳入（范围变更 SC-2，00 号计划书 r3） | v0.2 | v0.2 规划时 | 待处理 |
| DEF-011 | 2026-09-26 | M0-P6（V14） | 在真实 Safari 上复核并处理发件箱 Worker 的偶发停顿：WebKit 上发件箱 Worker 空闲之后，第一次异步操作（`crypto.subtle.digest`、`CompressionStream`）偶尔多等约 1 秒；5 MiB 表格约八分之一的捕获因此要约 2.2 秒，超过 00 号计划书 §12.2 的 2 秒。验证工程里 Worker 保持 100 ms 的空定时器后，40 次都没有出现（P6 报告 §2.3 第 11 条）。复核可以用 `spikes/m0/e2e/v14-pipeline.spec.ts` 的端到端（`M0_P6_E2E_RUNS=20`，对照页面参数 `outboxkeepalive=1`），或在 M4 的 E2E 里覆盖；空定时器在 Safari 上无效时，再评估 WebKit 上改用主线程放置（WebKit 的 gzip 在主线程上约 62 ms） | P6 只在 Playwright 的 WebKit 26.6（无头）上观察到，没有查到源码层面的原因；Safari 是另一个构建，可能没有这个现象，也可能更严重 | M4 | M4 实现发件箱 Worker 时 | 待处理 |
| DEF-012 | 2026-09-26 | M0-P3（V08、V10） | 在真实 Safari 上复核 P3 的捕获成本与性能基线（首屏、第一次输入、编辑响应、公式计算与冻结、捕获时的主线程阻塞）：可以沿用 P1 的页面自检方式，或在 M1 建立浏览器矩阵后跑 `spikes/m0/e2e/v08-capture.spec.ts`、`v10-perf.spec.ts` 的对应部分（性能用例单独运行）。由 DEF-003 拆出 | P3 以 Playwright 的 WebKit 26.6（无头）代表 Safari；计时精度与 Worker 调度在真实 Safari 上可能不同 | M4 | M4 实现本地优先管道之前 | 待处理 |
| DEF-013 | 2026-09-26 | M0-P5（V13） | macOS 真实输入法的人工核对（P5 报告附录 A）：在真实的 Safari 与 Chrome 里用系统输入法（简体拼音，可选五笔）操作记录页，确认 Safari 的提交顺序、SDK 默认与平台策略下撤销重做的正文、中英文标点下 `/` 的事件、Esc 与切换中英文与焦点切走时的组合处理，以及候选框的位置；用记录回放，结果补进 P5 报告 §3 与能力矩阵，并据此决定"候选框位置偏差"是否写进 00 号计划书 §4.5（SC-4 第 3 条）。做法：`spikes/m0` 下 `npx vite build`，再 `node scripts/ime-real.ts --open` | 需要人在真实浏览器里操作；原定在 M0 结束评审前完成，M0 结束评审（2026-09-26）决定改到 M6 开始前：它只影响文字文档模块（M6）的输入法处理与已知限制，不影响 M1–M5 | M6 | M6 开始前 | 待处理 |
