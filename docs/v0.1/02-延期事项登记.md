# v0.1 延期事项登记

> 文档编号：02｜所属版本：v0.1

这里登记所有被推迟处理的需求与问题，是延期事项的**唯一来源**。各 Phase 交接单中提到的延期项，都必须先登记在这里；每个 M 结束时复核一次。

**规则**

- 只能延期功能，不能延期质量门槛。
- 触及 00 号计划书的范围或上线门槛（A01–A20）的延期，必须走范围变更，并更新 00 号计划书的修订记录。
- "目标"一栏只能填两类：v0.1 中的某个 M，或者 v0.2。
- 状态分为：待处理、已排期、已完成、已取消（需写明原因）。

| 编号 | 登记日期 | 来源 | 事项 | 原因 | 目标 | 触发条件或前提 | 状态 |
|---|---|---|---|---|---|---|---|
| DEF-001 | 2026-09-24 | M0-P1（V02） | 在 Microsoft Edge 上补测 CSP 与编辑冒烟：用 Playwright 的 `msedge` 通道重跑 `spikes/m0/e2e/v02-csp.spec.ts` 与 `v02-csp-probe.spec.ts` | 本机未安装 Edge。真实 Safari 已在 P1 用页面自检补测，本项只剩 Edge；Edge 与 Chrome 同为 Chromium 内核，结论可以类推 | M1 | M1 建立 E2E 浏览器矩阵时 | 待处理 |
| DEF-002 | 2026-09-24 | M0-P2（V03） | 浮动图片（默认 `Position` 锚点）所跨的行因自动行高变高后（换行文字、放大字号、单元格图片），SDK 只在内存里保留尺寸、不回写终点锚点；之后切换工作表或重新打开时按旧锚点重算，图片被拉伸，下一次保存就把拉伸后的尺寸写进快照。修复：优先向上游报告并以补丁（`pnpm patch`）修正刷新路径：`_refreshDrawingTransform` 重算终点锚点，并让公共的 `refreshTransform`（`packages/drawing`，只复制 `transform` 等字段）写回锚点，或在表格侧另行写回，规模待 M5 按源码确认；也可以用平台插件回写锚点。用 `e2e/v03-drawing-autoheight.spec.ts` 验证（修复后把缺陷特征断言翻转为"尺寸不变"） | SDK 1.0.0 缺陷（`sheet-drawing-transform-affected.controller.ts` 的刷新路径）。**关联上线门槛 A02**：若改为"已知限制"，必须走范围变更并经需求方审批 | M5 | M5 验收表格图片能力时 | 待处理 |
| DEF-003 | 2026-09-24 | M0-P3（V06–V10） | 在真实 Safari 上复核 P3 的编辑语义与性能：变更检测中空闲任务的时序（延迟的自动行高、大表懒执行）、公式 Worker 的写回时序与捕获时机、阅读模式的入口拦截与界面切换、捕获成本与性能基线。可以沿用 P1 的页面自检方式，或在 M1 建立浏览器矩阵后直接跑 `spikes/m0/e2e/v06-*.spec.ts`、`v07-*.spec.ts`、`v09-read-mode.spec.ts`、`v08-capture.spec.ts`、`v10-perf.spec.ts` 的对应部分 | P3 的交互实验以 Playwright 的 WebKit 26.6（无头）代表 Safari；空闲回调、Worker 调度与计时精度在真实 Safari 上可能不同，而本 Phase 的规则（捕获时机、懒执行）依赖这些时序 | M4 | M4 实现本地优先管道与编辑器适配层之前 | 待处理 |
