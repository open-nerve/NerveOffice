# 团队在线文档平台：项目计划书

**文档编号：00｜文档修订：r2｜适用产品版本：v0.1（MVP）｜日期：2026-09-23｜状态：方向与里程碑编排已评审，待 M0 技术验证**

**技术方向：Node.js / TypeScript + PostgreSQL + Univer 开源 SDK（1.0 线）**

> 本期建设一个自部署、单租户的团队在线表格与文字文档平台。只采用可核实的开源能力，不引入 Univer Pro，不实现同一文档的多人实时共编，不接入 Agent。
>
> 多人使用采用"空间内默认可见、单文档单写者、编辑权可交接、不同文档并行编辑"。保存采用"本地优先"管道：修改先写入本机加密发件箱，再同步到服务器，断网期间已输入的内容不丢失。
>
> 本文档在初稿基础上，结合对 Univer 源码（`refer/univer`，dev 分支 d31d624，1.0.0-rc.0）的逐项核查与评审讨论修订而成。文中的实现方式、参数与指标仍是建议方案，须经 M0 验证。

**修订记录**

| 修订 | 日期 | 说明 |
|---|---|---|
| r1 | 2026-09-23 | 基于初稿，结合 Univer 源码核查与评审决策 D1–D8 修订 |
| r2 | 2026-09-23 | 里程碑重排为 M0–M8：新增工程底座与行走骨架，保存协议与离线恢复分开，文字文档独立成 M；新增上线门槛映射与文档交付流程（§13）；1.0 正式版门槛改到 M5 开始时（§3.2） |

---

## 0. 本版相对初稿的主要变化

| 主题 | 初稿 | 本版 | 依据 |
|---|---|---|---|
| SDK 版本线 | v0.25.2 | 1.0 线：M0 用 1.0.0-rc.x 精确锁版本，正式版发布后升级并冻结 | 0.25 已是维护线且缺包；1.0 正式版大概率早于上线（§3） |
| 租户与空间 | 工作空间 = 租户边界 | 部署 = 唯一租户；空间 = 组织与默认可见范围（个人空间 + 团队空间），可跨空间单独分享 | 评审决策 D3（§5） |
| 编辑权 | 申请、续租、释放、过期、强制结束 | 增加显式进入编辑、空闲释放、请求编辑与自动交出、本人接管、强制接管、另存为副本 | 评审决策 D4（§6） |
| 保存 | 在线保存，可选 IndexedDB 草稿 | 本地优先管道：先写本机加密发件箱再同步；断网不丢，崩溃仅丢数秒内输入 | 评审决策 D8（§7） |
| 快照存储 | PostgreSQL `jsonb` | `bytea` 存 gzip 压缩的原始 JSON | `jsonb` 拒收 `\u0000`、重排键、写放大（§8.1） |
| 插件资源 | 保存时不得丢弃资源字段 | 固定"插件档案"，服务端校验"资源不缩水" | 源码核实：未注册插件的资源在保存时被静默丢弃（§8.2） |
| 公式结果 | 未涉及 | 捕获快照前等待公式结果写回 | 源码核实：公式结果以 `onlyLocal` 回写，默认打开时不重算（§7.3） |
| 文档复制 | 需处理新标识与内部引用 | 复制时不改 Univer 内部 `unitId`；平台 `documentId` 与 `unitId` 解耦 | 源码核实：`unitId` 内嵌于图片、评论等对象（§8.3） |
| 前端只读 | 切换只读状态 | 只读仅作体验层；逐表只读、拦截撤销重做、隐藏保护菜单；取得编辑权后从服务端重载 | 源码核实：表格只读存在多处缺口（§6.5） |
| 外部请求 | 链接协议白名单 | 增加严格 CSP、粘贴外链图片清洗、限制 `IMAGE()` 公式 | 源码核实：Docs 粘贴保留外链图片；`IMAGE()` 可请求任意 URL（§11.3） |
| Docs 范围 | 基础文本、格式、列表、链接、简单图片 | 增加标题样式、复选列表、基础表格、图片环绕、查找替换；固定无分页版式 | 评审决策 D5（§4.3） |
| 导入导出 | 不做 | 表格 CSV 导入导出 | 评审决策 D6（§4.1） |
| 历史版本 | 仅运维使用恢复检查点 | 编辑者可查看小时级历史版本并"恢复为副本" | 评审决策 D7（§8.6） |
| 保存请求表 | `document_save_requests` | `document_revisions`：每次保存一行元数据，兼顾幂等、审计与历史 | 精简（§8.4） |
| 工程结构 | 5 个 packages | 起步只建 `apps/web`、`apps/api`、`packages/contracts` | 按需拆分（§9.4） |
| 里程碑 | M0–M5 | M0–M8：新增工程底座与行走骨架，保存协议与离线恢复分开，文字文档独立成 M，最后只做上线验证与演练 | 按颗粒度、独立性、可验证性重排（§13） |

## 1. 决策摘要与默认前提

### 1.1 需求方已明确的决定

| 项目 | 决定 |
|---|---|
| 产品形态 | 独立的团队在线文档平台，而非外部云文档的跳转入口 |
| 部署形态 | 自部署、单租户：一套部署服务一个组织，组织内可划分多个团队空间 |
| 商业依赖 | 依赖付费组件的功能全部排除，不以试用许可证作为生产方案 |
| 项目阶段 | 从零开始建设符合生产验收要求的 MVP |
| 后端语言 | Node.js / TypeScript |
| Agent | 不进入 MVP：不建设对话框、模型接入、Agent 调度或 MCP |
| 可定制性 | 保留自主控制产品、数据和扩展逻辑的能力 |

### 1.2 本轮评审确认的决策

| 编号 | 决策 | 结论 |
|---|---|---|
| D1 | SDK 使用方式 | 依赖 npm 发布包，定制通过自有插件与适配层实现；确需改源码时用登记过的补丁管理；`refer/univer` 仅作只读参考 |
| D2 | 版本基线 | 1.0 线（§3.2） |
| D3 | 组织与空间 | 个人空间 + 团队空间；空间决定默认可见范围，不是隔离墙；团队空间仅系统管理员可创建；系统管理员默认看不到文档内容（§5） |
| D4 | 编辑权交接 | 显式进入编辑、空闲释放、请求编辑、本人接管、强制接管；被中断方的未同步内容另存为副本（§6） |
| D5 | Docs 首发范围 | 在初稿基础上适度扩大，默认使用无分页的网页式版式（§4.3） |
| D6 | 数据导入导出 | 表格支持 CSV 导入导出；xlsx 数据级导入导出列为后续候选 |
| D7 | 历史版本 | 编辑者可查看小时级历史版本（保留 7 天），可预览、恢复为副本；查看者看不到（§8.6） |
| D8 | 离线与崩溃防护 | 本地优先管道 + 本机加密；mutation 增量日志作为 M0 验证项（§7） |

### 1.3 本稿采用的默认值

以下是为形成完整方案采用的默认值，没有逐项单独评审。可以在后续调整，但变更时需要同步修改验收范围。

| 事项 | 默认 |
|---|---|
| 数据库 | PostgreSQL，M0 锁定具体大版本与镜像 |
| 首发编辑类型 | 在线表格、文字文档；Slides、PDF 延后 |
| 内容格式 | 用平台信封封装的 Univer 原生快照；不承诺 Office 文件互通 |
| 服务组织 | 模块化单体，不拆微服务，不建独立的协同服务 |
| 首发终端 | 桌面 Web、简体中文；移动端编辑与 Electron 不进入 MVP |
| 用户入口 | 由系统管理员邀请注册；关闭公网自由注册与匿名分享 |
| 首个管理员 | 部署后通过命令行初始化 |
| 部署方式 | 单应用实例 + PostgreSQL + 持久化资源目录 + 独立故障域的备份目的地 |
| 回收站 | 保留 30 天后彻底删除 |

### 1.4 术语

| 术语 | 含义 |
|---|---|
| 登录会话 | 用户登录后的服务端 Session，由 Cookie 标识 |
| 编辑租约 | 某份文档当前唯一可提交修改的编辑权，有有效期，靠心跳续租 |
| 租约代次（`writeEpoch`） | 文档上单调递增的计数器，每产生一代新的编辑租约就加一，用于拒绝过期会话的写入 |
| 修订号（`revision`） | 文档内容每成功保存一次就加一 |
| 快照 | Univer 原生的文档完整数据（工作簿或文字文档），含插件资源 |
| 插件档案 | 某种文档类型固定使用的插件列表、配置与适用的 SDK 版本（§8.2） |
| 本机发件箱 | 浏览器 IndexedDB 中尚未同步到服务器的快照记录（§7.5） |
| SDK 版本 / 平台格式版本 | 前者是 Univer 包的版本；后者是本平台快照信封的格式版本，两者独立 |

## 2. 目标、定位与非目标

### 2.1 产品目标

团队成员可以登录，进入个人空间或所在的团队空间，新建表格或文字文档，把文档分享给同事查看或编辑。遇到断网、关闭页面、重新登录或应用重启，已输入的内容仍然可以继续使用。系统管理员管理账户与空间，空间管理员管理空间成员与空间内的文档，运维人员可以检查审计记录并执行故障恢复。

MVP 的价值不在功能数量，而在一个可靠的闭环：

**创建 → 编辑 → 写入本机 → 云端确认 → 分享 → 交接编辑 → 重新打开 → 故障恢复。**

### 2.2 本期不是完整的 Office 替代品

本期不承诺上传已有的 `.xlsx`、`.docx`、`.pptx` 后可以直接编辑，也不导出这些格式。数据进出的正式通道有两个：表格的 CSV 导入导出，以及剪贴板。剪贴板只保证验收范围内的文本与表格数据，不保留外部 Office 的复杂格式。

平台备份与恢复的是自身的数据格式，这不等于 Office 文件转换。产品说明使用"在线表格""在线文字文档"，不宣传为"完整 Excel/Word 在线版"。

### 2.3 明确不做的工作

- 不实现 OT/CRDT、实时光标、逐操作同步和同文档多人同时写入，也不自研这些能力来替代商业协同组件。
- 不引入官方 Office 转换、打印、PDF 导出、图表、数据透视表、行列分组（大纲）、迷你图、形状、协同编辑历史、Pro 服务端或许可证管理。Univer 把这些列为 Pro 能力。[S1]
- SSO、对外开放平台、复杂审批、全文搜索、单元格级权限、文件夹级权限、跨文档计算、Agent 自动修改、离线冷启动（断网状态下打开应用），都不进入本期。

## 3. 上游依据与版本基线

### 3.1 已核实的上游事实

以下版本信息于 2026-09-23 查询 npm registry 得到：

| 版本线 | 标签 | 版本 | 发布日期 | 说明 |
|---|---|---|---|---|
| 0.25（维护线） | `latest` | 0.25.2 | 2026-09-17 | 在 0.25.0（05-30）、0.25.1（06-27）之后只有少量修复 |
| 1.0（主线） | `rc` | 1.0.0-rc.0 | 2026-09-10 | alpha.0（06-24）→ beta.0（08-05）→ beta.2（08-22）→ rc.0 |

0.25 线已经落后于主线：
- `@univerjs/presets` 停在 0.25.1；
- `@univerjs/docs-find-replace`、`@univerjs/docs-toc` 在 0.25 线上没有发布；
- 移除 Uniscript 与加固自定义代码执行（#7217）只做在 1.0 线上。[S2]

**许可证（源码核查）**：
- `refer/univer` 中 60 个 packages 与 17 个 presets 的 `license` 字段全部是 Apache-2.0；
- 仓库内没有任何 `@univerjs-pro/*` 依赖，也没有许可证校验或未授权水印逻辑；
- 独立发版的 `@univerjs/icons` 是 MIT，间接依赖 `@wendellhu/redi` 是 MIT，`ot-json1` 是 ISC。

官方说明 Sheets 是最成熟的产品面，Docs 与 Slides 仍在演进。按官方 API 稳定性策略，Slides 属于实验性产品面，本期不纳入。[S1][S3]

### 3.2 版本决策

- M0 使用 1.0 线当时最新的 rc 版本。所有 `@univerjs/*` 协调发布包使用完全相同的精确版本，不用 `^` 或 `~`；独立发版的包（如 `@univerjs/icons`）按包清单声明的兼容范围单独锁定。
- 1.0.0 正式版发布后，在最近的 Phase 边界升级，经回归样本验证后冻结，作为首发基线。
- **门槛**：如果 M5 开始时 1.0.0 仍未正式发布，召开评审决定是否以 rc 版本上线，并记录风险接受的结论。之所以定在 M5，是因为从 M5 起会大量使用 SDK 的各项功能。
- 生产构建禁止使用浮动的 `latest`、未经评审的候选版或远程动态加载的 SDK；锁文件纳入版本控制。
- 实现依据优先使用锁定版本对应 tag 的源码、类型声明与示例；官网文档只作概念参考。

### 3.3 开源依赖准入

为每个启用的功能记录：功能名、npm 包、版本、源码位置、许可证、传递依赖、浏览器验证结果、保存重开结果。

生产依赖图和构建产物中不允许出现 `@univerjs-pro/*`。除了检查包名前缀，还要核查实际许可证、运行时的网络行为（§11.3），以及是否需要商业运行资源。

- 采用显式的插件白名单，即插件档案（§8.2）；没有验证过的功能隐藏入口，也不注册对应插件。
- 保留 LICENSE / NOTICE，生成第三方依赖清单；授权不明的依赖默认不纳入。
- 不把上游的 examples 工程复制过来作为生产平台，只作参考。
- 不使用 presets，直接按插件模式组装。原因是插件集合必须由我们精确控制，而 preset 会带入额外的插件。

### 3.4 参考源码的管理

`refer/univer` 只用于阅读与调试，不参与构建，保持只读。它目前是 dev 分支的浅克隆（d31d624，2026-09-22），和任何 npm 发布版都不完全对应。M0 锁定版本后，要重新检出到对应的 tag；此后随基线升级同步切换。

## 4. MVP 功能范围

### 4.1 平台功能

| 模块 | MVP 目标 | 范围约束 |
|---|---|---|
| 账户 | 邀请注册、登录、退出、修改密码、由管理员发起的重置密码 | 不开放匿名注册；重置链接单次有效，并记入审计 |
| 组织与空间 | 个人空间；团队空间的创建与归档、成员与空间角色管理；全员可见空间 | 团队空间只有系统管理员能创建（§5） |
| 文档管理 | 新建、重命名、列表、按标题搜索、文件夹、移动（含跨空间）、复制、回收站、恢复 | 不做全文索引；文件夹不设独立权限 |
| 分享 | 把文档分享给指定同事（查看者或编辑者），可以跨空间；"与我共享"列表 | 不做匿名链接，不做公开发布 |
| 编辑权 | 申请编辑、显示占用者、请求编辑、空闲释放、本人接管、强制接管 | 同一文档只有一个写者；同一用户开两个标签页也按两个会话处理（§6） |
| 保存与离线 | 本地优先保存、断网继续编辑、崩溃恢复、冲突时另存为副本、保存状态显示 | 可靠性边界见 §7.8 |
| 历史版本 | 编辑者可查看小时级历史版本、只读预览、恢复为副本；空间管理员可清除 | 不做逐操作回放、差异对比、就地回滚（§8.6） |
| 数据导入导出 | 表格按工作表导出 CSV；可从 CSV 新建表格，或导入为新工作表 | 使用带 BOM 的 UTF-8；防 CSV 公式注入 [S15]；受快照容量限制；在客户端完成 |
| 图片资源 | 受控上传、授权读取、文档内引用；复制与恢复后资源完整 | 支持 PNG/JPEG/WebP/GIF，单张不超过 5 MiB；不做任意附件，不做远程抓图 |
| 产品体验 | 简体中文；加载、错误、只读、离线等状态；保存状态；无权限提示；已知限制在产品内可见 | 编辑器能力不足时，不显示用不了的按钮 |
| 运维 | 迁移、健康检查、日志、监控、审计、备份、恢复演练、发布回滚方案 | 属于上线门槛，不作为"以后再补"的增强项 |

### 4.2 在线表格

**目标能力**：
- 多工作表、单元格编辑；
- 行列的插入、删除与宽高调整；
- 常用格式与数字格式；
- 常用公式，以及工作簿内的跨表引用；
- 冻结、筛选、排序；
- 数据验证、条件格式；
- 查找替换、超链接、备注（note）；
- 浮动图片与单元格图片。

**排除**：
- 行列分组（大纲）、图表、透视表、迷你图、形状、打印，这些都属于 Pro；
- 单元格保护与工作表保护：开源实现是客户端模拟，不构成安全边界，所以隐藏入口（§6.5）；
- 线程评论（§4.4）。

不承诺所有公式都能用，也不承诺任意复杂度的文件都能兼容；以 M0 的能力矩阵和保存重开样本为准。

### 4.3 文字文档

以下能力开源 1.0 线已经提供，但须逐项通过 M0 验证：

- 正文与标题样式（标题、副标题、H1–H5）
- 字符格式：粗体、斜体、下划线、删除线、上下标、字体、字号、文字颜色、高亮
- 段落对齐
- 有序、无序、复选列表
- 基础表格。限制：不支持嵌套表格，单元格里不能插入图片
- 图片：内联，以及浮动环绕
- 超链接；`/` 快捷插入（只包含已启用的能力）；查找替换

**版式**：固定使用无分页的网页式版式。隐藏以下入口：页面模式切换、页眉页脚、分节符与分栏符、形状、内嵌公式、目录（目录作为 M0 的候选项）。

**Docs 是 MVP 目标，不是可以悄悄取消的按钮。**
- M0 必须单独验证中文输入法、复制粘贴、列表、表格、图片、保存与重开。
- 某一项能力在 M0 中验证不稳定的，隐藏它，并记为范围变更。
- 如果基础 Docs 整体达不到要求，须提交范围变更评审，例如改成"先发布仅表格版本"；不得在未说明的情况下宣称完成了全部 MVP。

### 4.4 开源但暂缓的能力

- **线程评论**：开源实现默认把评论存进快照，而查看者没有写快照的权利。后续可以利用开源提供的可替换评论数据源（`IThreadCommentDataSourceService`）和独立的评论权限点，把评论单独存到平台接口，实现"查看者也能评论"。
- 其他暂缓项：Slides、目录、分页版式与页眉页脚、xlsx 数据级导入导出、查看者本地筛选（表格的 `filterOnly` 模式）、按用户组分享。

暂缓不等于"都是商业功能"，原因在于首发范围、成熟度和额外的产品工作量。

### 4.5 在产品内可见的已知限制

- 同一文档同一时刻只有一个人可以编辑；阅读者看到的是已保存的版本，不是实时输入。
- 不支持 Office 文件的导入导出（表格支持 CSV）；不支持打印与 PDF。
- 浏览器崩溃或页面被强制关闭时，还没写入本机的最后几秒输入可能丢失；清除浏览器数据或换了设备后，未同步的修改无法恢复。
- 历史版本按小时生成，保留 7 天。
- 字体渲染依赖用户系统里的字体，不同操作系统上显示可能不同。
- 公式在浏览器中计算，结果不能作为可信的财务结算依据。
- 隐藏的行、列、工作表不是保密手段，有读取权的人可以看到全部内容。
- 单份文档有容量上限（默认解压后快照不超过 5 MiB），接近上限时会提示。
- 移动端不支持编辑。

## 5. 组织、空间与权限

### 5.1 概念模型

| 层级 | 说明 |
|---|---|
| 组织 | 就是这一套部署，也是唯一的租户。所有账户都属于组织，同事之间可以互相分享文档 |
| 个人空间 | 每个账户自动有一个。默认只有本人可见；里面的文档可以单独分享给任何同事 |
| 团队空间 | 按部门或项目划分，由系统管理员创建并指定空间管理员。成员有空间角色，对空间内所有文档默认生效。可以设为"全员可见"，即所有有效账户自动成为该空间的查看者 |
| 文件夹 | 属于某个空间，只用于整理，不设独立权限，继承空间的权限 |
| 单独分享 | 文档可以额外分享给具体的人（可跨空间），角色为查看者或编辑者 |

团队空间的文档归空间所有，所以人员离职不会留下没人管的文档；个人空间的文档归本人所有。

### 5.2 角色与有效权限

- **系统角色**：系统管理员、成员。
- **空间角色**（团队空间）：空间管理员、编辑者、查看者。个人空间的所有者，相当于该空间的空间管理员。
- **文档授权**：查看者、编辑者。

用户对文档 D（位于空间 S）的有效权限，取下面两项中较高的一个：

1. 用户在 S 中的空间角色。全员可见的空间，把所有有效账户视为查看者；
2. 用户在 D 上的单独授权。

两项都没有，就是无权访问，而且不暴露这份文档是否存在。账户被停用，所有权限全部失效。

**系统管理员默认没有内容访问权。** 系统管理员负责账户、空间和运维操作；需要看文档内容时，必须先把自己加入相应的团队空间，这个操作会记入审计。个人空间的内容对系统管理员始终不可见。

### 5.3 操作权限

| 操作 | 查看者 | 编辑者 | 空间管理员 / 个人空间所有者 | 系统管理员（未加入该空间时） |
|---|---|---|---|---|
| 阅读文档 | ✓ | ✓ | ✓ | — |
| 申请编辑、保存 | — | ✓ | ✓ | — |
| 查看历史版本、恢复为副本 | — | ✓ | ✓ | — |
| 在空间内新建文档与文件夹 | — | ✓（需空间角色） | ✓ | — |
| 删除文档 | — | 仅本人创建的 | ✓ | — |
| 分享文档、调整单独授权 | — | — | ✓ | — |
| 移出本空间（跨空间移动） | — | — | ✓（在目标空间也需有新建权限） | — |
| 强制接管编辑 | — | — | ✓ | — |
| 强制结束编辑（运维） | — | — | ✓ | ✓ |
| 清除历史版本 | — | — | ✓ | — |
| 管理空间成员与角色 | — | — | ✓（团队空间） | ✓ |
| 创建、归档团队空间，设置全员可见 | — | — | — | ✓ |
| 账户管理、邀请、重置密码 | — | — | — | ✓ |

补充说明：
- 被单独授权为"编辑者"但不是空间成员的人，按"编辑者"一列执行，但不能在该空间新建文档，因为新建需要空间角色。
- 回收站按空间划分，删除者和空间管理员可以恢复。
- 首发阶段，编辑者不能分享文档；以后可以通过设置放开。

### 5.4 生命周期规则

- **移出空间**：空间角色带来的权限立即失效，单独授权保留。该用户在这个空间里持有的编辑租约立即终止（§6.4）。
- **账户停用**：全部权限失效，登录会话与编辑租约立即终止。这个人个人空间里的文档，由系统管理员转移到其他人的个人空间或某个团队空间。转移时管理员能看到文档标题列表，但不能打开内容。
- **跨空间移动文档**：权限随之改变；移动时终止当前的编辑租约。
- **团队空间归档**：空间变为只读，成员可以阅读、不能编辑；系统管理员可以恢复。

### 5.5 可见性与防泄漏

- 用户只能看到自己的个人空间、自己所在的团队空间，以及"与我共享"。
- 通过单独分享得到的文档，只出现在"与我共享"里，不暴露它所在空间的目录结构（可以显示所属空间的名称）。
- 列表、搜索、计数、资源接口和错误信息，都不得泄漏无权文档的标题或是否存在。
- 按每个请求在服务端做授权，默认拒绝，最小权限；不能只靠编辑器界面来表达权限。[S11]

## 6. 编辑权：租约与交接

### 6.1 产品行为

打开文档一律进入阅读模式，显示用户有权读取的最新已保存版本。有编辑权的用户点击"编辑"才申请编辑租约。申请成功后，以服务端返回的当前修订号为基准进入编辑状态；如果和本地的修订号不一致，先重新加载。

文档已经有人在编辑时，其他人仍可以阅读，会看到"张三正在编辑（最后活动 3 分钟前）"，也可以请求编辑。不同文档之间互不占用。

阅读者看到的是已保存的版本。页面默认每 30 秒做一次轻量的修订号检查（用基于修订号的 ETag，没有变化就不传内容）；有新版本时提示"有更新，点击刷新"，不做整份内容的轮询。

### 6.2 租约机制

- 租约存放在 PostgreSQL 里，不以进程内的变量为准。起始参数：有效期 **90 秒**，每 **20 秒**心跳续租一次。这两个值都要经过网络波动、后台标签页、休眠恢复等测试后再调整。
- 心跳时带上客户端最后一次用户操作的时间（`lastActiveAt`），服务端据此执行空闲回收。
- 每次续租、释放、提交，都要校验租约令牌（服务端只存摘要）和租约代次 `writeEpoch`。
- 只在申请、续租、保存这些动作中使用短事务，**不在整个编辑期间持有数据库事务或行锁**。[S8]
- 租约的时间以数据库时间为准，不信任浏览器的时钟。
- 正常关闭页面时，尽力释放租约（`pagehide` 事件 + 带 `keepalive` 的请求）。但正确性依赖租约到期，而不是依赖浏览器回调一定会执行。

### 6.3 交接规则

| 规则 | 行为 | 起始参数 |
|---|---|---|
| 显式进入编辑 | 打开文档即为阅读模式；点击"编辑"才申请，不自动抢锁 | — |
| 空闲释放 | 编辑者没有键盘、鼠标操作达到阈值后，客户端先完成保存再释放，回到阅读模式；服务端按 `lastActiveAt` 兜底回收 | 客户端 10 分钟，服务端 12 分钟 |
| 请求编辑 | 他人点击"请求编辑"：持有者已空闲达到阈值，就由持有者的客户端保存后自动交出；持有者正在编辑，则弹窗让持有者选择交出或继续。持有者的客户端没有响应时，按租约到期处理 | 自动交出阈值 2 分钟 |
| 本人接管 | 同一用户在其他标签页或设备上点击"在此编辑"。同一浏览器内，由旧标签页先同步再交出；跨设备时，旧会话立即失效，它的未同步修改按冲突处理 | — |
| 强制接管 | 空间管理员或个人空间所有者可以强制接管；系统管理员可以强制结束编辑 | 记入审计 |
| 被中断方 | 旧会话中没同步的修改，一律提供"另存为副本"，不覆盖 | §7.5 |
| 异常中断提示 | 租约因过期（而不是主动释放）结束后的一段时间内，其他人申请编辑时会被提示："上一位编辑者的会话异常中断，可能还有未同步的修改" | 30 分钟 |

### 6.4 失效、抢占与撤权

- 用服务端单调递增的 `writeEpoch`（存在文档行上）区分前后两代编辑会话。旧会话即使恢复了网络，也不能凭旧代次写入。释放租约时同样校验代次，避免旧页面误释放了新会话。
- 以下操作都在**同一个事务内**递增受影响文档的 `writeEpoch` 并终止租约：撤权、移出空间、账户停用、删除或移动文档、强制接管。保存以"代次和修订号都匹配"为条件执行更新，所以"撤权提交之后的保存必定被拒绝"是由数据库保证的，而不是靠前端按钮变灰。
- 已经发送到客户端的内容收不回来；撤权保证的是后续的访问与写入被阻止。撤权后，客户端会删除该文档在本机的未同步修改（§7.6）。

### 6.5 前端只读加固

源码核查表明：Univer 的权限体系完全在客户端执行，默认的授权服务在源码注释中写明"不要用于生产"；表格的只读还存在多处缺口。因此：

- **前端只读只是体验层；服务端的租约与代次检查，才是写入的边界。** 阅读模式下的任何本地改动都不会被提交。
- **表格（阅读模式下）**：
  - 除了工作簿级的 `setEditable(false)`，还要逐个工作表设置 `readOnly` 模式，否则筛选和排序仍然可以操作；
  - 在命令执行前拦截撤销和重做，因为重做会直接重放底层 mutation，绕过权限检查；
  - 隐藏新建、删除工作表的入口，因为这两个命令没有被权限检查覆盖。
- **文字文档（阅读模式下）**：使用 `FDocument.getPermission().setReadOnly()`，它按命令前缀拦截，覆盖得比较完整；同样要拦截撤销和重做。
- 通过 UI 插件的菜单配置 `hidden`，隐藏单元格保护、工作表保护相关的菜单和右键项，避免用户误以为数据受到了保护。
- 取得编辑权后，只要本地修订号落后，或者阅读模式下检测到本地有过改动，就一律先从服务端重新加载，再进入编辑。
- 失去编辑权时切换为只读，并清空撤销栈。清空要调用 `IUndoRedoService.clearUndoRedo`，Facade 没有暴露这个接口，需要通过注入器获取。

## 7. 保存与离线防护：本地优先管道

### 7.1 设计原则

- **先写本机，再同步服务器。** 这样网络状况只影响同步什么时候完成，不影响已输入的内容还在不在。
- **写入边界由服务端的三重检查保证**：
  - 编辑租约，决定当前谁可以写；
  - 修订号 `revision`，阻止旧内容覆盖新内容；
  - 租约代次 `writeEpoch`，阻止已失效的会话写入。
- **全量快照是累积状态。** 在单写者的前提下，"本会话追上自己之前的保存"总是可以安全地直接快进；只有"别人在中间产生了新的修订"才算真正的冲突。判断这一点只需要比较修订号，不需要合并算法。
- **绝不自动覆盖。** 出现真冲突时保留双方的内容：服务器上的版本不动，本地的内容另存为副本。

### 7.2 管道

```text
用户编辑 ─► 编辑器内存（Univer 模型）
             │ 变更检测（§7.3），本地修改序号 localSeq + 1
             │ 防抖；先等待公式结果写回
             ▼
       ① 捕获快照 → 序列化 → gzip
             │ 内容哈希与上次相同则跳过
             ▼
       ② 加密后写入本机发件箱（IndexedDB） ─► 状态："已保存在本机"
             │ 在线，并且持有编辑租约
             ▼
       ③ 上传服务器（条件写入，同时只有一个请求在途）
             │ 服务端确认
             ▼
       ④ 删除本机记录（只删到已确认的序号）─► 状态："已保存到云端"
```

**起始参数**（以 M0 与压测结果为准进行调整）：

| 环节 | 参数 |
|---|---|
| 本机捕获 | 修改停止 1 秒后捕获；持续编辑时最长每 3 秒捕获一次 |
| 上传服务器 | 本机有更新的快照、且没有在途请求时上传：停顿 2 秒后上传；持续编辑时最长每 15 秒上传一次 |
| 立即上传 | 释放编辑权、交出编辑权、标签页转入后台时 |
| 大文档 | 按实测的捕获耗时和体积拉长间隔，例如捕获间隔不小于捕获耗时的 10 倍 |

同一份序列化并压缩后的数据，既用来写入本机，也用来上传，不需要重复捕获。

### 7.3 变更检测与快照捕获

以下均依据源码核查的结论：

- **只有 `MUTATION` 类型的命令会改变持久化内容。** 监听命令执行事件时，排除带有 `onlyLocal`、`fromCollab`、`fromChangeset`、`fromFormula` 标记的 mutation。官方 Facade 的示例用的也是同样的过滤方式。
- **行高自适应要单独排除。** 这个 mutation（`sheet.mutation.set-worksheet-row-auto-height`）不带 `onlyLocal` 标记，打开文档后的空闲回调里也会触发。必须把它放进自己维护的排除名单，否则会出现"一打开就判定为有修改"。排除名单随 SDK 版本维护，并纳入回归测试。
- **公式结果要等写回完成再捕获。** 公式计算结果是以 `onlyLocal` 的方式写回单元格的，而默认的计算模式 `WHEN_EMPTY` 在打开文档时不会重算已有的缓存值。所以**捕获快照前必须等待 `getFormula().onCalculationResultApplied(timeout)`**。超时的话仍然捕获，但要标记"公式待更新"，等计算结束后再追加一次捕获，避免旧结果被持久化下来。
- **缩放比例**会直接写进快照对象，但它属于 operation，不会触发变更检测，会随下一次真实修改一起保存，可以接受。
- **SDK 没有内置脏标记**：快照里的 `rev` 字段从来不会被递增。本地修改序号由平台自己维护。
- 捕获使用 `FWorkbook.save()` / `FDocument.save()`，得到的结果包含插件资源。

### 7.4 服务端提交

每次上传包含：
- 文档标识、租约令牌、`writeEpoch`、`baseRevision`；
- 请求标识 `requestId`、客户端实例标识 `clientInstanceId`、本地序号 `localSeq`；
- 客户端构建版本；
- gzip 压缩的快照。

会话令牌与租约令牌不得出现在 URL 或普通日志里。

服务端的处理顺序：

1. **解压与校验**：解压时限制解压后的体积，防止压缩炸弹；然后做结构校验（§11.2），并计算内容哈希。
2. **幂等**：如果 `document_revisions` 里已经有相同的 `requestId`，负载摘要一致就返回原来的确认结果，不一致就拒绝。这一步只要求请求者对文档仍有访问权，不要求租约依然有效。
3. **单事务写入**：按统一的加锁顺序，依次执行：
   - 校验权限；
   - 校验租约与代次；
   - 校验 `baseRevision` 等于当前修订号；
   - 校验插件资源没有缩水（§8.2）；
   - 内容哈希与当前相同时，不递增修订号，直接返回确认；
   - 否则写入内容、修订号加一、写入 `document_revisions`、更新资源引用。
4. **拦截旧客户端**：客户端构建版本低于服务端要求的最低版本，或低于这份文档记录的 SDK 版本时，拒绝保存（`CLIENT_OUTDATED`）并要求刷新页面。这是为了防止部署新版本后，还开着的旧标签页用旧 SDK 把文档写回旧格式。
5. **拒绝时返回机器可读的原因**：无权限、租约失效、文档被占用、修订号冲突、负载超限、格式不兼容、客户端过旧。其中修订号冲突的响应，要附带当前修订号及其来源（`clientInstanceId`、`localSeq`），让客户端能判断是不是"自己追自己"。

### 7.5 本机发件箱与恢复

**发件箱记录**：每个用户的每份文档最多一条。内容包括：文档标识、基准修订号、`writeEpoch`、`localSeq`、在途请求标识、客户端构建版本、加密后的快照、更新时间。

**标签页协调**：处于编辑状态的标签页，通过 Web Locks 持有这份文档的排他锁；页面崩溃或关闭后，锁会自动释放。恢复流程只有拿到锁之后才启动，避免多个标签页争抢同一份草稿。[S16]

**恢复规则**：

| 场景 | 行为 |
|---|---|
| 断网后继续编辑 | 修改持续写入本机，横幅提示"已离线，修改已保存在本机"；恢复网络后自动续租并同步 |
| 断网时间超过租约有效期，但期间没人接手 | 恢复网络后重新申请编辑权。服务器修订号等于本地的基准修订号，就直接上传最新快照（快进），用户基本感觉不到 |
| 断网期间已经有人接手并保存 | 这是真冲突。不覆盖对方，提供"另存为副本"或"放弃本地修改" |
| 上传已经发出、确认还没收到，接着断网或崩溃 | 恢复时先用原来的 `requestId` 做幂等重试。如果服务器当前的修订号正是本机早先的请求产生的，就以新修订号为基准，重发最新的快照 |
| 标签页崩溃、被误关、浏览器被杀、系统休眠后页面被回收 | 重新打开这份文档，或者从文档列表的"未同步修改"提示进入时，提示"发现 14:32 未同步的修改"。修订号没变的话，一键恢复后继续编辑 |
| 被撤权、被移出空间、账户被停用 | 删除本机的未同步修改，不允许另存为副本 |
| 退出登录 | 有未同步修改时先提示；退出后清空该用户在本机的全部数据 |
| 本机记录超过保留期限 | 下次登录时提示，然后删除 |

**另存为副本**：
- 在原文档所在的文件夹里新建一份文档（前提是用户在该空间有新建权限），否则放进用户的个人空间；
- 标题后面追加"（冲突副本 时间）"；
- 副本不继承原文档的单独授权。

**离线时禁用**：插入图片、CSV 导入、分享、移动等需要服务端参与的操作。

**可选增强（M0 验证项）**：在两次快照之间，把用户产生的每一条 mutation 追加写入本机；崩溃后用"最近一次快照 + 重放这些 mutation"来恢复，把丢失窗口降到接近零。这个方案能否成立，取决于 mutation 能否被序列化、能否确定性地重放。开源版提供了 `onMutationExecutedForCollab` 钩子，但它不属于公开的稳定 API，所以要验证通过后再纳入。

### 7.6 本机数据安全

- **只存未同步的修改**：同步确认后立即删除，本机不缓存文档正文。
- **AES-GCM 加密**：
  - 密钥按用户生成，服务端用自己的主密钥加密后保存，登录后下发给客户端；
  - 客户端只把密钥放在内存里，所以退出登录或会话失效后，本机数据就无法解读；
  - 设备丢失时，管理员可以吊销该用户的本机密钥，这个人所有的本机草稿随即作废。
- 发件箱记录按用户隔离；单条记录最长保留 14 天。
- 向浏览器申请持久化存储（`navigator.storage.persist()`），降低被自动清理的概率（浏览器可能拒绝）；配额不足时提示用户。
- 敏感部署可以在系统设置里整体关闭本机草稿。关闭后退化为"内存暂存 + 离开提示"，并在界面上明确提示风险。

### 7.7 保存状态与用户提示

界面至少要区分这几种状态：已保存到云端、同步中、已保存在本机（等待同步）、离线、编辑权已失效、版本冲突、容量超限。只有服务端确认成功后，才显示"已保存到云端"。

- 有未同步修改时离开页面，弹出浏览器的离开提示。
- 快照达到容量上限的 80% 时提示；超过上限后上传会被拒绝，界面持续提示"文档超过容量上限，最近的修改无法同步"，本机记录保留。
- 文档列表页标出当前用户有未同步修改的文档。

### 7.8 可靠性承诺与丢失边界

| 情况 | 承诺 |
|---|---|
| 服务端已确认的数据 | 不会因应用重启或客户端异常而丢失；受备份保护，恢复目标见 §12.4 |
| 网络中断期间的输入 | 不丢失，因为已经写入本机；恢复网络后同步，或另存为副本 |
| 页面崩溃或被强制关闭 | 丢失的只是最后一次写入本机之后的输入：停顿后约 1 秒，连续输入时最多约 3 秒（大文档按实测放宽） |
| 清除浏览器数据、更换设备、本机草稿被关闭 | 未同步的修改无法恢复 |

不把关闭页面时发出的异步请求当作保存机制；不承诺离线冷启动。

## 8. 内容存储与数据模型

### 8.1 快照存储与平台信封

文档内容以 Univer 原生快照的形式保存，不把单元格映射成业务表；元数据用普通的关系表。

**快照用 `bytea` 存 gzip 压缩后的原始 JSON 字节，不用 `jsonb`。** 原因如下：

- `jsonb` 不接受 `\u0000`：用户粘贴一段带 NUL 字符的文本后，这份文档就再也存不进去了。[S9]
- `jsonb` 会重排键、去掉重复键，导致内容哈希和客户端算出来的对不上；而且每次写入都要解析整份 JSON。
- 服务端不需要在数据库里查询快照内部的字段。
- 压缩后，库的体积、WAL 和备份量都会明显下降；读取时还可以直接以 `Content-Encoding: gzip` 下发给浏览器。

**平台信封的字段**：
- 文档类型、平台格式版本；
- 插件档案的标识与版本；
- 写入时的 SDK 版本、客户端构建版本；
- 内容哈希（SHA-256，基于解压后的字节计算）；
- 修订号。

不以 SDK 版本号作为平台格式的唯一版本号。

### 8.2 插件档案与资源完整性

源码核查的结论是：保存时，SDK 只输出当前已注册插件的资源，并用它整体覆盖快照里的 `resources`。所以**没有注册的插件，它的资源会被静默丢弃**（`resource-loader.service.ts:164-173`）。对策如下：

- 为每种文档类型定义一份固定的"插件档案"，例如 `sheet@1`、`doc@1`，内容包括插件列表、各插件的配置、适用的 SDK 版本。
- 编辑器页面必须把档案里的插件全部加载完，才允许进入编辑；任何一个插件加载失败，都只允许阅读。
- 文档信封里记录它用的档案标识。档案变更（增删插件）视为数据格式变更，走 §8.7 的流程。
- 服务端校验两点：
  - 资源名称必须在档案的白名单内；
  - 原来存在且非空的资源，在新快照里消失了，就拒绝保存并告警。
- 阅读模式也加载同一份档案，因为渲染需要它（比如条件格式）。这不会影响数据，因为阅读模式不保存。
- 资源名称清单在 M0 时从锁定版本的源码中生成，并随版本升级复核。
- 表格的超链接存在单元格富文本的 `p.body.customRanges` 里，会随单元格数据一起持久化。

### 8.3 unitId 与文档复制

- 平台的 `documentId` 和 Univer 内部的 `unitId` 相互独立。`unitId` 在文档创建时生成、存在快照里，终身不变。复制文档时快照原样复制，**不改写** `unitId` 和工作表 id。
- 原因：`unitId` 与工作表 id 内嵌在图片、评论等对象的内部；筛选、验证、条件格式等资源又以工作表 id 为键；文档内部的链接也引用工作表 id。逐个字段改写很脆弱，而且会随版本变化。
- 约束：同一个 Univer 实例里，不能同时加载两份 `unitId` 相同的文档，否则 SDK 会抛异常。本期编辑器一页只加载一份文档，满足这个约束。将来如果需要在同一页加载多份文档（比如跨文档引用），再引入改写方案。
- 复制还需要处理：新文档的标识与授权（按目标空间的规则），以及资源引用关系（新增 `document_asset_links` 记录，不复制图片文件）。

### 8.4 初步实体

| 实体 | 职责与关键约束 |
|---|---|
| users / auth_sessions | 账户（含系统角色、状态）与登录会话；会话令牌只存摘要 |
| user_local_keys | 本机草稿的加密密钥，用服务端主密钥加密后存储，可以吊销 |
| invitations / password_resets | 单次有效、有时限、只存摘要，并做尝试限流与审计 |
| spaces | 类型（个人/团队）、名称、状态（活跃/归档）、是否全员可见 |
| space_members | 空间、用户、空间角色（管理员/编辑者/查看者） |
| folders | 所属空间、父文件夹；没有独立权限 |
| documents | 所属空间与文件夹、类型、标题、创建者、状态（正常/回收站）、当前修订号、`writeEpoch`、Univer `unitId`、插件档案、SDK 版本 |
| document_contents | 每份文档一份当前快照（gzip 压缩的 bytea）、内容哈希、字节数 |
| document_grants | 文档、用户、角色（查看者/编辑者）；不因空间变动而自动删除 |
| document_edit_leases | 每份文档至多一条：持有者、登录会话、令牌摘要、代次、到期时间、最后活动时间、请求编辑者与请求时间；另外记录最近一次租约的结束方式与时间，用于"异常中断提示" |
| document_revisions | 每次保存一行元数据：修订号、`requestId`、负载摘要、`clientInstanceId`、`localSeq`、保存人、时间、内容哈希。用于幂等、审计和历史版本列表；不存正文；保留 30 天 |
| document_checkpoints | 小时级历史版本，以及删除、迁移前的检查点（gzip 压缩的 bytea），带类型和到期时间 |
| assets / document_asset_links | 每次上传一条图片记录（独立的 assetId、上传者、类型、尺寸、内容哈希），图片文件按内容哈希去重存储；文档与图片之间的引用关系 |
| audit_events | 登录、授权、成员与空间变更、删除与恢复、强制接管、管理员加入空间、密钥吊销等；不记录文档正文 |

所有访问都从可信的会话和数据库里获取归属信息，不采信客户端提交的快照中的元数据。

### 8.5 图片资源

- **上传**：通过 `UniverDrawingPlugin` 配置里的 `override` 替换 `IImageIoService`。用户插入或粘贴图片时，先上传到平台，再返回同源地址 `/api/assets/{assetId}`；快照里只保存这个地址（`ImageSourceType.URL`），不内联 Base64。
- **鉴权读取**：Univer 渲染图片时使用 `crossOrigin="anonymous"`，而同源请求在这种模式下仍然会带上 Cookie，所以读取可以走正常的鉴权。
- **存储方式**：每次上传都生成独立的 assetId 并记录上传者；图片文件按内容哈希（SHA-256）去重存储，多个 assetId 可以指向同一个文件。文件只追加、不修改。
- **引用关系**：由服务端扫描快照中本平台的资源地址（按 `/api/assets/{uuid}` 模式匹配）得出，不需要理解 Univer 的内部结构；每次保存时更新 `document_asset_links`。
- **读取授权**：满足以下任一条件即可读取：
  - 请求者能读取任意一份引用了这张图片的文档；
  - 请求者是上传者，且图片上传还不到 24 小时。这是为了覆盖"已上传、但还没随快照保存"的窗口。
- **保存校验**：保存时逐个检查快照中的资源地址，保存者对每个地址都必须有读取权，或者是自己新上传的。这样可以拦住伪造他人文档资源地址的快照。
- **回收**：没有任何文档、历史版本或检查点引用的 assetId，超过宽限期后删除；某个文件已经没有任何 assetId 指向时，才删除这个文件。**宽限期不短于备份保留期**，这样数据库做时间点恢复之后，被引用的图片一定还在。
- **复制与恢复**：复制文档、从历史版本恢复为副本，都只新增引用关系，不复制文件。

### 8.6 历史版本与恢复检查点

- 当前快照始终保留。
- 文档有变更时，每小时最多生成一个检查点，保留 7 天。这就是用户可以看到的"历史版本"。
- 删除文档之前，以及 SDK 或插件档案升级迁移之前，额外生成检查点，保留 30 天，只供运维恢复。
- **谁能看**：编辑者可以查看历史版本列表（时间、保存人）、只读预览、"恢复为副本"。查看者看不到，因为历史版本中可能留着后来被删掉的敏感内容。空间管理员可以清除某份文档的全部历史版本，操作记入审计。
- **不做**：逐操作回放、差异对比、就地回滚。这和 Pro 的编辑历史是两回事，本平台只保存整份快照。
- 容量与保留策略，在试运行前按实际数据量验证。

### 8.7 SDK 升级与数据格式

源码核查的结论是：Univer 没有跨版本的快照迁移机制；`appVersion` 只是个信息字段，而文字文档的快照里连这个字段都没有。因此：

- SDK 升级与数据迁移分开评审。升级前，用历史样本（每个插件的保存/重开样本）做回归。
- 新版 SDK 打开旧快照并保存后，文档就按新格式写入了。所以升级前要为所有文档生成迁移前检查点。
- 未知的平台格式版本或档案标识，不得被静默改写；客户端拒绝编辑并给出提示。
- 服务端要求的最低客户端构建版本随发布更新，旧标签页不能再保存（§7.4）。
- 后续可以利用 Univer 在 Node.js 下的无界面运行能力，在服务端批量"打开再保存"来完成格式迁移；本期不实现。

## 9. 总体技术架构

### 9.1 推荐方案

一个前端应用，加一个模块化的 Node API 应用。Univer 运行在浏览器里；后端负责身份、组织与空间、授权、快照、租约、资源、历史版本和运维接口。

```text
浏览器：React + TypeScript + Vite
  ├─ 登录、个人空间、团队空间、与我共享、回收站、系统管理
  └─ 编辑器页（整页加载）：Univer 开源插件 + 平台适配层 + 本机发件箱（IndexedDB + Web Locks）
                     │
                HTTPS / REST（同源）
                     │
             Node.js + NestJS 模块化单体
              ├─ 账户 / 组织与空间 / 文档授权
              ├─ 文档元数据 / 快照 / 修订 / 编辑租约与交接
              ├─ 图片资源 / 历史版本 / 审计 / 健康检查
              └─ 本机密钥 / 备份与运维入口
                     │
            PostgreSQL + 持久化资源目录
                     │
              独立故障域的备份目的地
```

**不需要官方的协同 Server SDK，也不需要实现它的 PostgreSQL Adapter。** MVP 不要求服务端加载编辑器或重算公式；服务端也不把客户端算出的公式结果当作可信的业务事实。

### 9.2 技术选型

| 层次 | 建议 | 说明 |
|---|---|---|
| 前端 | React + TypeScript + Vite | Univer 视图层基于 React，支持 React 18/19；编辑器页是独立入口，整页加载 |
| 编辑器 | Univer 开源 1.0 线，插件模式 | 固定插件档案；不使用 presets；不引入 Pro |
| 后端 | Node.js 24 LTS + NestJS | 模块化单体；请求体上限按快照容量单独配置 |
| 数据库 | PostgreSQL（受支持的稳定大版本） | M0 锁定版本和镜像，不用浮动标签 |
| 数据访问 | Drizzle ORM + pg | 类型化模型与迁移；关键写入直接用参数化 SQL 与显式事务 |
| 会话 | 服务端 Session + PostgreSQL | 浏览器只持有 HttpOnly Cookie |
| 本机存储 | IndexedDB + Web Locks + WebCrypto + CompressionStream | 发件箱、标签页协调、AES-GCM 加密、gzip 压缩 |
| 资源 | StorageAdapter + 私有持久化目录 | 初期只实现文件系统驱动；S3 兼容驱动留作后续扩展 |
| 工程组织 | pnpm workspace | 一个 Monorepo，不引入额外的构建编排层 |
| 测试 | 单元测试、真实 PG 集成测试、Playwright E2E | 并发写入、断网、崩溃恢复都在真实环境中验证 |
| 部署 | 容器、反向代理、持久化卷、受控迁移 | 单实例首发；备份与恢复必须可用 |

Node.js v24 是当前的 LTS 版本；NestJS 支持 TypeScript 模块化服务；Drizzle 使用 Apache-2.0 许可证。[S5][S6][S7]

### 9.3 有意不增加的设施

首发不引入 Redis、消息队列、WebSocket、微服务、Kubernetes、Service Worker 或独立的文档服务。会话、租约、幂等记录都存在 PG 里。定时任务（回收、清理）在应用内执行，并用 PG 的 advisory lock 防止将来多实例时重复执行。

"单实例"只是部署规模上的选择，并不意味着把关键状态放在内存里。将来增加 API 副本之前，需要先迁移到共享的资源存储，并重新验证定时任务、限流与故障行为。

### 9.4 代码组织

```text
CLAUDE.md         协作约定：文档流程、目录与命名、代码与依赖约束、Git 约定
apps/
  web/            前端产品界面；src/editor/ 下放 Univer 适配层、插件档案、平台插件、本机发件箱
  api/            NestJS 后端；src/db/ 下放 Schema、迁移与仓储
packages/
  contracts/      前后端共享的 API 协议、错误码与类型
tests/
  fixtures/       表格、文字文档、权限、资源样本，以及每个插件的保存重开样本
  integration/    基于真实 PG 的并发、租约、撤权与事务测试
  e2e/            多浏览器、多标签页、断网与崩溃恢复的验收
spikes/           M0 的验证代码，不进入生产构建
docs/
  architecture/   活文档：当前架构总览，每个 Phase 结束时更新
  adr/            跨阶段生效的架构决策记录
  templates/      M 总设计、Phase 设计、spec、plan、review、handoff、ADR 的模板
  v0.1/           版本计划、工程规范、延期登记与各 M 的设计文档
deploy/           构建、容器与环境配置模板
```

其他共享包，等出现第二个使用方时再拆。编辑器适配层只封装实际需要的入口：创建与销毁、载入与捕获快照、订阅变更、切换只读、能力清单。不为"将来可能替换编辑器"提前建设通用框架。

## 10. SDK 集成要点

### 10.1 定制方式（按优先级）

1. **插件配置与菜单配置**：例如用 UI 插件的 `menu` 配置隐藏不支持的菜单项。
2. **依赖注入覆盖**：例如用 `UniverDrawingPlugin` 的 `override` 替换图片读写服务。
3. **自有插件**：例如变更检测、只读加固、粘贴清洗、平台自己的菜单项。
4. **Facade API**：快照读写、权限切换、公式事件等。

根据官方的 API 稳定性策略，优先使用公开的稳定 API。必须使用内部 API（例如 `IUndoRedoService`）时，在适配层集中封装并登记，升级时重点回归。[S3]

### 10.2 编辑器页面的生命周期

- 编辑器页面整页加载，一页只承载一份文档；离开时整页卸载。这样可以避开 Univer 实例在单页应用中反复创建、销毁造成的内存泄漏[S4]，以及用相同 `unitId` 重复创建时抛出的异常。
- 在阅读模式和编辑模式之间切换、需要重新加载快照时，先 `disposeUnit`，再重新创建。
- 编辑器按文档类型分别打包，按需懒加载。

### 10.3 补丁策略

只有公开配置、插件、依赖注入和 Facade 都解决不了的具体缺陷，才进入源码补丁评审。
- 补丁用 `pnpm patch` 管理，登记上游版本、修改原因、测试、影响范围，以及升级时如何重放。
- 不直接修改 `node_modules`，也不把上游代码复制进业务代码。
- 能提交给上游的修复，优先提交上游。

## 11. 安全要求

### 11.1 认证与会话

- 使用服务端 Session。Cookie 设置 HttpOnly、Secure、SameSite；对状态变更接口做 CSRF 防护与 Origin 检查；配置空闲过期和绝对过期；重置密码、停用账户时，让相关会话失效。[S12]
- 密码使用有人维护的 Argon2id 实现，参数按部署机器的基准测试结果设置。[S13]
- 邀请与重置令牌单次有效、有时限、只存摘要，并做尝试限流与审计。不依赖外部邮件服务，由管理员通过受控渠道分发链接。

### 11.2 输入校验

- API 做运行时的结构校验，并限制正文长度、JSON 嵌套深度、键的数量、文档容量和请求频率。TypeScript 类型不能代替网络输入校验。
- 快照校验包括：
  - 解压后的体积上限、嵌套深度；
  - 顶层类型和 `unitId` 与文档记录一致；
  - 资源名称在白名单内，且没有缩水（§8.2）；
  - 资源地址的归属（§8.5）；
  - 外链图片地址（§11.3）。
- 上传请求的体积同样受限，并防止压缩炸弹。

### 11.3 外部资源与内容安全策略

源码核查确认了几件好事：开源 SDK 默认没有遥测，不从 CDN 加载资源，也没有 `eval`；WEBSERVICE 函数没有实现；超链接点击前会经过 `isSafeUrl`，只放行 http、https 和 mailto。但仍有两条路径会让浏览器请求外部地址：

- 文字文档粘贴 HTML 时，会保留外部 `<img src>` 地址；
- `IMAGE()` 公式会把任意 URL 当作图片加载。

这两条都可能被用来追踪谁打开了文档，或者探测内网地址。对策如下：

- **严格的 CSP**：以下面这组策略为起点，M0 验证 Univer 在其下能否正常工作：
  `default-src 'self'; img-src 'self' data: blob:; connect-src 'self'; font-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; worker-src 'self' blob:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'`；
  并把违规报告发到平台自己的接口。
- **粘贴清洗**：由插件在粘贴时移除或替换外部图片地址；`data:` 图片改为走上传。
- **限制 `IMAGE()`**：只允许本平台的资源地址，或者整体禁用；具体实现方式在 M0 确定。
- **服务端校验快照中的图片地址**：只接受本平台的资源地址。M0 确认所有插入路径都走上传之后，`data:` 图片也不再接受。
- **链接与脚本**：服务端对快照中的链接地址做协议白名单检查。不启用任意用户脚本、宏、远程数据读取，也不加载不受控的插件。Uniscript 已从 1.0 线移除，不引入。

### 11.4 图片上传

- 检查扩展名、真实的媒体类型（文件头）、大小和像素数；
- 由服务器生成文件名，不采信客户端提供的路径；
- 只接受 PNG/JPEG/WebP/GIF，不接受 SVG、HTML 等主动内容；
- 文件存放在不可执行、不公开的目录里，经过鉴权接口读取，响应带上 `Content-Type` 与 `X-Content-Type-Options: nosniff`。[S14]

### 11.5 日志与隐私

- 日志中禁止写入密码、会话令牌、租约令牌、本机密钥、完整快照，以及未脱敏的敏感信息。
- 静态资源、字体与 SDK 依赖全部自己托管，不从第三方 CDN 加载。
- 审计事件不记录文档正文。

## 12. 生产验收目标

下列数字是项目目标与边界，**不是已完成的测试结果，也不是 Univer 官方的性能承诺**。M0 和试运行中必须记录真实的环境、样本和测量结果。

### 12.1 起始规模与容量

| 项目 | 起始目标 / 边界 |
|---|---|
| 组织规模 | 约 100 名成员，若干个团队空间 |
| 在线负载 | 50 个在线会话，其中 20 个会话在不同文档上持续编辑 |
| 单文档并发 | 读者数量受整体容量限制；可提交修改的编辑会话最多一个 |
| 单快照上限 | 解压后的 JSON 默认不超过 5 MiB；异常嵌套与超量对象另外限制 |
| 表格样本 | 约 5 万个实际使用的单元格，其中至少 1,000 个公式 |
| 文字样本 | 约 2 万汉字，包含标题、列表、基础表格与图片 |
| 图片 | 单张不超过 5 MiB，另设像素上限与组织级配额 |
| 浏览器 | 桌面 Chrome / Edge 的当前版本和前一个主要版本；Safari 的当前主要版本单独验收 |

全量快照的路线面向中小型文档，不承诺百万行、无限大的文件或无限复杂的公式依赖。

### 12.2 性能目标

基准环境从 4 vCPU / 8 GiB 内存、SSD 的应用与数据库测试部署开始，同时记录浏览器所在机器、网络和数据库的配置。

| 指标 | 目标 |
|---|---|
| 1 MiB 快照的保存请求，服务端处理耗时 | p95 ≤ 1 秒 |
| 在 100 ms 往返延迟、20 Mbps 带宽下，从捕获到"已保存到云端" | p95 ≤ 3 秒 |
| 从修改停止到"已保存在本机"（含公式等待） | p95 ≤ 2 秒 |
| 1 MiB 文档捕获快照时的主线程阻塞 | p95 ≤ 100 ms（M0 实测后确认） |

首屏可编辑时间、编辑响应、事件循环延迟、内存增长、PG 写入量与 WAL 增长，都要测量。

如果保存频率和快照体积造成了瓶颈，优先限制体积、去重、调整保存频率；不要临时引入未经设计的"增量协同"来掩盖问题。

### 12.3 写入量与存储预算

- **估算公式**：WAL 写入量 ≈ 同时编辑的人数 × 压缩后的快照体积 ÷ 上传间隔。
- **示例**：20 人持续编辑 1 MiB 的文档、每 15 秒上传一次；如果压缩后约为原来的 10%–20%，WAL 大约是每小时 0.5–1 GiB。
- **压缩率**：Univer 快照的真实压缩率在 M0 实测。
- **需要单独预算的存储**：时间点恢复所需的 WAL 归档保留期、小时级检查点（保留 7 天）、`document_revisions` 元数据。

### 12.4 运维与恢复

**上线前必须具备**：
- HTTPS、环境变量校验、进程优雅退出、数据库连接池、超时控制、请求标识、就绪与存活探针；
- 监控：错误率、按原因分类的保存失败、租约冲突、客户端上报的同步延迟、CSP 违规报告、快照体积分布。

**迁移与升级**：数据库采用受控迁移，发布前先备份；不在每个应用实例启动时并发执行不可逆的迁移。编辑器升级前，先跑历史样本回归。

**恢复**：
- 数据库用基础备份加 WAL 归档，支持时间点恢复。[S10]
- 建议的灾备目标：**RPO 不超过 1 小时，RTO 不超过 4 小时**。两者都要经过演练验证，不视为已达成的 SLA。
- 图片与数据库必须能恢复到相互对应的水位：图片只追加、延迟删除，并持续同步到备份目的地（§8.5）。
- 备份放在独立于主机的故障域，限制访问并加密。备份任务的成功日志，不能代替真正的恢复演练。

初期单实例不提供自动高可用。MVP 的生产标准是：边界明确、数据有保护、故障可以被发现也可以恢复；而不是宣称具备并不存在的跨机房容灾。

## 13. 实施里程碑

排期按交付物与验收门槛推进。具体的日历工期，要在 M0 摸清真实的兼容性和工作量之后，结合实际投入的人员再确定。

### 13.1 编排原则

- **颗粒度**：每个 M 是一块内聚的能力增量，通常包含 2–5 个 Phase。
- **独立性**：每个 M 只依赖前面 M 的产出；完成后不需要回头修改前面 M 的代码；数据模型只做加法。
- **可验证性**：每个 M 都有自己的用户故事（US 编号）和 E2E，并明确负责哪几条上线门槛（§13.3）。
- **风险前置**：保存正确性、离线恢复这类风险最高的部分尽早做，并且各自独立验收。
- **质量不随范围打折**：范围可以通过延期收缩，质量门槛不能降低（§13.5）。

为了让各个 M 相互独立，约定：
- M1 就建 `spaces` 表（这时只有个人空间），M2 再加团队空间；M1 的文档表就带 `revision`，M3 再加租约和代次。
- M2 里的撤权、移动、停用等操作，一开始就递增 `writeEpoch`。这样 M3 只需接入租约校验，不必回头修改 M2 的事务。
- 审计、错误码、配置、日志、迁移这些基础设施在 M1 建好，后面各 M 只需接入。

### 13.2 里程碑编排

| 阶段 | 目标 | 主要内容 | 交付物 | 退出条件 |
|---|---|---|---|---|
| M0：技术验证 | 回答阻断架构决策的 SDK 问题，锁定版本与插件档案 v1 | 按 §13.4 清单逐项验证；代码放在 `spikes/`，不进入生产构建 | 验证报告与证据、依赖与许可清单、能力矩阵、插件档案 v1、版本基线、测量记录 | 清单每一项都有结论和证据；阻断项有明确的对策或范围变更；本文档已按结论更新 |
| M1：工程底座与行走骨架 | 建立生产级的工程底座，打通最薄的端到端链路 | 工程底座：Monorepo 与工具链、模块边界强制、单元/集成/E2E 测试框架、CI、开发环境；配置、日志、错误码、审计、迁移的基础设施<br>最小但生产级的链路：登录、个人空间、新建表格 → 编辑 → 在线保存 → 重开 | 工程底座、行走骨架、架构总览 v1、首批 ADR；《工程规范与完成定义》落地 | CI 全绿；骨架链路的 E2E 通过；能构建容器并部署到测试环境 |
| M2：组织、空间与权限 | 完整实现 §5 的组织与权限模型 | 邀请、停用、重置密码；团队空间、成员与角色、全员可见；文件夹、文档元数据、移动、复制、回收站；单独分享、"与我共享"、按标题搜索；系统管理与审计事件 | 数据迁移、API 协议、管理界面、权限测试 | §5.3 权限矩阵的用例全部通过；越权访问被拒绝；成员移除与账户停用的逻辑正确 |
| M3：编辑权与保存协议 | 多人场景下的在线编辑正确、不覆盖 | 编辑租约与交接（§6）；`writeEpoch` 与撤权联动；保存协议（§7.4）；插件档案强制、变更检测与公式等待（§7.3、§8.2）；只读加固（§6.5）；编辑态界面与阅读者的更新提示 | 保存协议的实现、集成测试、双用户与多标签页的 E2E | 不发生静默覆盖；失效会话不能保存；确认丢失后可以安全重试；交接按 §6.3 工作 |
| M4：本地优先与离线恢复 | 断网不丢，崩溃可以恢复 | 本机发件箱、加密密钥、Web Locks；恢复规则与冲突副本（§7.5）；离线体验；退出登录与撤权时的本机清理（§7.6）；mutation 增量日志（视 M0 的结论） | 本地优先管道、恢复流程、断网与崩溃的 E2E | 断网编辑与崩溃恢复符合 §7.8 的边界；真冲突一律另存为副本 |
| M5：表格能力与资源 | 表格功能达到 §4.2 的范围，内容类能力可用 | 表格插件白名单逐项完成；图片资源服务、鉴权与回收（§8.5）；收紧 CSP、粘贴清洗、限制 `IMAGE()`（§11.3）；历史版本（§8.6）；CSV；复制时带上资源 | 能力验收报告、资源存储实现、历史版本与 CSV 功能 | 启用的表格功能保存重开一致；图片与插件资源完整；不向第三方发请求；若本 M 开始时 1.0.0 尚未正式发布，已完成 §3.2 的门槛评审 |
| M6：文字文档 | 文字文档达到 §4.3 的范围 | 文档插件档案、编辑器接入、中文输入、表格与图片、查找替换、粘贴清洗、只读 | 文字文档能力验收报告 | 启用的文字文档功能保存重开一致；M0 中验证不稳定的能力已隐藏，并记录了范围变更 |
| M7：上线准备 | 全部上线门槛通过，具备上线条件 | 压测与容量、监控告警、备份与恢复演练、部署包与运维手册、升级与回滚演练、独立安全审查、升级到 1.0 正式版并回归、复核已知限制、门槛总验收 | 部署包、运维手册、安全与性能报告、恢复记录、门槛验收报告 | §14 的全部门槛通过；已知限制在产品中可见；数据损坏或越权问题清零 |
| M8：受控试运行 | 真实团队试用，收敛到发布候选版本 | 内部小团队试用，观察真实文档与故障场景，修复阻断问题 | 试运行记录、问题清单、正式发布候选 | 核心流程稳定；数据安全、授权、保存与恢复都没有阻断项 |

不以"页面能打开""编辑器能输入"作为完成标准。每个 Phase、每个 M 结束时，都要有可以复核的测试结果。

### 13.3 上线门槛与里程碑的映射

| 门槛 | 首次验证 | 补充验证 |
|---|---|---|
| A01 开源依赖 | M0（证据） | M1（CI 自动检查）；M7（总验收） |
| A02 原生内容完整性 | M3（保存管道） | M5（表格功能）；M6（文字文档） |
| A03 越权访问 | M2 | M5（图片资源） |
| A04 编辑唯一性 | M3 | — |
| A05 过期旧会话 | M3（在线） | M4（离线与另存为副本） |
| A06 撤权一致性 | M3（租约与保存） | M4（本机清理） |
| A07 确认丢失 | M3（在线重试） | M4（崩溃场景） |
| A08 保存中继续输入 | M3 | M4 |
| A09 进程与浏览器异常 | M1（API 重启） | M4（浏览器异常） |
| A10 复制与资源 | M2（元数据与快照） | M5（图片资源） |
| A11 输入与容量 | M3（快照校验与容量） | M5（图片、链接、外链） |
| A12 恢复演练 | M7 | — |
| A13 升级回归 | M3（拦截旧客户端） | M7（升级演练） |
| A14 体验真实 | 各 M 的界面 | M7（总验收） |
| A15 断网编辑 | M4 | — |
| A16 崩溃恢复 | M4 | — |
| A17 插件资源完整 | M3 | M5、M6（新增插件时） |
| A18 公式一致性 | M3 | M5 |
| A19 外部请求 | M5（表格） | M6（文字文档）；M7（总验收） |
| A20 交接规则 | M3 | — |

M7 对全部门槛做一次总验收。每个 M 的 00 号总设计，都要列出本 M 负责的门槛以及对应的测试。

### 13.4 M0 验证清单

以下是 M0 的验证范围。每一项使用 V 编号；如何划分 Phase，以 M0 的 00 号总设计为准。

1. **V01**：安装 1.0 线最新的 rc 版本并精确锁版本；生成依赖图与许可清单；确认没有 Pro 依赖，也没有远程运行依赖。
2. **V02**：在严格的 CSP（§11.3）下运行表格与文字文档编辑器；如果启用了公式 Web Worker，一并验证。
3. **V03**：为每个启用的插件准备保存—重开样本，验证内容与资源一致；并验证"保存 → 重开 → 再保存"两次得到的快照一致。
4. **V04**：复现"插件缺失导致资源丢失"，并验证服务端"资源不缩水"校验的思路可行。
5. **V05**：复制文档（不改 `unitId`）后，两份文档各自编辑、保存、重开都正常。
6. **V06**：打开文档不会被判定为"有修改"；确定变更检测的排除名单。
7. **V07**：修改公式所依赖的单元格后立即保存，快照里的公式结果与重新计算的结果一致。要覆盖大表、慢计算和易变函数。
8. **V08**：测量快照捕获耗时与主线程阻塞时间（1 MiB 与 5 MiB 样本）。
9. **V09**：在表格与文字文档的阅读模式下，所有编辑入口都被拦截，或者不会产生可提交的内容。编辑入口包括撤销重做、筛选排序、增删工作表、拖动图片、粘贴。同时验证阅读模式与编辑模式之间的切换，以及保护相关功能和其他不支持功能的入口是否已隐藏。
10. **V10**：用 5 万单元格、1,000 个公式的样本，测量首屏时间、编辑响应、公式计算和内存占用。
11. **V11**：替换图片读写服务后，插入、粘贴、复制图片都会上传，并以同源地址保存；同源鉴权读取正常；找出所有还会残留 Base64 的路径。
12. **V12**：验证 Docs 粘贴外链图片的清洗、`IMAGE()` 的限制方案，以及 CSP 的拦截效果。
13. **V13**：验证文字文档的以下各项：
    - 中文输入法（拼音、五笔）；
    - 复制粘贴，来源包括 Word、网页和本平台；
    - 标题、列表、基础表格、图片环绕、查找替换；
    - 无分页版式；
    - 目录功能是否纳入。
14. **V14**：测量 IndexedDB 写入 1–5 MiB 快照的耗时，以及配额行为；验证 Web Locks 的多标签页协调、加密开销，以及浏览器进程被杀之后的恢复。
15. **V15**：对 mutation 增量日志做可行性验证：能否序列化、能否确定性重放、能否与快照组合恢复。
16. **V16（可选）**：在 Node.js 下以无界面方式加载快照，为将来的服务端迁移工具做可行性记录。此项不作为阻断项。

### 13.5 文档与交付流程

- **JIT 滚动推进**：
  - 以本文档为版本总纲；
  - 每个 M 开始时，编写 `M{n}-<短名>/00-M{n}-总设计.md`，内容包括 Phase 规划、用户故事和负责的门槛；
  - 每个 Phase 开始时，基于上一个 Phase 完成后的最新代码，编写 `NN-P{k}-<短名>.md`（架构设计与实施规划，拆到 Step 为止）；
  - Step 按需要先写 spec，再写 plan。
- **审批与自主推进**：每个 M 的总设计须经用户讨论并审批；审批通过后，自主逐个推进 Phase，Phase 设计文档编写并自审后直接实施。遇到架构回归、跨模块的协同调整或意料之外的高风险发现时，停下来汇总问题和建议方案，由用户决策。
- **Phase 完成审查**：每个 Phase 完成后，必须经过代码审查（优先由独立的审查者进行）、修复与复验，审查报告存入 `reviews/`。
- **分支纪律**：每个 Phase 一个分支；审查通过后以 `git merge --no-ff` 合并到 `main`，保留分支提交线。
- **每个 Phase 收尾**：更新架构总览与 ADR；编写交接单，存入 `handoffs/`；延期项登记到《延期事项登记》（`docs/v0.1/02-延期事项登记.md`）。
- **延期的边界**：只能延期功能，不能延期质量门槛。触及本文档范围或上线门槛的延期，必须走范围变更，并更新本文档的修订记录。
- **具体约定**：见仓库根目录的 `CLAUDE.md`，以及《工程规范与完成定义》（`docs/v0.1/01-工程规范与完成定义.md`，在 M1 开始前定稿）。

## 14. 上线硬门槛

| 编号 | 场景 | 通过标准 |
|---|---|---|
| A01 | 开源依赖 | 生产依赖图与构建产物中没有 Pro；功能不依赖许可证服务；第三方清单完整 |
| A02 | 原生内容完整性 | 表格与文字文档的目标功能在保存、关闭、重开后，内容与资源一致；二次保存结果稳定 |
| A03 | 越权访问 | 修改 URL、文档 ID、空间 ID、资源 ID，都不能读取或写入无权的数据；单独分享、空间角色、全员可见、系统管理员无内容权限等规则都按 §5.3 生效 |
| A04 | 编辑唯一性 | 两个用户同时申请同一份文档的编辑权，最多一个成功；同一用户开两个标签页时同样适用 |
| A05 | 过期旧会话 | A 失去租约、B 接手并保存后，A 恢复网络提交旧内容一定被拒绝，A 的内容可以另存为副本 |
| A06 | 撤权一致性 | 编辑过程中被撤权、被移出空间或账户被停用，之后的保存、续租、资源访问都被拒绝，本机的未同步修改被删除 |
| A07 | 确认丢失 | 服务端已经提交、客户端却没收到确认时，重试不会重复写入、不会重复递增修订号，也不会误判为冲突 |
| A08 | 保存中继续输入 | 保存的回包不会把之后发生的修改误标为已保存，也不会丢掉最新的待保存状态 |
| A09 | 进程与浏览器异常 | API 重启不会丢失已确认的数据；休眠、后台标签页、网络切换时，有清晰的只读或恢复行为 |
| A10 | 复制与资源 | 新文档拥有独立的身份与授权；图片引用和图片回收不会相互串扰、损坏 |
| A11 | 输入与容量 | 超限的快照、危险链接、伪造图片、外链图片、伪造的资源地址和异常结构都被拒绝，并且不能拖垮整个服务 |
| A12 | 恢复演练 | 在独立环境中，从备份恢复真实的样本文档和图片，核对恢复耗时与一致性 |
| A13 | 升级回归 | 旧数据样本在目标版本中可以读取；不会静默丢弃资源；旧客户端不能保存；不可逆的升级有明确的恢复路径 |
| A14 | 体验真实 | 没有实时共编、没有 Office 转换、容量与丢失边界，这些都在产品中可见；不显示虚假的"已保存到云端" |
| A15 | 断网编辑 | 断网状态下持续编辑 10 分钟后恢复网络：没人接手时全部同步；有人接手时不覆盖对方，本地内容可以另存为副本 |
| A16 | 崩溃恢复 | 编辑过程中强制结束浏览器进程，重新打开后可以恢复到最后一次写入本机时的状态 |
| A17 | 插件资源完整 | 插件没加载全的客户端无法进入编辑；资源缩水的快照被服务端拒绝 |
| A18 | 公式一致性 | 保存下来的公式结果，与重新计算的结果一致 |
| A19 | 外部请求 | 打开和编辑任意样本（包括含外链图片、`IMAGE()` 的样本）期间，浏览器不向第三方地址发起任何请求 |
| A20 | 交接规则 | 空闲释放、请求编辑时自动交出、本人接管、强制接管、异常中断提示，都按 §6.3 工作 |

凡是涉及数据丢失、静默覆盖、越权访问、越权写入、无法恢复的缺陷，都是阻断上线的问题，不以"这是 MVP"为由豁免。

## 15. 责任分工与交付物

职责可以由同一个人兼任，但不能缺位。

| 责任 | 主要工作 |
|---|---|
| 产品负责人 | 确认范围、空间与权限规则、文档样本、交接行为与发布条件 |
| 编辑器负责人 | 版本与插件档案、UI 接入、只读加固、快照完整性、本机发件箱、中文输入与资源适配 |
| 服务端负责人 | 数据模型、认证授权、空间与分享、租约与保存事务、审计、迁移 |
| 质量与运维负责人 | 并发与故障测试、断网与崩溃测试、安全验收、性能记录、部署、备份与恢复 |

项目最终交付的不只是前后端代码，还包括：
- 数据库迁移、容器部署配置；
- 第三方依赖与许可清单、能力矩阵与插件档案；
- API 协议、测试样本与测试结果；
- 备份恢复手册、升级回滚说明、已知限制清单。

软件选型不依赖 Pro 授权，并不代表运行零成本。服务器、存储、备份、域名、带宽和人工运维仍需要预算；本稿不在缺少容量和人员数据的情况下编造总价。

## 16. 主要风险与应对

| 风险 | 应对 |
|---|---|
| 1.0 正式版延期，或者从 rc 到正式版之间有破坏性变更 | 精确锁版本；M5 开始时做门槛评审；正式版发布后在最近的 Phase 边界升级并回归 |
| 插件资源漏存，或因插件缺失被丢弃 | 固定插件档案；插件没加载全就不允许编辑；服务端校验资源不缩水；每个插件都有保存重开样本 |
| 公式结果被存成旧值 | 捕获快照前等待公式结果写回；A18 |
| 前端只读有缺口，导致误操作 | 服务端租约是唯一的写入边界；逐表只读、拦截撤销重做、取得编辑权后重新加载 |
| 基础 Docs 达不到目标体验 | M0 逐项验证；不稳定的功能隐藏；必要时提交范围变更 |
| 某个开源功能间接依赖商业组件或远程资源 | 许可证、传递依赖、运行时网络三重核验；CSP 兜底 |
| 本机草稿泄露，或被浏览器清理 | 只存未同步的内容、加密、可吊销、可整体关闭；申请持久化存储；写进已知限制 |
| 本地优先的恢复逻辑出错，导致内容被覆盖 | 修订号与代次双重检查；真冲突一律另存为副本；A05、A07、A15、A16 |
| 全量快照带来写放大和客户端卡顿 | 容量上限、图片外置、压缩存储、串行上传、哈希去重、按体积自适应调整间隔 |
| 用到的内部 API 在升级时失效 | 集中封装、登记在案、升级时重点回归 |
| 不支持 Office 转换，影响用户采用 | 产品说明中明确"在平台内新建"的模式；提供 CSV；首批试用选择适合在线新建文档的团队 |
| 单实例主机失效 | 独立备份、可复现的部署、恢复演练；不宣称具备自动高可用 |
| SDK 升级损坏旧数据 | 迁移前检查点、格式版本、样本回归、旧客户端禁止保存、回退计划 |

## 17. 范围冻结与后续评审

本稿冻结的首发方向是：

**自部署、单租户的团队文档平台；Node.js / TypeScript + PostgreSQL；Univer 开源 1.0 线；个人空间 + 团队空间；在线表格与基础文字文档；单文档单写者与编辑权交接；本地优先保存；不使用 Pro，不建设 Agent。**

后续评审值得关注的方向：查看者评论、xlsx 数据级导入导出、按用户组分享、查看者本地筛选、mutation 增量日志（视 M0 的结论而定）。

本稿中的实现方式、容量参数与里程碑，都属于建议方案。实施之前先完成 M0；不要把这份计划文档当作已经验证过的架构、已经通过的安全审计，或已经实现的产品。

---

## 附录 A：建议的 API 边界

以下是接口职责的草案，不是已实现的接口；详细的字段和错误结构在实施设计中锁定。

| 接口职责 | 行为要求 |
|---|---|
| 登录会话 | 创建与销毁、限流、Cookie、审计 |
| 账户与邀请 | 系统管理员邀请、停用、重置密码；首个管理员通过命令行初始化 |
| 空间与成员 | 创建、归档团队空间与设置全员可见（系统管理员）；成员与角色（空间管理员） |
| 文档元数据 | 创建（类型白名单、幂等）、重命名、移动、复制、删除、恢复；列表、按标题搜索、与我共享 |
| 分享 | 单独授权的增删改；与租约终止放在同一事务中 |
| 文档内容读取 | 返回快照（gzip）、修订号、档案与格式信息、调用者的权限；以修订号作为 ETag |
| 修订号检查 | 只返回修订号、保存时间、当前编辑者，不传内容 |
| 编辑租约 | 申请、续租（带最后活动时间）、释放、请求编辑、交出、本人接管、强制接管；使用数据库时间与条件更新 |
| 提交快照 | 权限、租约、代次、基准修订号、请求去重、资源校验、客户端版本一起校验 |
| 历史版本 | 列表、只读读取、恢复为副本、清除 |
| 图片 | 上传（校验类型、容量、像素）、鉴权读取 |
| 本机密钥 | 登录后获取；管理员吊销 |
| 运维 | 健康检查、强制结束编辑、备份与恢复入口、CSP 违规报告接收、客户端同步指标上报 |

CSV 的导入导出在客户端完成，不新增服务端的转换接口。

错误需要区分：无权限、租约失效、文档被占用、修订号冲突、负载超限、格式不兼容、客户端过旧。对于没有读取权限的对象，不暴露其是否存在；HTTP 状态与业务错误码保持统一。

## 附录 B：源码核查记录

- **核查对象**：`refer/univer`（dev 分支 d31d624，2026-09-22，版本 1.0.0-rc.0），只读静态分析；版本信息来自 npm registry（2026-09-23）。
- **注意**：行号基于上述提交，锁定版本后需要复核。

| 结论 | 证据 |
|---|---|
| 保存时用已注册插件的资源整体覆盖 `resources` | `packages/core/src/services/resource-loader/resource-loader.service.ts:164-173` |
| 用相同的 unitId 重复创建会抛异常 | `packages/core/src/services/instance/instance.service.ts:224` |
| 工作簿快照有 `appVersion`（仅信息字段），文字文档快照没有 | `packages/core/src/sheets/typedef.ts:51`；`packages/core/src/types/interfaces/i-document-data.ts` |
| 行高自适应 mutation 不带 `onlyLocal` | `packages/sheets-ui/src/services/auto-height.service.ts:160` |
| 公式结果以 `onlyLocal`、`fromFormula` 写回 | `packages/sheets/src/controllers/calculate-result-apply.controller.ts:94-95` |
| 默认计算模式是 `WHEN_EMPTY` | `packages/sheets-formula/src/controllers/trigger-calculation.controller.ts:150` |
| 等待公式结果写回的 Facade | `packages/engine-formula/src/facade/f-formula.ts:234`（`onCalculationResultApplied`） |
| 官方的变更过滤示例 | `packages/network/src/facade/f-univer.ts:71` |
| 重做会直接重放 mutation | `packages/core/src/services/undoredo/undoredo.service.ts`（`RedoCommand` 调用 `sequenceExecute(element.redoMutations, …)`） |
| 清空撤销栈的接口（Facade 未暴露） | `packages/core/src/services/undoredo/undoredo.service.ts:69,278` |
| 默认授权服务注明"不要用于生产" | `packages/core/src/services/authz-io/authz-io-local.service.ts:42,53` |
| 表格工作表的 `readOnly` / `filterOnly` 模式 | `packages/sheets/src/facade/permission/f-worksheet-permission.ts:278-282` |
| 表格权限按命令白名单拦截（增删工作表未覆盖） | `packages/sheets/src/controllers/permission/sheet-permission-check.controller.ts` |
| 文字文档权限按命令前缀拦截 | `packages/docs/src/controllers/doc-permission.controller.ts` |
| 超链接协议白名单 | `packages/core/src/common/url.ts:371-378` |
| 表格超链接存在单元格富文本里 | `packages/sheets-hyper-link/src/commands/commands/add-hyper-link.command.ts:88-107` |
| 图片渲染使用 `crossOrigin='anonymous'` | `packages/engine-render/src/shape/image.ts:149` |
| 图片服务可通过插件的 `override` 替换 | `packages/drawing/src/plugin.ts:62-67` |
| 默认图片类型白名单（没有 WebP） | `packages/drawing/src/basics/config.ts:33` |
| Docs 粘贴时外部图片地址按 URL 保留 | `packages/docs-ui/src/services/clipboard/html-to-udm/converter.ts:180,686-687` |
| `IMAGE()` 公式的结果被转成 URL 图片 | `packages/sheets-formula/src/controllers/image-formula-cell-interceptor.controller.ts:148` |
| WEBSERVICE 未实现 | `packages/engine-formula/src/functions/web/webservice/index.ts:19-24` |
| Uniscript 已被移除 | `CHANGELOG.md:334`（#7217） |
| 剪贴板粘贴使用惰性 DOM 解析 | `packages/docs-ui/src/services/clipboard/html-to-udm/parse-to-dom.ts`；`packages/sheets-ui/src/services/clipboard/html-to-usm/utils.ts:41` |
| 评论数据源可以替换 | `packages/thread-comment/src/services/tc-datasource.service.ts` |
| mutation 协同钩子 | `packages/core/src/services/command/command.service.ts:270` |
| 许可证 | 60 个 `packages/*/package.json` 与 17 个 `presets/packages/*/package.json` 全部为 Apache-2.0 |

## 附录 C：来源

- **核对日期**：2026-09-23。
- **官方资料的作用**：只用来支撑上游事实。本稿中的产品范围、架构拆分、数据库表名、参数、指标和排期门槛，都是本项目的建议。
- **尚未做的事**：本次编制没有运行 Univer PoC，也没有完成生产压测。

- **[S1] Univer README（dev 分支，1.0.0-rc.0）**：开源与 Pro 的能力边界、兼容性说明。`refer/univer/README.md`；<https://github.com/dream-num/univer>
- **[S2] Univer Releases 与 npm dist-tags**：各版本线的发布时间与标签。<https://github.com/dream-num/univer/releases>；<https://www.npmjs.com/package/@univerjs/core>
- **[S3] Univer API Stability Policy**：稳定、实验性、内部 API 的划分与兼容性说明。`refer/univer/docs/API_STABILITY.md`
- **[S4] Univer 内存泄漏排查指南**：`refer/univer/docs/FIX_MEMORY_LEAK.md`
- **[S5] Node.js Releases**：<https://nodejs.org/en/about/previous-releases>
- **[S6] NestJS 官方文档**：<https://docs.nestjs.com/>
- **[S7] Drizzle 官方文档与许可证**：<https://orm.drizzle.team/docs/overview>；<https://github.com/drizzle-team/drizzle-orm/blob/main/LICENSE>
- **[S8] PostgreSQL Explicit Locking**：<https://www.postgresql.org/docs/current/explicit-locking.html>
- **[S9] PostgreSQL JSON Types**：<https://www.postgresql.org/docs/current/datatype-json.html>
- **[S10] PostgreSQL Continuous Archiving and PITR**：<https://www.postgresql.org/docs/current/continuous-archiving.html>
- **[S11] OWASP Authorization Cheat Sheet**：<https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html>
- **[S12] OWASP Session Management Cheat Sheet**：<https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html>
- **[S13] OWASP Password Storage Cheat Sheet**：<https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html>
- **[S14] OWASP File Upload Cheat Sheet**：<https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html>
- **[S15] OWASP CSV Injection**：<https://owasp.org/www-community/attacks/CSV_Injection>
- **[S16] MDN Web Locks API**：<https://developer.mozilla.org/en-US/docs/Web/API/Web_Locks_API>
