# 只读参考源码（本地自行获取，见 README）
/refer/

# 依赖与构建产物
node_modules/
dist/
dist-e2e/
coverage/
*.tsbuildinfo

# 环境变量
.env
.env.*
!.env.example
# 本机开发的环境变量只有开发库的账号（与 deploy/dev/compose.yaml 相同），入库，新克隆即可 pnpm dev:api
!apps/api/.env.development

# Claude Code 本地文件
.claude/worktrees/
.claude/settings.local.json

# 系统与编辑器
.DS_Store
Thumbs.db
.idea/
*.log

# 测试与构建的输出
.vite/
test-results/
playwright-report/
blob-report/

# lint 规则自测临时创建的探针文件（tools/src/lint/lint-rules.test.ts）
lint-probe-*
